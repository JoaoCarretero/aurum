# MEANREV — Simplified Mean-Reversion Engine

> Resposta direta ao archive do ornstein v1 (2026-04-18 00:15). Hint do Joao:
> "mean reversion com as mms e rsi da pra botar um preco medio na entrada".
> Este engine implementa exatamente isso — nada mais.

## Tese

Mean reversion via deviation do EMA50, confirmada por RSI extremo, alvo
na média. Sem multi-TF consensus. Sem bateria OU/Hurst/ADF/VR. Uma camada
de discriminação (deviation + RSI) em vez de oito AND'd.

Premissa empírica: em cripto 15m, extremos de deviation acompanhados de
RSI confirmatório são estatisticamente menos prováveis de continuar — não
porque o mercado é MR puro, mas porque a combinação "preço X ATRs longe
da média AND RSI em extremo" é regime-neutro o suficiente pra dar edge
positivo. Hipótese testável, não artigo de fé.

## Contra-modelo (não arquiva isso sem validação negativa)

- Se expectativa < 0 em backtest honesto: mean-rev simples NÃO serve pra
  cripto. Arquiva. Lane de mean-rev no AURUM fica fechada.
- Se expectativa ≈ 0 mas com alto sample: é noise-trader. Arquiva.
- Se expectativa > 0 com N razoável: vale tune rigoroso.

## Parâmetros (OrnsteinParams-style, dataclass)

```
deviation_enter: float = 2.0   # z-score threshold (close - ema50) / atr
rsi_long_max: float = 30.0     # long only if RSI <= this
rsi_short_min: float = 70.0    # short only if RSI >= this
atr_stop_mult: float = 2.0     # stop = entry ± atr_stop_mult * ATR
time_stop_bars: int = 96       # bars to hold (96 @ 15m = 24h)
risk_per_trade: float = 0.01   # 1% account risk per trade
tf_exec: str = "15m"
```

Intencionalmente conservador. Se der edge com esses defaults, ótimo; se
precisar tunar, próximo passo.

## Pipeline

```
fetch(15m) → core.indicators.indicators(df)
           → compute deviation = (close - ema50) / atr
           → scan per bar:
              entry_long  = deviation <= -deviation_enter AND rsi <= rsi_long_max
              entry_short = deviation >= +deviation_enter AND rsi >= rsi_short_min
              target = ema50 at entry bar
              stop   = entry ± atr_stop_mult * atr
           → simulate trade path (time stop at N bars)
           → apply C1+C2 cost model
```

## Testes unitários

- `test_entry_long_signal_fires` — sintético: close 2 ATR abaixo do ema50 + rsi 25 → long
- `test_entry_short_signal_fires` — mirror
- `test_no_signal_in_normal_range` — deviation 0.5 → no trade
- `test_rsi_gate_blocks_long` — deviation extremo mas rsi 50 → no trade
- `test_stop_triggers_loss` — path simulation
- `test_target_triggers_win` — path simulation
- `test_time_stop_closes_open_trade` — no hit → close at bar N
- `test_costs_applied_to_pnl` — PnL considera SLIPPAGE+SPREAD+COMMISSION

## Critério de sucesso (smoke test inicial)

Rodar `BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT / 180d`:

- `N_trades >= 30` (sample viável)
- `Sharpe > 0.5` (> random noise)
- `WinRate >= 40%` (mean-rev tipicamente tem WR 40-60% com RR favorável)
- `Expectancy (R) > 0` (positiva)

Se passa os 4: promissor, abre lane de tune formal (novo spec, grid
fechado, DSR, etc). Se falha em qualquer um: arquiva e fecha lane
mean-rev no AURUM (engine simples já é o baseline mínimo; se nem ele dá
edge, não há edge a capturar).

## Escopo fora

- Multi-TF: não.
- Divergência consensus: não.
- OU/Hurst/ADF/VR: não.
- Regime gating (macro BTC trend): não por enquanto. Pode adicionar depois
  se simple version mostrar sinal promissor em parte do tempo.
- Kill-switch portfolio-level: não (é backtest-only engine por enquanto).
- Sizing convexo: não — size fixo por `risk_per_trade / stop_distance`.

---

## Resultados (smoke 2026-04-18)

### Config: default params (mean-reversion direction) — 15m majors/180d

| Métrica | Valor | Critério | Passa? |
|---------|-------|----------|--------|
| N_trades | 1758 | ≥ 30 | ✅ |
| Sharpe | -5.417 | > 0.5 | ❌ |
| WinRate | 37.14% | ≥ 40% | ❌ |
| Expectancy (R) | -0.168 | > 0 | ❌ |
| PF | 0.806 | > 1.0 | ❌ |
| MaxDD | 14.08% | — | — |
| PnL | -$1,393.69 | positive | ❌ |

**Falha 4 de 4 critérios.** Run dir: `data/meanrev/2026-04-18_0011`.

### Config: reverse direction (trend-continuation) com alvo simétrico RR=1:1

| Métrica | Valor | Critério | Passa? |
|---------|-------|----------|--------|
| N_trades | 1904 | ≥ 30 | ✅ |
| Sharpe | -3.442 | > 0.5 | ❌ |
| WinRate | 51.68% | ≥ 40% | ✅ |
| Expectancy (R) | -0.117 | > 0 | ❌ |
| PF | 0.881 | > 1.0 | ❌ |
| MaxDD | 7.84% | — | — |
| PnL | -$688.96 | positive | ❌ |

**Falha 3 de 4.** Melhor que direção mean-rev em tudo, mas ainda anti-edge.
Run dir: `data/meanrev/2026-04-18_0014`.

### Config: default params em 1h TF (time_stop=24h)

| Métrica | Valor | Critério | Passa? |
|---------|-------|----------|--------|
| N_trades | 2188 | ≥ 30 | ✅ |
| Sharpe | -4.119 | > 0.5 | ❌ |
| WinRate | 40.27% | ≥ 40% | ✅ (borderline) |
| Expectancy (R) | -0.152 | > 0 | ❌ |
| PF | 0.852 | > 1.0 | ❌ |
| MaxDD | 22.66% | — | — |
| PnL | -$2,100.63 | positive | ❌ |

**Falha 3 de 4.** TF maior marginalmente melhor em WR mas pior em MDD e PnL.

## Diagnóstico

Sinal `(close - EMA50)/ATR em extremo + RSI em extremo` é essencialmente
**ruído com ligeiro viés trend-continuation** em cripto 15m/1h majors
2025-2026. Três evidências convergem:

1. **Mean-rev direction (37% WR):** perde 63% das vezes — mais que
   aleatório.
2. **Reverse direction (52% WR):** ganha 52% das vezes — levemente
   melhor que aleatório, mas não o suficiente pra cobrir 0.1%
   round-trip cost em 1900 trades.
3. **Expectancy por trade próximo de zero em ambos**: -0.12R a -0.17R —
   o sinal filtra ~2% das barras (1700-2000 trades em 86k bars disponíveis)
   mas as 2% filtradas são estatisticamente indistinguíveis de noise.

**Conclusão (consistente com archive do ornstein v1):** o par "deviation
extrema + RSI extremo" em cripto recente não identifica mean-reversal
nem trend-continuation com edge suficiente. **Não é param tuning que vai
salvar** — o problema é o próprio sinal de trigger.

## Variant search pós-archive (2026-04-18 00:20-00:30)

Joao pediu testar mais variantes antes de arquivar de vez. Engine extendida
in-place com 3 params novos (`regime_filter`, `anchor_type`, `side_filter`) +
ferramenta `tools/meanrev_variant_search.py` pra rodar sweeps em lote.

### Rodada 1 — 12 variantes (mistura de hypotheses)

Run dir: `data/meanrev/variant_search_2026-04-18/`

| Variant | N | Sharpe | WR | PF | ExpR | PnL |
|---|---|---|---|---|---|---|
| V00 baseline_mr | 1757 | -5.40 | 37.2% | 0.81 | -0.168 | -$1,390 |
| V01 regime_low_vol | 993 | -5.09 | 36.2% | 0.77 | -0.200 | -$792 |
| V02 vwap_anchor | 947 | -3.98 | 26.1% | 0.78 | -0.209 | -$1,003 |
| V03 vwap_low_vol | 528 | -5.52 | 22.2% | 0.65 | -0.330 | -$773 |
| V04 long_only | 972 | -5.62 | 35.8% | 0.74 | -0.200 | -$1,125 |
| V05 short_only | 852 | -1.58 | 39.7% | 0.92 | -0.114 | -$261 |
| V06 reverse_low_vol | 1039 | -4.08 | 51.9% | 0.82 | -0.143 | -$484 |
| V07 reverse_vwap | 1076 | -1.64 | 52.3% | 0.92 | -0.093 | -$261 |
| V08 dev4_reverse | 839 | -2.99 | 50.1% | 0.85 | -0.141 | -$416 |
| V09 short_only_reverse | 901 | -4.75 | 49.7% | 0.78 | -0.171 | -$600 |
| **V10 long_only_reverse** | **1017** | **-0.60** | **53.4%** | **0.97** | **-0.069** | **-$93** |
| V11 tight_time_stop | 1962 | -4.98 | 48.6% | 0.83 | -0.132 | -$930 |

**Achado material:** V10 (long-only, reverse direction = entrar LONG em
extreme UP + RSI≥70) chega quase a breakeven — Sharpe -0.60, PF 0.97, WR
53.4%, perde apenas $93 em 1017 trades / 180d. **O "edge" está em
continuation longs de breakout extremo**, não em mean-reversion. Custos
(0.1% round-trip × 1017 trades = ~10% drag) comem o pequeno excesso.

Secundário: V05 (short_only direção original = shortear extreme UP + RSI≥70
como mean-rev) também fica próximo com Sharpe -1.58, PF 0.92. Ambos usam
o MESMO signal de overbought extreme, mas em direções opostas, e ambos
ficam perto de breakeven. O signal tem valor marginal; a direção certa
parece ser continuation (V10 > V05).

V02/V03 (VWAP anchor) degradam performance — o EMA50 é melhor ref.
V01 (regime low_vol) não salva — breakouts limpos precisam de vol.
V08/V11 (deeper dev ou tight time-stop) pioram sample sem melhorar edge.

### Rodada 2 — pushing V10 em cima

Run dir: `data/meanrev/variant_search_2026-04-18/` (overwrite rodada 1).

Variants em cima da V10 baseline. Run dir (overwrite): `data/meanrev/variant_search_2026-04-18/`.

| Variant | N | Sharpe | WR | PF | ExpR | PnL |
|---|---|---|---|---|---|---|
| V10 baseline | 1017 | -0.60 | 53.4% | 0.97 | -0.069 | -$93 |
| **V12 V10_high_vol** | **637** | **+1.66** | **55.9%** | **1.11** | **+0.008** | **+$233** |
| V13 V10_low_vol | 547 | -3.69 | 51.4% | 0.79 | -0.144 | -$316 |
| V14 V10_dev3 | 874 | +0.51 | 55.3% | 1.03 | -0.031 | +$74 |
| V15 V10_dev4 | 448 | +0.26 | 54.2% | 1.02 | -0.046 | +$28 |
| V16 V10_tstop24 | 1040 | -2.85 | 49.0% | 0.86 | -0.111 | -$419 |
| V17 V10_tstop8 | 1180 | -4.37 | 44.2% | 0.79 | -0.119 | -$567 |
| V18 V10_atr3 | 814 | +0.14 | 52.8% | 1.01 | -0.037 | +$29 |
| V19 V10_rsi80 | 1017 | -0.60 | 53.4% | 0.97 | -0.069 | -$93 |
| V20 V10_vwap | 1001 | -0.87 | 53.1% | 0.96 | -0.074 | -$134 |
| V21 V10_composite | 894 | -2.09 | 50.0% | 0.89 | -0.089 | -$288 |
| V22 short_only_reverse_high_vol | 520 | -4.99 | 46.9% | 0.72 | -0.200 | -$527 |

**4 variants com Sharpe positivo:** V12 (+1.66), V14 (+0.51), V15 (+0.26),
V18 (+0.14). Padrão comum: **V10 base + uma restrição adicional**:
- V12: só em HIGH/EXTREME vol regime (filter estrutural)
- V14/V15: deeper extreme (dev 3 ou 4)
- V18: wider stop (3 ATR em vez de 2)

V13 (low_vol) piora ao inverso de V12 — **edge requer alta volatilidade**,
não baixa. Faz sentido: breakouts reais acontecem em vol alta; em vol
baixa, extreme RSI + deviation é mais artefato de ruído.

V16/V17 (tstop mais curto) pioram — precisa dar tempo pra continuation
desenvolver. V19 (rsi_short_min=80) é idêntico a V10 porque RSI raramente
passa de 80 em bars fora do extreme universe que V10 já filtra — empate
vácuo.

### V12 em detalhe — o candidato promissor

```
side_filter: long_only
reverse_direction: True     # entra LONG em extreme UP + RSI>=70
regime_filter: high_vol     # só vol_regime ∈ {HIGH, EXTREME}
(resto: defaults — deviation=2.0, rsi_short_min=70, atr_stop=2.0, time_stop=96)
```

Setup conceitual: "compre breakouts de alta volatilidade quando o preço
dispara acima do EMA50 com RSI overbought." É **momentum continuation de
breakout**, não mean-reversion. Nome `meanrev` mentiria se promovido.

## Decisão (2026-04-18 00:30)

**Não arquiva a lane.** Evidência de edge existe (V12 Sharpe 1.66, PF 1.11,
PnL +$233 em 180d × 5 majors × $10k), mas **precisa validação antes de
promover:**

1. **DSR haircut com n_trials=24** (total de variants testados em 2
   rodadas). Tarefa pra amanhã via `analysis/dsr.py`.
2. **Walk-forward OOS**: segmentar 180d em 3 × 60d, train em 2, test em 1.
   Rotacionar 3x. V12 sobrevive?
3. **Universe sensitivity**: roda em `bluechip_active` (19 símbolos) em
   vez de majors (5 símbolos). Edge generaliza?
4. **Re-nomeação**: se edge sobrevive validações, engine deveria chamar
   `momentum_breakout` (ou similar), não `meanrev`.

Estado atual: `engines/meanrev.py` commitado com extensões. Runs da
variant search preservados em `data/meanrev/variant_search_2026-04-18/`.
Lane aberta pra continuar amanhã.

