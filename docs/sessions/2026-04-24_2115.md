# Session Log — 2026-04-24 21:15

## Resumo
Extensão da sessão 15:44 pra consertar o bug real que bloqueava a visualização de trades paper no cockpit. User reportou no Telegram que AVAXUSDT fechou em TARGET com +$3.56 de pnl mas o cockpit TRADE HISTORY ficava vazio. Diagnose via probe VPS + SSH direto no servidor: flag `primed` invertido no paper runner (regressão de commit anterior eb7c375 "fix(paper): ... primed flag"). Reverti o Bug 2 dessa commit no VPS, mantendo o Bug 1 (exit_at inversion via tz-compare). Patchei trades.jsonl históricos, reiniciei 5 serviços paper. Trades agora visíveis: 3 em millennium paper (AVAXUSDT/OPUSDT/RENDERUSDT), 2 em renaissance paper.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| a30fec3 | feat(cockpit): surface VPS runner trades.jsonl serialization gap | launcher_support/engines_sidebar.py (local) |
| VPS 667614b | fix(paper): revert primed=state.primed in trade writes — eb7c375 regression | tools/operations/millennium_paper.py + _paper_runner.py (feat/per-engine-runners, VPS only) |

## Mudanças Críticas
**ATENÇÃO**: reverti parcialmente o commit `eb7c375` de outro agente no branch `feat/per-engine-runners` do VPS. **Trade records paper agora são escritos com `"primed": False` hardcoded** (era `state.primed`). A semântica correta do flag `primed` em trade records é "warmup residue — filter out"; como paper só escreve trades REAIS no trades.jsonl, eles nunca são warmup. Heartbeat writes mantidos como `state.primed` (legitimamente refletem estado do runner).

Não mexi em `core/indicators.py`, `core/signals.py`, `core/portfolio.py`, `config/params.py` — zero CORE.

## Achados
- **Bug 2 do commit eb7c375 regride visibilidade de trades**: Codex/agente anterior viu trades escritos com `primed=False` hardcoded e assumiu que era bug — trocou pra `state.primed`. Mas `state.primed` fica `True` APÓS warmup (primeiro tick), então TODOS os trades reais passaram a ter `primed=True`. O filtro default do endpoint `/v1/runs/{id}/trades` (`tools/cockpit_api.py:299`) dropa `primed=True`, pensando que é warmup residue (semântica correta vinda do shadow). Resultado: todos os paper trades invisíveis no cockpit pós-commit.
- **Telegram + heartbeat + account consistentes**: enquanto o trades.jsonl registrava o trade corretamente (com primed flipado), os 3 canais de notificação/estado já mostravam equity +$3.56 e `trades_closed=1`. Só o endpoint do cockpit e meu aggregator filtravam. Esse ponto foi a única pista visível no início — bastou SSH no VPS pra ver trades.jsonl crua.
- **Bug 1 do commit eb7c375 continua válido** (exit_at inversion via tz-compare): deixei intacto. Só reverti o Bug 2 (primed).
- **Semântica `primed` consolidada**: trade records = "warmup signal to skip" (filter default drops True). Heartbeat = "runner has completed warmup" (reflete state.primed).

## Estado do Sistema
- **Tests local**: 2034 passed (suite completa), 92 targeted cockpit paper
- **VPS services**: 12/12 active, 5 paper reiniciados com novo código pós-commit 667614b
- **Tunnel SSH**: UP (port 8787)
- **Cockpit aggregator**: millennium paper 3 trades visíveis (AVAXUSDT +$3.56, OPUSDT -$0.98, RENDERUSDT +$5.81), renaissance paper 2 trades
- **Backlog restante**:
  - Sync VPS→DB automático (ainda manual)
  - Posição órfã LINKUSDT SHORT em `2026-04-21_012204` (millennium paper stopped, VPS hb still running com posição)
  - feat/per-engine-runners não merged em main — próxima sessão pode cherry-pick o fix 667614b
- **Próximo passo sugerido**: abrir launcher, validar visualmente que TRADE HISTORY mostra os 3 trades de millennium paper (AVAXUSDT visível).

## Arquivos Modificados
**Local:** (nenhum — branch feat/research-desk já tinha `"primed": False` no millennium_paper.py local, o bug só existia em feat/per-engine-runners que é a branch deployada no VPS)
**VPS:**
- `tools/operations/millennium_paper.py` (4 trade-write sites: `state.primed` → `False`)
- `tools/operations/_paper_runner.py` (4 trade-write sites: idem)
- `data/millennium_paper/2026-04-24_174017p_desk-paper-a/reports/trades.jsonl` (primed:true → false)
- `data/millennium_paper/2026-04-24_174018p_desk-paper-b/reports/trades.jsonl` (idem)

## Notas para o Joao
- **AVAXUSDT LONG +$3.56 agora aparece no cockpit** (millennium paper, run `2026-04-24_174017p_desk-paper-a` e sua replica `_174018p_desk-paper-b`). Abre launcher.py, aba PAPER, clica em MILLENNIUM, a seção TRADE HISTORY deve listar 3 trades: AVAXUSDT, OPUSDT, RENDERUSDT.
- **Atenção ao que Codex/outros agents fazem com o VPS**: o commit `eb7c375` foi feito hoje às 14:36 (antes das minhas sessões). Quem fez viu uma audit filtrando `primed=True` e assumiu que todos os trades deveriam ter primed=True. Na verdade a audit é que estava errada (ou tinha semântica legacy). Se quiser me contar sobre qual audit é essa, posso revisar/corrigir ela também — agora ela está certamente quebrada (paper trades = primed=False sempre).
- **`feat/per-engine-runners` (VPS branch) não tá merged em main ainda**. Meu revert 667614b vive só lá. Quando for mergear essa branch pra main numa próxima sessão, o commit `eb7c375` vai vir junto — precisa ver se cherry-pick do 667614b preserva a correção ou se precisa squash.
- **Banner defensivo no cockpit** (`a30fec3` local): deixei pra detectar mismatches futuros entre `account.trades_closed` e `/v1/runs/.../trades` count. Se o bug reaparecer por qualquer motivo (regressão, novo flag, filter bug), o operator vê um warning amber em vez de silêncio.
- **O SSH tunnel tá durando pouco hoje** — caiu 3+ vezes na sessão. Checar configuração `ClientAliveInterval`/`ClientAliveCountMax` no `/etc/ssh/sshd_config` do VPS se ficar repetitivo.
