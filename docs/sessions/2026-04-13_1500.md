# Session Log — 2026-04-13 15:00

## Resumo
Sessão de diagnóstico profundo: todos os engines foram tornados operacionais,
testados com param grid search, filtro HTF fractal, e validação de robustez 180d.
Nenhum engine passou o teste de robustez para paper trading, mas DE SHAW 4h e
CITADEL 15m mostraram edge em janelas de 90d. Bug crítico de BTC macro encontrado
e corrigido em JUMP e RENAISSANCE.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 7e69566 | feat(infra): Phase C — atomic persistence, health ledger | core/transport,health,persistence,fixture_capture,versioned_state + bot,launcher,live |
| b8b9c6d | fix(backtest): restore HTML report + HMM fix | engines/backtest.py, core/chronos.py |
| 70989f0 | feat(engines): DE SHAW + BRIDGEWATER operational | engines/newton.py, engines/thoth.py, core/sentiment.py, config/params.py |
| 9ae3db4 | feat(tools): param_search grid + THOTH direction threshold | tools/param_search.py, config/params.py, engines/thoth.py |
| 3107f31 | feat(core): HTF alignment filter + A/B test | core/htf_filter.py, core/htf.py, tools/htf_ab_test.py |
| 54f5975 | fix(engines): BTC macro fetch JUMP + RENAISSANCE | engines/mercurio.py, engines/harmonics_backtest.py, tools/baseline_all.py |
| a90d02d | feat(core): contrarian HTF + Phase 4-6 tests | core/htf_filter.py, tools/phase456_test.py |

## Mudanças Críticas

**ATENÇÃO: Mudanças em lógica de trading detectadas e documentadas:**

1. **HMM fix (chronos.py):** `np.nanargmax` crashava com ValueError em all-NaN slices.
   Fix: preencher NaN rows com zeros antes do argmax, mascarar depois. Não muda
   comportamento do trading (HMM é observation-only em Fase 3).

2. **BTC macro bug (JUMP + RENAISSANCE):** Ambos engines chamavam `detect_macro()`
   mas NÃO incluíam BTCUSDT no fetch. Resultado: macro SEMPRE "CHOP", sizing a 45%.
   Fix aplicado. Impacto no trading: sizing agora correto (100% em BEAR, 85% em BULL).
   Testado: não desbloqueou edge em nenhum dos dois engines.

3. **OI endpoint fix (sentiment.py):** URL errada (`futures.binance.com` → `fapi.binance.com`)
   causava HTTP 202 em todos os símbolos. OI agora funciona.

4. **core/htf.py fix:** `import core.indicators as _ind` resolvia para função em vez de
   módulo devido ao `core/__init__.py`. Fix: usar `sys.modules["core.indicators"]`.

5. **Penalty active-only (branch experiment/omega-3d):** `score_omega` penalty agora
   considera apenas componentes com peso > 0. Preservado no branch, não no main.

## Achados

### Grid Search Results
- **DE SHAW 15m:** 24 combos, best Sharpe -0.887. Sem edge.
- **BRIDGEWATER 15m:** 25 combos, best Sharpe -0.485. Quase breakeven.
- **Thresholds não são o gargalo do BRIDGEWATER** — todos os top 10 combos convergem para o mesmo resultado (1171t, Sharpe -0.485).

### HTF A/B Test
- **CITADEL:** HTF desnecessário (baseline Sharpe 5.15, HTF 4.90)
- **BRIDGEWATER:** HTF genérico PIOROU (-0.485 → -1.291). Contrarian HTF cortou MaxDD pela metade (13.8% → 6.2%) mas Sharpe igual.
- **DE SHAW:** marginal (+0.012 Sharpe)
- **JUMP:** marginal (+0.036 Sharpe)

### Timeframe Tests (descoberta principal da sessão)
- **DE SHAW 15m:** Sharpe -2.21
- **DE SHAW 1h:** Sharpe +0.42 (449t, MC 61%)
- **DE SHAW 4h:** Sharpe +2.04 (113t, MC 95%, MaxDD 3.6%)
- **JUMP 5m:** Sharpe -11.4 (ruído puro)
- **JUMP 1h:** 4 trades (sem significância)

### Robustez 180d
- **CITADEL 15m 180d:** Sharpe 0.39, OOS -$441, WR 42.9%. Edge existe mas não robusto.
- **DE SHAW 4h 180d:** Sharpe -0.86, apenas 1 par cointegrado (vs 11 em 90d). Edge efémero.

### Bug de BTC macro
- Afetava JUMP e RENAISSANCE (sizing a 45% permanente)
- Corrigido em ambos
- NÃO desbloqueou edge em nenhum engine

## Estado do Sistema
- Smoke test: 170/170
- Engines operacionais: CITADEL, DE SHAW, BRIDGEWATER, JUMP, RENAISSANCE
- Branch experiment/omega-3d: preservado (omega 3D + stop 2.1 + penalty fix)
- Main: params originais (5D, stop 1.8)

## Arquivos Modificados
46 arquivos, +4454 / -126 linhas (inclui trabalho do Codex em infra)

Principais:
- `core/htf_filter.py` (+134) — filtro HTF genérico + contrarian
- `core/htf.py` (+5/-2) — fix sys.modules import
- `core/chronos.py` (+10/-4) — fix HMM all-NaN crash
- `core/sentiment.py` (+1/-1) — fix OI endpoint URL
- `config/params.py` (+9/-3) — HALFLIFE_MAX, MIN_PAIRS, THOTH thresholds
- `engines/newton.py` (+133/-5) — UTF-8, argparse, macro fetch, persistence, macro fix
- `engines/thoth.py` (+147/-5) — UTF-8, argparse, macro fetch, OI renorm, persistence
- `engines/mercurio.py` (+90/-29) — macro fetch, argparse cleanup
- `engines/backtest.py` (+15/-1) — HTML report wiring, ablation label
- `engines/harmonics_backtest.py` (+218) — macro fetch + validation entry
- `tools/param_search.py` (+355) — grid search via monkey-patch
- `tools/htf_ab_test.py` (+268) — A/B test framework
- `tools/phase456_test.py` (+257) — timeframe comparison tests
- `tools/baseline_all.py` (+173) — multi-engine baseline

## Notas para o Joao

1. **Nenhum engine está pronto para paper trading.** O CITADEL é o mais promissor
   (Sharpe 0.39 em 180d, MC 76%) mas OOS negativo. Não overstate.

2. **O edge do CITADEL é regime-dependent.** Funciona em BEAR (WR 56%, +$443)
   mas perde em BULL (WR 46%, -$179). Regime-adaptive sizing seria o próximo passo.

3. **DE SHAW funciona em 4h mas cointegração crypto é efémera.** 11 pares em 90d,
   1 par em 180d. O engine precisa de rolling cointegration (recalcular pares
   periodicamente) para ser viável em live.

4. **BRIDGEWATER está a $0.13/trade do breakeven.** O filtro contrarian HTF
   melhorou MaxDD mas não Sharpe. Possível próximo passo: ensemble com CITADEL
   (momentum + contrarian = diversificação natural).

5. **JUMP não tem edge em nenhum timeframe testado (5m, 15m, 1h).** Classificar
   como ❌ no estado atual. O CVD/order flow pode funcionar com dados de book
   reais (L2), mas com OHLCV+tbb é ruído.

6. **O Codex commitou infra (Phase C, health ledger, transport) no commit 7e69566.**
   Revisado: código sólido, não toca em lógica sagrada.

7. **Branch experiment/omega-3d preservado.** Contém omega 3D + stop 2.1 + penalty
   active-only. Threshold precisa recalibração (0.53 over-filters, 0.45 under-filters).
   Sweet spot entre 0.47-0.50 não testado ainda.

8. **Ferramentas criadas nesta sessão:**
   - `tools/param_search.py` — grid search reutilizável para qualquer engine
   - `tools/htf_ab_test.py` — A/B baseline vs HTF filtered
   - `tools/phase456_test.py` — teste de timeframes multi-engine
   - `tools/baseline_all.py` — baseline completo de todos os engines
   Todas funcionam via CLI com --days, --basket, --engines.
