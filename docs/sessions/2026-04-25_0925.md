# Session Log — 2026-04-25 09:25

## Resumo

Continuação do dia depois da auditoria 360° matinal. Joao pediu "fix tudo e limpe" sobre o backlog operacional (DB zombies, orphan position, primed fix VPS-only, SSH tunnel keepalive); todos os 4 fechados. Em seguida pediu polish visual do main menu (4 tiles mostrando só 2 children cada, queria todos visíveis) e consertou o lag do ENGINES LIVE (engines não apareciam até clicar — race no async refresh). Audit independente do feature-dev:code-reviewer pegou 1 issue real (overflow defensivo no tile com 8º item futuro), corrigido. Zero CORE.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| (este) | docs(sessions+days): 2026-04-25_0925 — cleanup VPS + main menu + engines lag | docs/sessions/2026-04-25_0925.md, docs/days/2026-04-25.md |
| (-3) | refactor(runs-history): consolidate local collect_vps_runs to fast path | launcher_support/runs_history.py |
| (-2) | fix(engines-live): warm cockpit cache sync on first paint | launcher_support/engines_live_view.py |
| (-1) | feat(main-menu): show all tile children + clip overflow guard | launcher.py |

## Mudanças Críticas

Nenhuma mudança em lógica de trading (sinais, custos, sizing, risco). Tudo foi UI/UX e ops-cleanup.

VPS-side (não em repo, mas operacional):
- `cleanup_stale_rows(30)` no DB — 0 rows afetadas (era falso positivo, `_effective_status` no cockpit_api já marca como stopped via staleness fallback)
- Heartbeat de `data/millennium_paper/2026-04-21_012204/state/heartbeat.json` reescrito de `running` → `stopped` (orphan LINKUSDT SHORT, 4 dias stale)
- Push de `667614b fix(paper): revert primed=state.primed` pra `origin/feat/per-engine-runners` (FF, era VPS-only single-point-of-failure)

## Achados

- **`_VPS_RUNS_CACHE`/`_DB_RUNS_CACHE` no `runs_history.py` são caches de 2s** — TTL curto demais pra runs stopped (deveria ser >5min). Não troquei agora pra evitar mudança de comportamento; nota pro backlog.
- **`collect_vps_runs` no `launcher_support/runs_history.py` é dead code** — sombreado por `from core.ops.run_catalog import collect_vps_runs` na linha 487 (late import wins). Reescrevi pra fast path por consistência (run_catalog já tinha), mas em runtime nada mudou. Reviewer confirmou via grep cross-repo que ninguém importa o local def.
- **`_schedule_state_refresh` tem race no flag `_refresh_scheduled`** — quando worker async completa entre dois refreshes regulares, debounce coalesce sem rerender. Não consertei o root (precisaria de signal/event), mas resolvi o sintoma fazendo o first refresh ser sync.
- **Main menu tile 200px aceita 7 rows × 17px stride** — MARKETS (7 children) cabe com 15px de slack. Um 8º item futuro overflowaria 2px sobre o footer (silencioso). Clip defensivo `max_rows = (y2 - 18 - preview_y) // 17` garante o limite.
- **`feat/per-engine-runners → main` é cherry-pick cirúrgico, não cleanup**: 211 commits em main vs 57 no branch desde o merge base (`aa0579b`, 2026-04-19). Dry-run merge produz 12 conflitos, dois deles modify-vs-delete em `engines/deshaw.py`+`engines/ornstein.py` (main deletou em Phase 1, branch ainda modifica). Tarefa de sessão dedicada.

## Estado do Sistema

- **Suite**: 2118 passed / 9 skipped (full minus `tests/core/test_arb_hub_v2.py` env-flake — embora o root cause foi achado e corrigido na sessão 09:21 hoje, eu não rebuildei a env aqui pra validar). Targeted: 72/72 main_menu, 92/92 engines_live + cockpit, 48/48 runs_history.
- **Cockpit live**: 11 running + 150 stopped (efetivo correto via `_effective_status`)
- **Tunnel SSH**: UP via ~/.ssh/config alias `aurum-vps` (ServerAliveInterval=30, CountMax=4)
- **VPS services**: 12/12 active (cockpit_api + probe + 5 engines × paper/shadow)
- **Posição órfã LINKUSDT**: heartbeat marcado stopped; positions.json preservado como histórico
- **Backlog restante** (do daily log da manhã + adições):
  - Decisões pendentes Joao: BW archive/quarentena, PHI re-run/archive, anomaly gate signal, live.py extraction skip-this-sprint
  - Reconcile `data/index.json` (17 missing dirs)
  - Stashes triagem (3 itens)
  - **NOVO**: Cherry-pick cirúrgico `feat/per-engine-runners → main` (extrair só os runners por engine + commits 151a49b/a9a4c8c, sem trazer modify-on-deleted-engines)
- **Próximo passo sugerido**: rodar `python launcher.py`, validar visualmente: (1) main menu mostra todos children dos 4 tiles; (2) ENGINES LIVE abre com sidebar populada após ~1s de warm-fetch (sem precisar clicar).

## Arquivos Modificados

```
launcher.py                           | 16 ++++----    (-9 / +7)
launcher_support/engines_live_view.py | 19 ++++++++-   (-1 / +18)
launcher_support/runs_history.py      | 74 +++++++++++ (-23 / +51)
3 files changed, 76 insertions(+), 33 deletions(-)
```

## Notas para o Joao

- **Cherry-pick `feat/per-engine-runners → main`** ficou pendente. Mostrei o diff stat (335 files, +16k -44k) e os 12 conflitos do dry-run; recomendei sessão dedicada com plano em vez de tentar limpar agora. Não fiz `/schedule` automático porque o trabalho precisa de você decidindo cada split (quais files trazer additive vs quais skipar). Posso plantear um plano em qualquer momento que você quiser.

- **A "trava" do ENGINES LIVE virou explícita**: em vez de tela vazia esperando refresh fantasma, agora bloqueia ~1s na primeira entrada (warm-fetch via SSH tunnel) e abre populated. Subsequente é instantâneo (cache 60s). Se VPS down, bloqueia 5s (cockpit timeout), depois abre vazio mas funcional.

- **Reviewer apontou 1 false-positive sobre allow_sync NÃO bloquear** — eu mostrei que linhas 416-421 de `_load_cockpit_runs_cached` rodam síncrono APÓS o `with` block (o flag `loading=True` apenas previne async caller concorrente de spawnar duplicate). Reviewer retratou. Mantenho a confiança no fix.

- **`runs_history.py` ainda tem código morto** que não removi: `_collect_single_vps_run` linha 259 + minha rewrite de `collect_vps_runs` linha 188 (ambas sombreadas pelo import linha 487). Decidi NÃO remover porque ambas servem como safety net se alguém algum dia importar via path direto, e remoção seria diff maior sem ganho operacional. Se preferir limpar, é um diff de ~140 linhas pro lixo.

- **Hub drift, smoke, MEMORY** todos verdes. CORE intocado.
