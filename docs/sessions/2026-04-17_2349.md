# Session Log — 2026-04-17 23:49

## Resumo

Sessão de validação do ORNSTEIN v2 (preset `robust` + wrapper que Codex
havia escrito ontem à noite). Spec pré-registrada + plano executável +
protocolo de 4 etapas rodado com disciplina anti-overfit. **Baseline v1
default em `bluechip_active / 360d` retornou 0 trades**, invalidando a
premissa de v2 e curto-circuitando o resto do protocolo. Decisão final:
**archive v2** sem promover, sem iterar. Lane 2 (hardening não
relacionado) separada em commit próprio antes do archive.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 92f7805 | docs(ornstein): v2 validation spec — 4-step protocol + decision matrix | docs/superpowers/specs/2026-04-17-ornstein-v2-validation-design.md |
| c50cfcd | docs(ornstein_v2): implementation plan — 10 tasks, 3 decision branches | docs/superpowers/plans/2026-04-17-ornstein-v2-validation.md |
| c21aa01 | hardening: run_manager compare lookup + vps host parse + cockpit order | core/run_manager.py, launcher_support/bootstrap.py, launcher_support/engines_live_view.py + 3 test files |
| 8be7416 | docs(ornstein_v2): validation report skeleton — to be filled in-flight | docs/engines/ornstein_v2/2026-04-17_validation.md |
| 6197334 | docs(ornstein_v2): etapa 1 baseline zeroed — protocol short-circuit | docs/engines/ornstein_v2/2026-04-17_validation.md |

## Mudanças Críticas

Nenhuma mudança em lógica de trading. Lane 1 (preset `robust`, 5 params
novos em `OrnsteinParams`, wrapper `engines/ornstein_v2.py`, 3 testes
novos, registry entry `ornstein_v2`) foi **revertida via `git checkout
HEAD`** — nunca chegou a ser commitada. Engine v1 (`engines/ornstein.py`
no commit `a1fb95e`) intocado.

Lane 2 (3 hardenings não relacionados) foi commitada (c21aa01) mas não
toca core de trading:
- `core/run_manager._resolve_compare_run_dir` — compare_runs aceita
  run_id ou caminho explícito, resolve via índice quando layout canônico
  não se aplica.
- `launcher_support/bootstrap` — VPS host normalize aceita `user@host`;
  fail-closed quando host não configurado.
- `launcher_support/engines_live_view` — cockpit KPI order reorg +
  bucket_header_title helper.

## Achados

**ACHADO CRÍTICO (fora do escopo original):** ORNSTEIN v1 em config
default produz 0 trades em `bluechip_active / 360d` (19 símbolos, 1 ano
de dados). Vetos dominantes:
- `no_divergence`: 316,766 (47.9%)
- `rsi_block`: 306,119 (46.3%)
- `hurst_block`: 36,945 (5.6%)

Os filtros de trigger (divergência multi-TF + RSI) estão estrangulando o
sinal ANTES da bateria estatística (OU / Hurst / ADF / VR) sequer
avaliar. Isso contradiz a premissa do engine de "filtros estatísticos
pesados". O commit `a1fb95e` marca v1 como "research-only until
overfit_audit 6/6 passes" — este audit confirma que não dá nem pra rodar
overfit_audit sem sample.

**Implicação:** a premissa de v2 (adicionar guards sobre v1 pra reduzir
falsos positivos) era vazia porque v1 já estava em zero. Codex ontem
tentou iterar os thresholds sem rodar baseline — fishing expedition
clássico que o anti-overfit protocol proíbe.

**Backlog novo (issue separada):** revisar `compute_fractal_divergence`
e `compute_rsi_consensus` em `engines/ornstein.py` — thresholds podem
estar herdados da spec original em universo diferente e não se traduzem
pra cripto 2025-2026.

## Estado do Sistema

- Suite: `tests/test_ornstein.py` 20/20 passed (pós-revert, sem os 3
  testes novos da Lane 1). Lane 2 tests 89/89 passed.
- Backlog restante pós-archive:
  1. v1 signal generator audit (novo, escopo próprio)
  2. BACKTEST hierarchy refactor (ainda pendente em worktree
     `.worktrees/backtest-hierarchy`)
  3. Merge decision do branch `feat/phi-engine` (58+ arquivos, Codex
     sugeriu split em 3 PRs)
- Próximo passo sugerido: dormir. Dia denso. Decisão de arquivar v2 é o
  fechamento correto — não dá pra validar edge contra zero.

## Arquivos Modificados

```
docs/superpowers/specs/2026-04-17-ornstein-v2-validation-design.md  | +231 (novo)
docs/superpowers/plans/2026-04-17-ornstein-v2-validation.md         | +1117 (novo)
docs/engines/ornstein_v2/2026-04-17_validation.md                   | +118 (novo)
core/run_manager.py                                                  |  +24/-2
launcher_support/bootstrap.py                                        |  +24/-6
launcher_support/engines_live_view.py                                |  +51/-10
tests/test_run_manager_contracts.py                                  |  +26/-2
tests/test_launcher_bootstrap_contracts.py                           |  +35/-3
tests/test_engines_live_view.py                                      |  +22/-5
```

Lane 1 (revertido, não aparece em git log):
- engines/ornstein.py — 5 params novos + preset `robust` + lógica de
  guards em `_resolve_ornstein_exit` e `scan_symbol` — revertidos via
  `git checkout HEAD`.
- config/engines.py — entry `ornstein_v2` revertida.
- tests/test_ornstein.py — 3 testes novos revertidos.
- engines/ornstein_v2.py — wrapper untracked, deletado via `rm`.

## Notas para o Joao

- **Archive é a decisão honesta.** Codex ontem tentou "endurecer" v2 sem
  baseline. Baseline provou que v1 já está em zero. Não há o que
  endurecer. O protocolo anti-overfit economizou ~1 hora de ablation
  inútil hoje.
- **O v1 precisa de um audit separado.** Quando quiser, me avisa que
  abro spec nova. Hipótese inicial: `MTF_MIN_CONFIRM`, `rsi_htf_min_confirm`
  ou `htfs_min_consensus` estão calibrados pra outro mercado/timeframe.
- **Lane 2 (hardening) ficou commitada** — `c21aa01`. Inclui melhorias
  úteis no `compare_runs` e VPS config que valem independente do v2.
- **Branch `feat/phi-engine` segue** sem divergir do que você + Codex
  vinham fazendo. 5 commits meus hoje à noite (spec/plan/hardening/skel/
  baseline doc). Codex tocou 2 commits em paralelo (`8f8a627` janestreet
  test default, `3ca4c73` cockpit spec) — não conflitam com meu escopo.
- **Memory updated:** `project_engine_status_2026_04_16_oos.md` precisa
  adicionar que ornstein v1 em config default produz 0 trades em bluechip
  1y — ajustar na próxima sessão.
