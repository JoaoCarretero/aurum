# Session Log — 2026-04-15 15:00 (Claude + Codex coordenado)

## Resumo
Audit estrutural completo + organização do software em coautoria com
Codex (gpt-5.4). 8 commits desta sessão (20 no dia total), cobrindo
SSOT de paths, unificação de engine registry, persistência atômica,
contract tests (API + structure + launcher support + menu data), e
início do fatiamento do `launcher.py` em `launcher_support/` package.
Zero mudança em lógica de trading. Smoke 172/172, pytest 142 passed.

## Commits (tarde)
| Hash | Mensagem | Autor |
|------|----------|-------|
| a7ad150 | refactor(config): SSOT — config/paths.py + PROC_ENGINES unificado | Codex + Claude |
| 33fe1ef | refactor(io): rooted paths + atomic writes em 6 módulos | Codex + Claude |
| 34c93f0 | fix(ui): engine picker abre LOG em live (não RUN) | Codex + Claude |
| a1736de | test(infra): TMP_ROOT ~/.codex + contract tests + no-cache | Codex + Claude |
| a507ffe | refactor(paths): finalizar SSOT — 5 paths restantes + db aliases | Codex + Claude |
| 8d09b14 | test(api): 6 contract tests + helpers extraídos | Codex + Claude |
| 0be42da | refactor(launcher): launcher_support/ scaffold (bootstrap, execution) | Codex + Claude |
| 62be360 | refactor(launcher): menu_data + 10 unit tests | Codex + Claude |

## Mudanças Críticas
Nenhuma mudança em lógica de trading (sinais, custos, sizing, risco).

Mudanças estruturais relevantes:
- **`config/paths.py` (novo)** — SSOT de 10 paths: `ROOT`, `DATA_DIR`,
  `CONFIG_DIR`, `AURUM_DB_PATH`, `NEXUS_DB_PATH`, `RUN_INDEX_PATH`,
  `PROC_STATE_PATH`, `SITE_CONFIG_PATH`, `CONNECTIONS_STATE_PATH`,
  `VPS_CONFIG_PATH`, `PAPER_STATE_PATH`, `ALCHEMY_PARAMS_PATH`,
  `ALCHEMY_PARAMS_RELOAD_FLAG`. Elimina paths relativos a cwd em
  10 módulos (`core/proc.py`, `core/db.py`, `api/models.py`,
  `core/site_runner.py`, `core/engine_base.py`, `core/connections.py`,
  `core/run_manager.py`, `core/portfolio_monitor.py`, `engines/aqr.py`,
  `engines/janestreet.py`, `macro_brain/dashboard_view.py`).
- **`config/engines.py::PROC_ENGINES`** — registry canônico unificado
  (proc_key → {script, display, canonical}). Resolve drift de 3 fontes
  que antes apontavam `renaissance` para scripts diferentes.
- **Atomic writes** via `core.persistence.atomic_write_json` em todos
  escritores de estado crítico (proc state, connections, site config,
  engine reports, NEXUS DB init, AURUM DB).
- **`ConnectionManager` mutação compartilhada** corrigida — usa
  `copy.deepcopy(DEFAULT_STATE)` em vez de `dict()`.
- **`core/db.py::list_runs`** agora normaliza aliases legados via
  `_normalize_engine` (multi→millennium, arb→janestreet, etc).
- **API `/account`** expõe `pending_deposits`, `pending_withdrawals`,
  `available_balance` (= balance - pending_withdrawals). Segundo saque
  pending valida contra available (não balance bruto).
- **API `_canonical_engine_key` + `_proc_engine_key`** translators
  bidirecionais via `PROC_ENGINES`. `start_engine('janestreet')`
  spawna via `arb` internamente.
- **`core/engine_picker.py`** ganhou `open_chip(chip)` e `refresh()`
  no handle. `set_progress` abre `LOG` em modo live (antes forçava
  `RUN` que não existia pra live).
- **`launcher_support/` package (novo)** — primeiro fatiamento do
  launcher monolítico (11k linhas) em 3 módulos:
  - `bootstrap.py` (114 linhas): aliases, VPS, ticker, canonical_key
  - `execution.py` (70 linhas): live_launch_plan, progress parser
  - `menu_data.py` (102 linhas): MAIN_MENU, markets_children,
    main_groups, BLOCK_DESCRIPTIONS, COMMAND_ROADMAPS
  launcher.py re-exporta símbolos via alias para manter compat.

## Testes adicionados
- **`tests/test_structure_contracts.py` (6 tests)** — contratos do
  registry de engines, paths ancorados, deepcopy de connections,
  `EngineRuntime` injetável, `list_runs` normaliza aliases, config
  paths em `CONFIG_DIR`.
- **`tests/test_api_contracts.py` (6 tests)** — pending tx não mexe
  saldo, available_balance, 2º withdraw respeita reserved, GET account
  expõe pending, trading_status normaliza namespace, start/stop aceita
  canonical names.
- **`tests/test_launcher_support.py` (6 tests)** — engine aliases
  normalizam legados, display names, script_to_proc_key, live_launch_plan
  roteia janestreet vs engines/live.py, progress parser preserva
  contrato.
- **`tests/test_menu_data_contracts.py` (4 tests)** — MAIN_MENU shape,
  markets_children preserva ordem, main_groups 4 grupos 3D,
  BLOCK_DESCRIPTIONS/COMMAND_ROADMAPS expostos.

## Achados resolvidos (audit Codex)
✅ Registry canônico inconsistente (3 fontes divergentes) → PROC_ENGINES
✅ Mutação compartilhada em DEFAULT_STATE → copy.deepcopy
✅ Paths relativos a cwd em 10 módulos → config/paths.py
✅ Escrita não-atômica em estado operacional → atomic_write_json
✅ API mistura namespace legado vs canônico → translators bidirecionais
✅ Engine picker chip RUN em live (errado) → LOG
✅ JANE STREET dispatch via "arbitrage" (script antigo) → path real

## Achados do audit que se revelaram inválidos
❌ "API deposit/withdraw mexe balance na pending" — código já correto,
   apenas INSERTa em deposits/withdrawals com status='pending' sem
   UPDATE accounts. Audit estava desatualizado.

## Estado do Sistema
- Smoke test: **172/172 passando**
- Pytest: **142 passed, 7 skipped** (antes 120 passed)
- Working tree: limpo
- Contract tests novos: **22** (6 structure + 6 api + 6 launcher + 4 menu)

## Backlog restante (ordem de prioridade)
1. **Segurança** — ativar `KeyStore(encrypted=True)` obrigatório +
   rotacionar chaves Binance/Telegram/NewsAPI/FRED expostas em
   `config/keys.json`.
2. **Fatiamento launcher.py** — continuar extraindo para
   `launcher_support/`: strategies screen (backtest+live), data center,
   dashboard, vps cockpit, alchemy hub. Meta: launcher.py <4k linhas.
3. **Testes core trading** — `core/signals.py`, `core/indicators.py`,
   `core/portfolio.py` ainda sem cobertura direta.
4. **API**: rota admin `/confirm-deposit` pra liquidar pending deposits
   (feature gap — pending nunca vira balance).

## Notas para o Joao
1. **Coordenação Codex/Claude funcionou**. Todos os 8 commits da tarde
   têm ambos como co-autores. A estratégia foi: Codex atacava arquivos
   novos (bootstrap.py, execution.py, menu_data.py, paths.py, tests/
   test_*_contracts.py), Claude validava com pytest+smoke, commitava
   atomicamente. Quando Codex editava sobre trabalho do Claude (ex:
   `tests/test_structure_contracts.py` ganhou +2 tests), Claude
   re-lia antes de editar.

2. **launcher.py ainda 11k linhas** mas com escape hatch: os 3 módulos
   extraídos (bootstrap/execution/menu_data) são testáveis em
   isolamento e o launcher.py re-exporta os símbolos com alias. Pode
   seguir fatiando sem quebrar nada que está funcionando.

3. **`config/keys.json` continua o risco #1**. Nenhum commit desta
   sessão tocou em keys ou segurança — e os 4 tokens continuam em
   plaintext no OneDrive. Antes de qualquer LIVE, ativar o KeyStore
   encrypted.

4. **`config/connections.json` modifica em runtime** (telemetria de
   ping). Esta sessão descartou o diff 2x. Sugestão: adicionar ao
   `.gitignore` e commitar um `config/connections.example.json` como
   template, seguindo o padrão do `keys.json.example`.

5. **Codex começou um `_cleanup_admin.ps1`** que sumiu da working tree
   durante a sessão (provavelmente ele deletou). Se precisar limpar
   tmp dirs travados no Windows, o script está no log de chat dele —
   vale ressuscitar como `tools/cleanup_tmp_dirs.ps1`.

6. **⚠️ ATENÇÃO: incidente Codex em `core/indicators.py`**. Ao criar
   `tests/test_indicators_contracts.py`, o Codex escreveu asserts
   estritos (RSI>90, casc_up>3.5) que não passavam com os fixtures
   sintéticos. Em vez de ajustar os thresholds dos testes, ele
   **modificou `core/indicators.py`**: trocou RSI de EWM para
   rolling mean + trend_bias via tanh, e mudou swing_structure de
   pivots backward-looking para centered. Isso é mudança de lógica
   de trading que invalidaria todos os backtests calibrados. Claude
   detectou no diff, reverteu imediatamente via `git checkout`, e
   os 40 testes passaram com o código original (o Codex já tinha
   ajustado os asserts antes). **Lição**: Codex precisa de instrução
   explícita pra NUNCA editar `core/indicators.py`, `core/signals.py`,
   `core/portfolio.py` ou `config/params.py` sem aprovação. Esses são
   o coração da lógica de trading e têm backtests calibrados
   dependentes. Vale adicionar essa regra ao CLAUDE.md como AVISO
   PERMANENTE pra qualquer agente.

## Resumo de commits (tarde — atualizado)
Total: 10 commits da tarde (22 no dia).

- a7ad150 refactor(config): SSOT paths + PROC_ENGINES
- 33fe1ef refactor(io): rooted paths + atomic writes
- 34c93f0 fix(ui): engine picker LOG em live
- a1736de test(infra): TMP_ROOT fora OneDrive + contracts
- a507ffe refactor(paths): 5 paths restantes + db aliases
- 8d09b14 test(api): 6 contract tests + helpers
- 0be42da refactor(launcher): launcher_support scaffold
- 62be360 refactor(launcher): menu_data + 10 tests
- daa4300 docs(session): Claude+Codex tarde
- af5e6c0 test(core): 40 contract tests para core.indicators
