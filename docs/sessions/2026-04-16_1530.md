# Session Log — 2026-04-16 15:30

## Resumo
Audit full do software com 5 agentes paralelos → 7 fixes não-trading aplicados.
Depois forense em 4 engines (JUMP, DE SHAW, KEPOS, GRAHAM) em cima de runs
existentes para não queimar compute. Descoberto bug estrutural
"exit-no-bar-de-entrada" nos dois engines Hawkes (KEPOS 13% / GRAHAM 100% dos
trades eram fantasmas duration=0). Fix mínimo aplicado — KEPOS Sharpe 1.30 →
1.53, max_dd 9.86% → 6.64%. GRAHAM: bug corrigido mas trend-following 1h
segue sem edge (decisão: redesenhar em outra sessão).

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| b44164c | fix(audit): CORS gate, atomic writes, MC seed, rmtree robust, open timeout | api/server.py, core/evolution.py, core/db.py, tools/reconcile_runs.py, analysis/montecarlo.py, launcher.py |
| 1ff8b18 | fix(overfit_audit): aceita schema de trades do KEPOS/GRAHAM | engines/graham.py, engines/kepos.py, analysis/overfit_audit.py |

Nota: `1ff8b18` foi coautorado pelo Codex companion — minha fix estrutural
dos engines ficou empilhada junto da normalização de schema do overfit_audit
que o Codex tava fazendo em paralelo.

## Mudanças Críticas
**ATENÇÃO: alteração em engines de trading (KEPOS/GRAHAM).**

- `engines/kepos.py:_resolve_exit` e `engines/graham.py:_resolve_exit` —
  adicionado guard `if bar_idx <= entry_idx: return None` no topo. Antes:
  o loop de scan colocava `entry_idx = t+1` e na próxima iteração já
  chamava `_resolve_exit` com `t == entry_idx`, permitindo que qualquer
  exit (stop/tp/regime_low/trend_break) disparasse no mesmo bar da
  entrada — trade fechava com `duration=0` pagando só C1+C2. Agora
  earliest exit é `t == entry_idx + 1`.
- Core protegido (`indicators`, `signals`, `portfolio`, `params`) NÃO
  foi tocado.

## Achados
1. **Bug estrutural same-bar-exit nos engines Hawkes**: forense em
   `data/graham/graham_2026-04-16_1409/trades.json` mostrou que 100% dos
   1068 trades tinham `duration=0` (100%). KEPOS 1412 tinha 13.4%. Fix
   no `_resolve_exit` eliminou ambos e, em KEPOS, melhorou todas
   métricas.
2. **KEPOS invert (H1-INV) é o vencedor empírico**: 10 runs escaneados
   mostram que modo fade original não gera edge em candle data
   (Sharpe ≤ 0.04), mas invert=True produz Sharpe 1.30-1.53. A
   memória "η em candle não atinge 0.95" está confirmada; riding the
   extension funciona, fading não.
3. **KEPOS edge é concentrado em altcoins líquidas**: SOL carrega o
   engine (+$1428 em 24 trades). BTC é o único símbolo com EV negativo
   (41% WR × $93 avg win - 59% × $68 avg loss = -$1.83/trade). Bluechip
   inteiro ainda é positivo, mas BTC é uma âncora.
4. **GRAHAM não tem edge fundamental em 1h**: mesmo com bug corrigido
   e filtros tightened (slope 0.0005→0.002, η sustain 3→8), WR ~35-41%
   com R:R simétrico = EV negativo. Trend-follow 1h em crypto
   estruturalmente é noise. Candidatos pra outra sessão: TF 4h/1d ou
   signal base diferente.
5. **GRAHAM invert H2-INV também não tem edge**: mesmo sem o bug,
   counter-trend em ENDO fecha -$9590 em 865 trades. Hipótese rejeitada.
6. **DE SHAW max_dd 13.25% é end-of-sample**: drawdown peak no idx
   990/1033 (últimos 4% da amostra). Não é issue crônico. Nada a fazer.
7. **JUMP está no ponto**: Sharpe 4.56, max_dd 1.99%, 205 trades. Regra
   "não mexe em vencedor" aplicada.
8. **Audit report tinha 3 exageros reais**: (a) agente de segurança
   alegou `config/keys.json` estar vazado publicamente — é gitignored
   e untracked, só local. (b) JANE_STREET atomic_write via
   `mkstemp+os.replace` é atômico, só não usa o helper padrão. (c)
   Token Telegram hardcoded refere ao mesmo arquivo gitignored.

## Estado do Sistema
- Smoke test: 176/176 pass (verificado antes e depois dos fixes)
- Pytest full: 933 passed / 7 skipped
- Arvore: limpa, 0 mods pendentes
- Core trading: inalterado
- Backlog restante (audit): refator launcher↔CLI dedup pra
  `core/execution.py` (1-2h, adiado — risky)
- Próximo passo sugerido: testar GRAHAM em TF 4h/1d com warmup Hawkes
  maior (≥720d); KEPOS já pronto pra overfit 6/6 formal

## Arquivos Modificados (nesta sessão)
- `api/server.py` — CORS env gate (+12/-2)
- `core/evolution.py` — 3 json.dump → atomic_write_json (+3/-14)
- `core/db.py` — robust_rmtree em delete_run (+4/-2)
- `tools/reconcile_runs.py` — atomic_write_json em INDEX_PATH (+8/-4)
- `analysis/montecarlo.py` — seed kwarg opcional (+5/-2)
- `launcher.py` — timeout + try/except em _open_file (+11/-3)
- `engines/graham.py` — no-exit-on-entry-bar guard (+10/-0)
- `engines/kepos.py` — no-exit-on-entry-bar guard (+9/-0)

## Notas para o Joao
1. **KEPOS ficou 1.53 de Sharpe com o mesmo comando que dava 1.30**.
   Valor de graça — só por corrigir o bug de exit. DD caiu 33%. Está
   pronto pra overfit 6/6 formal no próximo ciclo.
2. **GRAHAM precisa de decisão estratégica**: o bug foi corrigido mas
   o edge em 1h não existe. Opções pra próxima sessão:
   (a) testar 4h/1d (exige `--days ≥ 720` pra Hawkes warmup funcionar),
   (b) redesenhar signal base (descartar EMA cross como trigger),
   (c) arquivar o engine e assumir que Hawkes só funciona pra
   reversion-style (KEPOS).
3. **Agents às vezes exageram**: auditoria paralela produziu 3 false
   positives críticos (vazamento de keys, atomic write, token hardcoded).
   Validar sempre antes de agir.
4. **O Codex companion commitou bastante em paralelo** durante a sessão
   (5 commits não-meus): OHLCV cache, launcher picker KEPOS/GRAHAM, CLI
   aurum bt com 10 engines, overfit_audit schema. Meu commit de engines
   ficou bundled num commit dele devido a timing — conteúdo preservado.
5. **DE SHAW e JUMP intocados**. Regra "não mexe em vencedor" respeitada.
