# Session Log — 2026-04-16 23:54

## Resumo
Implementação completa do engine **PHI** (Fibonacci puro multi-fractal 5 TFs)
em 12 tasks TDD com subagent-driven-development, mais paridade de output
com GRAHAM, mais bateria de parâmetros Stage A (completa) + Stage B (108
combos rodando) + Stage C (universo × top 4 combos rodando). PHI end-to-end
funcional mas com edge suspeito (Sharpe 5+ em BNBUSDT = overfit provável).
Validação universal em curso. **Bateria continua rodando em background,
Stage B/C devem completar durante a noite.**

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 36ae0aa | docs(phi): design spec Fibonacci puro multi-fractal | `docs/superpowers/specs/2026-04-16-aurum-phi-design.md` |
| 64e8bcd | docs(phi): plano de implementação (12 tasks, TDD) | `docs/superpowers/plans/2026-04-16-aurum-phi.md` |
| 652966d | feat(phi): esqueleto + registro em config/engines.py | `engines/phi.py` (+153), `config/engines.py` (+1) |
| c42e7db | feat(phi): indicadores locais (ATR/RSI/BB/ADX/EMA200) + teste lookahead | `engines/phi.py`, `tests/test_phi.py` |
| 5f75e28 | feat(phi): zigzag com pivots confirmados N=2 + teste lookahead | `engines/phi.py`, `tests/test_phi.py` |
| dc54759 | feat(phi): fibs a partir dos dois últimos pivots | `engines/phi.py`, `tests/test_phi.py` |
| 86f68cc | feat(phi): merge_asof HTF com shift de 1 período (sem lookahead) | `engines/phi.py`, `tests/test_phi.py` |
| 5fba10b | feat(phi): cluster detection multi-TF (≥3 confluências Fib 0.618) | `engines/phi.py`, `tests/test_phi.py` |
| 887fd8f | feat(phi): regime gates (ADX/BB%/EMA200 dist) + Golden Trigger | `engines/phi.py`, `tests/test_phi.py` |
| ec7699c | feat(phi): scoring Ω_PHI + sizing Golden Convex com cap 2% | `engines/phi.py`, `tests/test_phi.py` |
| e76fddf | feat(phi): SL/TP fib + trailing monotônico | `engines/phi.py`, `tests/test_phi.py` |
| b671306 | feat(phi): kill-switch (2.618%/6.18%) + scan_symbol loop | `engines/phi.py`, `tests/test_phi.py` |
| 3188684 | feat(phi): run_backtest multi-symbol + summary (Sharpe/Sortino/PF/DD/Exp) + save_run | `engines/phi.py` |
| 90c9e75 | feat(phi): CLI main() + smoke run | `engines/phi.py`, `tests/test_phi.py` |
| e8f159d | refactor(phi): save_run envelope + per_symbol (paridade com GRAHAM) | `engines/phi.py` |
| 13c2779 | feat(tools): phi_sweep Stage A (24 combos) | `tools/phi_sweep.py` |
| aa0c7cc | feat(tools): phi_sweep_stage_b (108 combos, 5-dim grid) | `tools/phi_sweep_stage_b.py` |
| 1455253 | feat(tools): phi_stage_c (universo × top 4) | `tools/phi_stage_c.py` |

## Mudanças Críticas
**ATENÇÃO — CORE de trading NÃO foi tocado.** PHI é backtest-first, self-contained.
Indicadores locais (ATR/RSI/BB/ADX/EMA200) recomputados dentro do engine.
Cost model C1+C2 importado de `config.params` sem modificação. Sizing local
convex (Phi_Score²) sem coupling com `core/portfolio`. Registrado em
`config/engines.py` mas **FORA de FROZEN_ENGINES / ENGINE_INTERVALS** até passar
overfit_audit 6/6.

**3 bugs de runtime encontrados e corrigidos durante smoke test** (o subagent
T12 achou e ajustou testes, não código real, conforme CLAUDE.md):
1. `volume` → `vol` (coluna canônica do AURUM, não `volume`)
2. `datetime64[ms]` vs `[us]` mismatch no `merge_asof` (cast explícito pra `[ms]`)
3. `IntCastingNaNError` em `detect_cluster` (fillna(0) antes do astype)

**Zigzag tem correção do plano:** implementer T3 detectou que `np.argmax(high[:t+1])`
causaria lookahead intermitente. Substituído por `run_ext_idx` atualizado
bar-a-bar. Testado via `test_zigzag_no_lookahead`.

## Achados

### Sparsity problem do design
Como previsto no spec (§15), o design 5-TF × confluência × 3 gates de regime
é **extremamente restritivo**. Default params → 0 trades em BNBUSDT 2 anos.
Gargalo dominante: `regime_block` (1533 hits em ~50k barras antes de chegar
no cluster).

### Stage A (completo)
Top combo BNBUSDT: `cluster=2, omega=0.382, adx=20.0`
- 7 trades, Sharpe 1.59, WR 71.4%, PF 3.83, DD 0%
- n baixo demais pra ser significativo
- `cluster=3` **está morto** — zero trades em todas as 12 combos testadas

### Stage B (parcial — 8/108 visíveis, tee pipe morreu)
Descoberta preliminar: `cluster=1, tol=0.5, adx=10, wick=0.382` →
**177 trades, Sharpe 5.53** — red flag de overfit. Stage C vai validar
no universo.

### Bateria da noite (rodando em background às 23:54)
- Stage B: 108 combos × ~27s/combo (Python loop em `scan_symbol`), completa ~01:30
- Stage C: universo 11 símbolos × 4 combos. Prep pesado (~6min/símbolo pq
  zigzag loop sobre 210k candles de 5m), completa ~00:00

### Gargalo de performance
`scan_symbol` e `compute_zigzag` são loops Python puros sobre 50k-210k
barras. Causa do runtime alto. Não é bug — é design. Vetorização possível
em PR separado (trade-off: +1h dev pra 10× speedup).

## Estado do Sistema

### Commits
18 commits no branch `feat/phi-engine` hoje (mais 5 do Codex em paralelo
no mesmo branch). Árvore limpa (trabalho todo commitado).

### Tests
- `tests/test_phi.py`: **23/23 passed** (≤1s)
- Suite geral: não rodei suite full hoje; confirmar antes de merge pra main

### Backlog restante (para amanhã)
1. **Stage B/C resultados** — ler JSONs de `data/phi/_sweep/`:
   - `sweep_stage_b_*.json` (108 combos full ranking)
   - `stage_c_*.json` (veredito overfit/real por símbolo)
2. **Stage D** — walk-forward 6 janelas no melhor combo do universo
3. **Stage E** — Monte Carlo bootstrap 1000× (Sharpe P5/P50/P95)
4. **Stage F** — `analysis/overfit_audit.py <trades.json>` — 6/6 audit
5. **Decisão** — promover pra FROZEN_ENGINES ou documentar anti-pattern

### Próximo passo sugerido
Ler JSONs de Stage B/C amanhã de manhã. Se Stage C mostrar PnL concentrado
>60% em BNBUSDT, engine é overfit e vira anti-pattern documentado (como
KEPOS foi). Se PnL distribui entre ≥7 símbolos com Sharpe≥1, é real e
prossegue pra Stage D.

## Arquivos Modificados
```
docs/superpowers/specs/2026-04-16-aurum-phi-design.md  +303 (new)
docs/superpowers/plans/2026-04-16-aurum-phi.md         +1898 (new)
engines/phi.py                                         +1099 (new)
tests/test_phi.py                                      +340 (new)
tools/phi_sweep.py                                     +165 (new)
tools/phi_sweep_stage_b.py                             +175 (new)
tools/phi_stage_c.py                                   +150 (new)
config/engines.py                                      +1 (PHI registrado)
```

## Notas para o Joao

1. **Bateria segue rodando durante a noite.** Não mate processos Python —
   são Stage B (até ~01:30) e Stage C (até ~00:00). Resultados vão pra
   `data/phi/_sweep/*.json`.

2. **Stage B pipe tee morreu às 22:22 (combo 8/108)** mas o Python continua.
   Quando abrir amanhã: `ls data/phi/_sweep/` mostra se completou. Se
   `sweep_stage_b_*.json` existe, foi até o fim.

3. **Red flag do dia**: Sharpe 5+ em BNBUSDT é alto demais pra ser real
   sem causa identificada. Candidatos:
   - Overfit genuíno (Stage C vai revelar)
   - Bug em `_pnl_with_costs` que não pega algum caso específico (testes
     cobrem mas não exaustivamente)
   - Lookahead sutil que passou pelos testes (zigzag passou, HTF align
     passou, mas talvez em compute_scoring ou scan_symbol)
   - Micro-structure em ADX=10 que dá Sharpe alto mas não tradeable na vida real

4. **PHI já aparece no launcher BACKTEST picker** (foi pego automaticamente
   via `config/engines.py`, graças ao fix 89deb4). Se quiser testar manual:
   `python -m engines.phi --threshold-cluster 1 --omega-entry 0.382`
   (mais permissivo).

5. **Codex trabalhou em paralelo na mesma branch** e mergeu toneladas
   (MEDALLION engine novo, `--end` flag em 6 engines, cockpit redesign).
   Coordenação foi pacífica — só tocamos arquivos diferentes. Sessions
   2302/2332/2347 são do Codex.

6. **Overfit audit precisa de trade list**: quando decidir o combo final
   do PHI, basta `python -m engines.phi --threshold-cluster X` pra gerar
   o run, depois `python -m analysis.overfit_audit data/phi/<run>/trades.json`.

7. **Quando promover (SE passar 6/6)**: adicionar PHI a `FROZEN_ENGINES` e
   `ENGINE_INTERVALS["PHI"] = "5m"` em `config/params.py` (arquivo protegido,
   precisa aprovação explícita — mas nesse caso é só adicionar entrada,
   não mudar lógica).
