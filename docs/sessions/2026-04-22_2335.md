# Session Log — 2026-04-22 23:35

## Resumo
MACRO BRAIN overhaul visual do zero — começou com Tier 1/2 TradingView
(sparklines + flash + north-star XL + statusbar + popup click), Joao
rejeitou ("muito desorganizado, ta comprido, graficos crus"), sucessivas
iterações simplificaram pra padrão final: **ticker tape compacto + tiles
sem inline chart + click-to-popup com line chart area-filled + grid**.
Depois padronizou tudo (desk banner, COT matrix) no padrão 10Y, colocou
real-time sync 3s, consertou bug de scroll wheel global, e reorganizou
BRASIL+CRIPTO no padrão EUA (banner + chunked sections). Descartou 3
tabs redundantes (SINAIS/REDE/MOTORES). Net -740 linhas.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 16fc9fb | feat(macro-brain): tiles TradingView-vibe — sparklines + flash on tick | dashboard_view.py |
| dd0f2b5 | feat(macro-brain): Tier 1 design polish — fade flash + bounds + mini-candles | dashboard_view.py |
| 0291844 | feat(macro-brain): Tier 2 design — watchlist + status bar + tile popup | dashboard_view.py |
| c83b734 | fix(macro-brain): compacta north-star pra ticker tape 1-linha + line chart no popup | dashboard_view.py |
| 723d502 | polish(macro-brain): area-filled line charts + grid X/Y + last-value highlight | dashboard_view.py |
| 589f97b | polish(macro-brain): remove sparklines dos tiles — chart so no click | dashboard_view.py |
| 78bd838 | polish(macro-brain): padroniza tudo no padrao 10Y (click abre chart popup) | dashboard_view.py |
| b92d4cf | feat(macro-brain): tick_update 3s pra real-time sync sem overhead | launcher_support/screens/macro_brain.py + test |
| 5caa04a | fix(macro-brain): scroll wheel nao buga mais entre telas | dashboard_view.py |
| c9802e6 | refactor(macro-brain): organizar BRASIL+CRIPTO padrao EUA, descartar tabs redundantes | dashboard_view.py |

Total: **10 commits**, branch `feat/per-engine-runners` em sync com origin.

## Mudanças Críticas
Nenhuma mudança em lógica de trading — só UI. Zero CORE tocado
(indicators/signals/portfolio/params intactos). Zero keys.json tocado.

## Achados
- **Bug global de scroll wheel em 6 screens do launcher** (Connections,
  Data Backtests, Data Lake, Deploy Pipeline, Live Runs, Results
  Overview): todas usam o padrão `bind_all("<MouseWheel>")` sem
  `add="+"` no Enter + `unbind_all` no Leave, que nuka scroll de
  outras telas globalmente. Só fixei no macro_brain/dashboard_view.py
  por ora (via ancestry check + rebind defensivo). **Vale puxar o
  mesmo fix pros outros screens** em sessão futura.
- **Duplicação no main worktree**: meu primeiro Edit usou path absoluto
  `/c/Users/Joao/OneDrive/aurum.finance/macro_brain/dashboard_view.py`
  em vez do relativo (estou em `.worktrees/per-engine-runners/`). A
  edição pousou no worktree errado (feat/phi-engine, dirty). Depois
  refiz corretamente no worktree certo e sinquei os arquivos manualmente
  (`cp` do correto pro main) pra garantir que o launcher do Joao (que
  ele roda do main worktree) pegue as mudanças.

## Estado do Sistema
- Suite: **1682 passed, 72 skipped, 1 failed** (test flake pré-existente
  `test_resolve_bridgewater_module_imports_from_repo_root` espera repo
  name == "aurum.finance" mas estamos em worktree `per-engine-runners`).
- Smoke macro/dashboard: **28/28** passed
- Dashboard view: **2.232 linhas** (era 2.969, net **−737 linhas**)
- Tabs Macro Brain: 5 (EUA / BRASIL / CRIPTO / MACRO / LIVRO)
- VPS: 10 runners ativos, sem mudança.
- Branch: `feat/per-engine-runners` 10 commits ahead de origin antes
  desta sessão, agora 20 commits ahead (10 novos).

## Arquivos Modificados
- `macro_brain/dashboard_view.py` (−737 linhas no total; muita
  reescrita — north-star, tile, desk_banner, cot_matrix, detail chart,
  scroll wheel, br_tab, crypto_tab; deleções de markets_tab v1/v2/v3,
  network_tab v1/v2, book_tab v1, engines_tab impl+stage,
  insights_tab)
- `launcher_support/screens/macro_brain.py` (+1 linha; interval 10s→3s
  no auto_tick, footer label atualizado)
- `tests/launcher/test_screen_entrypoints.py` (+1 linha; assert
  atualizado 10_000→3_000)

## Notas para o Joao

### Tabs finais: EUA · BRASIL · CRIPTO · MACRO · LIVRO
- **EUA** — inalterado (já tava no padrão)
- **BRASIL** — overview banner (IBOV/USD-BRL/EUR-BRL/SMALL/IFIX/PETR4) +
  seções chunked (B3 blue chips, mid caps, ADRs)
- **CRIPTO** — overview banner (BTC/ETH/SOL/BTC.D/MCAP/F&G) + seções
  chunked (BTC network, positioning, alts+HL perps, DeFi). Absorveu o
  BTC advanced fees/block time do REDE.
- **MACRO** — inalterado (global cross-asset + COT mundial)
- **LIVRO** — inalterado (paper P&L, theses, positions, regime)

### Descartes (3 tabs, −260 linhas vivas + −480 de dead code legacy)
- **SINAIS** (`_render_insights_tab`): `compute_all()` retornava
  analytics derivadas magras. Se precisar, volta como seção dentro de
  MACRO.
- **REDE** (`_render_network_tab_v2`): BTC on-chain foi pra CRIPTO;
  VPS status + process monitor já tá no launcher principal.
- **MOTORES** (`_render_engines_tab*`): engine picker redundante com
  Live Cockpit que já tá na main do launcher.

### Padrão "click no ticker abre gráfico" (padrão 10Y)
Tudo converge no `_open_tile_detail` — popup 460×340 com line chart
area-filled, grid X+Y dotted, last-value horizontal dashed + dot,
Y-axis labels (max/last/min), cor direcional (GREEN/RED/AMBER). Afeta:
- `_tile` (grid de métricas normal)
- `_desk_banner` (overview banner do topo)
- `_cot_matrix` (células individuais clicáveis — MARKET · COLUMN label)
- North-star ticker tape (no topo, sempre visível)

### Real-time sync 3s (sem overhead)
- `tick_update()` UI refresh: **10s → 3s**. Só re-lê do SQLite
  (1 query batched `macro_series_many`) + configure in-place nos
  labels. Zero network.
- `brain.run_once()` background cycle: **mantido em 300s**. APIs
  externas (FRED, Yahoo) têm rate limits próprios; jobs internos têm
  throttle por source (news 15min, macro 24h, etc).

### Scroll wheel fix defensivo
Handler agora é `bind_all` único com ancestry check via
`winfo_containing` — se widget sob cursor não pertence ao canvas, é
no-op. Zero interferência com outras telas. Suporta Linux Button-4/5.
Rebind no `<Enter>` caso outra tela (padrão antigo, usado em
launcher.py e 6 screens) tenha chamado `unbind_all` nuking tudo.

**Porém — o bug raiz ainda existe em outras 6 telas** (Connections,
Data Backtests, Data Lake, Deploy Pipeline, Live Runs, Results
Overview). Se alguma dessas receber foco e depois sair, a scroll
global pode quebrar brevemente antes do rebind no próximo Enter do
Macro Brain. Fix permanente pede padronizar todos os screens no mesmo
padrão — sugestão pra próxima sessão.

### Pendência: main worktree tem dirty dashboard_view.py
O main worktree em `/c/Users/Joao/OneDrive/aurum.finance/` (na branch
`feat/phi-engine`) tem `M macro_brain/dashboard_view.py` dirty com
versão que bate com o último commit do per-engine-runners (sincronizei
via `cp` durante a sessão pra o launcher funcionar). Se quiser:
- **Opção A:** descartar com `git -C /c/Users/Joao/OneDrive/aurum.finance checkout -- macro_brain/dashboard_view.py` e deixar só o feat/per-engine-runners carregar quando mergear.
- **Opção B:** commitar no main worktree como trabalho independente de feat/phi-engine (menos limpo, duplicação).
- **Recomendação:** A. Mergear a feat/per-engine-runners → feat/phi-engine via PR traz tudo de uma vez.

### Próximos passos sugeridos
1. Walkthrough manual do MACRO BRAIN reabrindo o launcher — confirmar
   ticker tape + tabs BRASIL/CRIPTO organizadas + real-time 3s + scroll
   OK + click-to-popup nos 5 tabs.
2. Se aprovar visual, **abrir PR `feat/per-engine-runners → feat/phi-engine`** (20 commits, 10 de per-engine-runners + 10 de macro-brain polish).
3. Opcional — padronizar scroll wheel fix nos outros 6 screens (pra
   eliminar o risco de dessincronização global).
