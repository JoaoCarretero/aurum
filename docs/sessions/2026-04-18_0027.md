# Session Log — 2026-04-18 00:27

## Resumo

Continuação da sessão 2026-04-17_2349 após pivot do Joao ("não desista da
estratégia"). Grid de tune em cima do ornstein v1 (7 configs) + sanity T7:
todas zeraram ou deram anti-edge. Archived ornstein v1 com evidência dura.
Implementei engine novo `engines/meanrev.py` (MM+RSI simplificado, sem
OU/Hurst/ADF/VR) + variant search de 24 configs. **V12 achou Sharpe +1.66
(PF 1.11, WR 55.9%, PnL +$233/180d)**: long-only com direção reverse +
regime high_vol. Conceito real é momentum breakout continuation, não
mean-reversion. Lane aberta pra validação amanhã.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 10e1e19 | docs(ornstein): v1 tune grid — 7-config followup | docs/engines/ornstein_v2/2026-04-17_v1_tune_grid.md |
| 31c58e6 | docs(ornstein): v1 archive confirmed — tune grid + sanity check failed | docs/engines/ornstein_v2/2026-04-17_v1_tune_grid.md |
| 0c16e30 | feat(meanrev): simplified mean-rev engine + empirical archive | engines/meanrev.py, tests/test_meanrev.py, docs/engines/meanrev/2026-04-18_design.md |
| 4b1b3a9 | feat(meanrev): extend engine + variant search finds edge hint | engines/meanrev.py, tools/meanrev_variant_search.py, docs |

## Mudanças Críticas

Nenhuma mudança em lógica de trading protegida. `engines/meanrev.py` é
engine NOVO, não toca CITADEL/JUMP/MILLENNIUM/BRIDGEWATER/ornstein.
Usa `core.indicators` como import (protegido, não modificado).

Engine `meanrev` é research-only — não está em `FROZEN_ENGINES` nem em
`OPERATIONAL_ENGINES`. Precisa de DSR haircut + walk-forward OOS antes
de considerar live-ready.

## Achados

**Finding principal (2026-04-18 00:30):** V12 variant do meanrev tem edge
OOS-like positivo em majors/180d.

Config:
```
side_filter: long_only          # só trades long
reverse_direction: True          # entra LONG quando signal dispara SHORT
                                 # (ou seja: entra long em extreme UP + RSI overbought)
regime_filter: high_vol          # só em vol_regime ∈ {HIGH, EXTREME}
deviation_enter: 2.0             # default
rsi_short_min: 70.0              # default
atr_stop_mult: 2.0               # default
time_stop_bars: 96               # default (24h em 15m)
```

Resultado em `BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT / 180d`:
- N_trades: 637
- Sharpe: **+1.658**
- WR: 55.9%
- PF: 1.112
- Expectancy R: +0.008
- MaxDD: baixo (<5%)
- PnL: +$233.40 sobre $10k

**Interpretação:** o sinal NÃO é mean-reversion. É **momentum continuation
de breakout em vol alta**. Quando preço dispara 2+ ATRs acima do EMA50 E
RSI>=70 E regime é HIGH/EXTREME vol, entrar long e segurar até meia
continuação (alvo simétrico RR=1:1 via atr_stop_mult=2) dá edge marginal
positivo.

**Outros findings secundários:**
- V14 (V10 + dev=3): Sharpe +0.51, PF 1.03
- V15 (V10 + dev=4): Sharpe +0.26, PF 1.02
- V18 (V10 + atr_stop=3): Sharpe +0.14, PF 1.01

4/24 configs com Sharpe > 0 — suficiente pra suspeitar edge, insuficiente
pra confirmar sem DSR (n_trials=24 é penalidade pesada).

**Findings negativos (importantes):**
- **Ornstein v1 completamente inútil em cripto atual** — 0 trades em
  config default, anti-edge em T7 permissivo. Engine existe no branch
  (research-only) mas deveria ser arquivado definitivamente via commit
  explícito em sessão futura.
- **Mean reversion PURO não funciona** em 15m/1h cripto — sinal é
  inconsistente em ambas direções (tested baseline + 7 regime/anchor
  variantes, nenhuma passou).

## Estado do Sistema

- Suite: `tests/test_meanrev.py` 13/13 passing (cobre decide_entry em
  várias configs + simulate_trade happy path + edge cases).
- `tests/test_ornstein.py` 20/20 passing.
- Branch `feat/phi-engine`: +4 commits nesta sessão (`10e1e19, 31c58e6,
  0c16e30, 4b1b3a9`). Total na noite de 17→18: 10+ commits Claude.
- Backlog restante:
  1. **Validar V12 edge**: DSR haircut n_trials=24, walk-forward 3×60d,
     teste em bluechip_active.
  2. **Renomear engine** se V12 sobrevive validação (meanrev → momentum_breakout).
  3. **Arquivar ornstein v1 definitivamente** (commit de revert ou
     delete).
  4. **BACKTEST hierarchy refactor** (worktree separada,
     `.worktrees/backtest-hierarchy` — 4 tasks restantes).

## Arquivos Modificados

```
docs/engines/ornstein_v2/2026-04-17_v1_tune_grid.md  | +203 (novo)
docs/engines/meanrev/2026-04-18_design.md            | +246 (novo + append)
engines/meanrev.py                                    | +306 (novo) +22 (extend)
tests/test_meanrev.py                                 | +104 (novo)
tools/meanrev_variant_search.py                      | +74 (novo)
```

## Notas para o Joao

- **V12 é um lead sério mas não validado.** Antes de celebrar ou colocar
  em produção: DSR haircut (n_trials=24) + walk-forward OOS + teste em
  bluechip_active. Sharpe 1.66 com 637 trades pode ou não sobreviver —
  depende de skew/kurtosis dos retornos + como edge se comporta em
  janelas distintas. Plano pra amanhã: análise estatística rigorosa
  ANTES de qualquer novo variant.
- **O engine se chama `meanrev` mas na verdade virou momentum-breakout
  continuation** com as configs V10/V12. Se V12 passar validação, renomeia.
  Por enquanto fica com nome `meanrev` porque é o que está no código.
- **Anti-overfit protocol pagou 2x nesta sessão**:
  1. Impediu Codex de iterar infinitamente no v2 (baseline zeroed → archive).
  2. Exigiu 24 variants pra descobrir V12 — agora n_trials=24 precisa entrar
     no DSR amanhã. Sem isso, Sharpe 1.66 poderia ser noise de selection.
- **Protegi ornstein v1 intocado** via `git checkout HEAD` — os 5 params
  "robust" + wrapper ornstein_v2.py que Codex escreveu ontem foram
  revertidos. Engine v1 continua no commit `a1fb95e`, intocado mas morto
  em prática (produz 0 trades).
- **Commits aparentemente redundantes** (vários em ornstein_v2): é o
  histórico do spec → plano → execução de cada etapa do protocolo. Cada
  um é intencional e rastreável. Se quiser squash no final, fácil.
- **Lane 2 (hardening c21aa01)**: run_manager._resolve_compare_run_dir,
  VPS host normalize, cockpit KPI order — 3 melhorias não-relacionadas
  que achei no diff do Codex e separei em commit próprio. Utilidade
  independente.
