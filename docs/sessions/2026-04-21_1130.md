# Session Log — 2026-04-21 11:30

## Resumo
Rodou MILLENNIUM battery 365d pra mostrar pro Mael por que a estratégia funciona
(Sharpe 8.23, Sortino 11.70, Calmar 26.87, ROI 87.86%, DD 3.27%). No meio do
caminho, corrigiu 3 camadas de export de métricas: (1) HTML report do battery
estava com só 5 cards, faltando Sortino/Calmar/AvgR/MaxStreak; (2) terminal
só printava 4 métricas no fim de cada window; (3) launcher DATA > BACKTESTS
botão METRICS renderizava quebrado porque o battery não gerava JSON canônico
e a overview tab só mostrava 6 métricas sem Calmar/MaxDD/per-strategy.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| (pendente) | fix(metrics): export completo em HTML/terminal/launcher | analysis/report_html.py, tools/batteries/millennium_battery.py, launcher.py |

## Mudanças Críticas
Nenhuma mudança em lógica de trading. CORE protegido (indicators/signals/
portfolio/params) **intocado**. Todas as mudanças são em export/renderização
de métricas derivadas (o que se exibe, não o que se calcula).

## Achados
- **Battery não gerava JSON canônico.** `_show_results` no launcher esperava
  schema CITADEL (`trades` + `equity` + `monte_carlo` + `summary` nested).
  Battery só escrevia `summary.json` + `per_strategy.json` (skip_names pra
  `_show_results`) → overview tab renderizava quebrado ou pegava per_strategy
  com schema errado.
- **`_normalize_summary` só lê top-level de `data`.** Mesmo depois de escrever
  o JSON canônico, `ret` e `total_trades` saíam `None` porque os aliases do
  normalizer não desciam no `data["summary"]`. Fix: duplicar as chaves no top
  level do payload.
- **Overview tab ignorava Calmar/MaxDD/AvgR/MaxStreak/per-strategy.** Embora
  os dados estivessem no payload, a renderização só tinha 6 cards e não
  exibia breakdown por engine.

## Estado do Sistema
- Suite crítica não foi rerun nesta sessão (mudanças cosméticas de export).
- Battery 365d: 526 trades · WR 72.81% · ROI 87.86% · Sharpe 8.23 ·
  Sortino 11.70 · Calmar 26.87 · MaxDD 3.27%
  - CITADEL: 267 trades, 70% WR, $5,544 PnL (só shorts)
  - RENAISSANCE: 127, 84% WR, $1,226 PnL
  - JUMP: 132, 67% WR, $2,016 PnL
- Artefato: `data/millennium/battery_2026-04-21_1102/w365d/` com HTML + JSON
  canônico + summary.json + per_strategy.json + trades.csv/jsonl

## Arquivos Modificados

### `analysis/report_html.py` (+20 linhas)
`_build_result_box`: summary card de 5 → 9 cards.
- **Novos cards:** Sortino, Calmar, Avg R (computado do portfolio), Max Streak
- **Cores semânticas:** Sortino>1 verde, Calmar>3 verde, AvgR>0 verde,
  Streak≥6 vermelho

### `tools/batteries/millennium_battery.py` (+174 / -9)
1. `_write_html_report.ratios` agora inclui sortino, calmar, sharpe_daily,
   max_consec_losses (antes: só ret/sharpe/sortino, e sortino não era usado)
2. Novo `_print_window_metrics` — terminal mostra WR/ROI/PnL/Sharpe/Sortino/
   Calmar/MaxDD/SharpeD/MaxStreak + breakdown per-engine (n/WR/L/S/PnL/AvgR/
   Streak) depois de cada window
3. Novo `_print_battery_summary` — tabela consolidada de todas as janelas no
   fim (Window/Trades/WR%/ROI%/Sharpe/Sortino/Calmar/MaxDD%/PnL)
4. Novo `_write_report_json` — gera `millennium_<interval>_<days>d.json` com
   schema canônico compatível com `_show_results`: config, summary,
   per_strategy, monte_carlo={}, bear_market={}, trades, equity, + **aliases
   top-level** (ret, roi_pct, sharpe, sortino, calmar, n_trades, win_rate,
   total_pnl, max_dd_pct) pra `_normalize_summary` achar
5. `_register_window_run` inclui `report_json_path` no index.json + calmar

### `launcher.py` (~ 90 linhas no `_results_build_overview`)
- KEY METRICS agora em 2 linhas: performance (6 cards) + risk (5 cards)
  - Novos: Calmar, Max DD, Avg R, Max Streak, Final Equity
- Nova seção **BREAKDOWN POR ENGINE** — tabela compacta com colunas
  ENGINE/N/WR%/L/S/PnL/AvgR/STREAK pra runs multi-engine (MILLENNIUM)
- Não mexe nos demais changes em launcher.py (boot timing metrics — são de
  sessões anteriores)

## Notas para o Joao

**Pra mostrar pro Mael:** `data/millennium/battery_2026-04-21_1102/w365d/report.html`
(dashboard visual) + `battery_summary.md` (tabela consolidada 365d).

**No launcher:** abrir `DATA > BACKTESTS`, procurar
`millennium_battery_2026-04-21_1102_w365d`, clicar **METRICS**. Overview deve
mostrar 11 cards + breakdown por engine.

**Ressalva OOS:** o número 87.86% é in-sample. Audit de 2026-04-16 mostrou
CITADEL/JUMP com edge real confirmado, RENAISSANCE inflado ~2× OOS. Pro Mael
honestidade: o headline é teto otimista, OOS real ~40–60%.

**Runs antigas:** batteries anteriores (antes deste fix) não têm o JSON
canônico — se clicar METRICS numa delas, vai pegar per_strategy.json
(schema errado) e quebrar. Fix retroativo seria um backfill simples (gerar
o millennium_*.json em cada w*d/ existente). Não fiz porque não foi pedido
e runs antigas são pra descartar mesmo.

**Backlog pra considerar:**
- Backfill de JSON canônico pros battery runs velhos (script simples)
- Estender mesmo pattern de overview pra outras engines (BRIDGEWATER, JUMP
  standalone também só têm 6 cards)
