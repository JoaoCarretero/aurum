# Session Log — 2026-04-17 10:15

## Resumo

Pivot da audit acadêmica (Bloco 0) pra mandato "todas estratégias
rodáveis HOJE, condizentes com realidade do mercado". Aplicados 7
commits Claude + 5 commits Codex paralelos cobrindo: BRIDGEWATER
sentiment bug (Sharpe 11→3.03 BEAR), KEPOS/MEDALLION cost asymmetry,
KEPOS threshold (k_sigma 2.0→1.0 — dispara trades), EXPERIMENTAL_SLUGS
quarentena, params.py honest comments, full audit doc.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 9b41c76 | fix(sentiment): accept end_time_ms for OOS/backtest — BRIDGEWATER unbounded | core/sentiment.py, engines/bridgewater.py |
| 18db6dc | fix(kepos,medallion): apply slip+spread on entry (cost symmetry) | engines/kepos.py, engines/medallion.py |
| 55857f3 | chore(params,kepos): realistic defaults + honest comments | config/params.py, engines/kepos.py |
| cc4a642 | feat(engines): EXPERIMENTAL_SLUGS quarantine flag | config/engines.py |
| d7d91bb | docs(methodology): meta-trigger log — 2026-04-17 first entry | docs/methodology/anti_overfit_protocol.md |
| 157fae2 | chore: runnable HOJE status — KEPOS/MEDALLION quarantined + docs | config/engines.py, engines/kepos.py, docs/audits/ |
| ad618cc | fix(kepos): k_sigma 2.0 → 1.0 (fires now) + update status docs | engines/kepos.py, config/engines.py, docs/ |

(Codex commitou em paralelo: `1085c32` security, `c1ab62d` dead code
cleanup, `ae6dbde` full audit doc, `77e4088` bridgewater sentiment
limits, `e5f0b3f` live/telegram hardening, `250a69e` wave 2 addenda,
`35a3642` structural audit.)

## Mudanças Críticas

**ATENÇÃO — mudanças em PnL / lógica de custos:**
- `core/sentiment.py` — as 3 fetches (funding/OI/LS) agora aceitam
  `end_time_ms`. Sem isso, backtest histórico usava sentiment data de
  HOJE (look-ahead severo). Impacto: BRIDGEWATER BEAR 2022 Sharpe
  cai de 11.04 → 3.03 — os 8 pontos eram bug, 3.03 é o real.
- `engines/kepos.py` + `engines/medallion.py` — `_pnl_with_costs`
  agora aplica SLIPPAGE+SPREAD no entry (era só exit). +3 bps de
  custo por trade. Impacto: Sharpe in-sample cai alguns décimos
  honesto. MEDALLION last-360d agora -3.69 (era pior esconddido).
- `engines/kepos.py` — defaults relaxados (`eta_critical` 0.95→0.75,
  `eta_sustained_bars` 10→5, `hawkes_k_sigma` 2.0→1.0) pra match da
  calibração sweep 2026-04-16. Engine agora dispara trades pela
  primeira vez em candle data.

## Achados

1. **BRIDGEWATER bug era LIVE_SENTIMENT_UNBOUNDED** (Codex track'd root
   cause). Confirmado com fix: Sharpe 11.04 → 3.03 em BEAR 2022.
   Magnitude da queda (-73%) = look-ahead severo confirmado.
2. **BRIDGEWATER last-360d Sharpe 13.11 / 11165 trades** — suspeito
   mas tecnicamente rodável. Regime atual pode explicar (contrarian
   sentiment achando muitos setups em bull top), OU leak residual.
   Pendente investigação.
3. **KEPOS multi-AND gate**: eta≥0.75 sustentado + price_ext>2σ +
   atr_r>1.3 exige 3 condições coincidentes. Com defaults 2.0/0.95
   originais + k_sigma=2.0, 0 trades em qualquer janela. Calibração
   sweep exigia k_sigma=1.0.
4. **MEDALLION grid-best era overfit** (Codex audit já flagou). Cost
   fix revelou magnitude: -3.69 Sharpe last-360d, consistente com
   colapso OOS. Engine não carrega edge fora do train.
5. **Codex e Claude em harmonia**: lanes claras (Codex infra+security+
   audit full-stack; Claude engines+params+trading-core). Zero
   conflito direto, 2 findings materiais complementares (Codex:
   BRIDGEWATER root cause; Claude: KEPOS/MEDALLION cost asymmetry).

## Estado do Sistema

- **7 engines rodáveis confirmados** via smoke last-360d:
  - ✅ CITADEL 2.96, JUMP 3.74, RENAISSANCE 6.16 (edge real)
  - ⚠️ BRIDGEWATER 13.11 (investigar suspeita)
  - 🔴 DE SHAW -1.9, KEPOS -2.08, MEDALLION -3.69 (EXPERIMENTAL)
- `config/engines.EXPERIMENTAL_SLUGS` = {deshaw, graham, kepos, medallion}
- Backlog: investigar BRIDGEWATER 13.11; DE SHAW regime filter; MEDALLION arquivar-vs-reformular
- Smoke test: engines individuais OK. Suite completa não rodada (sessão de trading fixes).

## Arquivos Modificados (Claude)

- `core/sentiment.py` — +12/-3 linhas (end_time_ms param x3)
- `engines/bridgewater.py` — +11/-1 linhas (pass END_TIME_MS) — Codex refinou depois
- `engines/kepos.py` — +22/-4 linhas (cost fix + 3 defaults)
- `engines/medallion.py` — +11/-7 linhas (cost fix)
- `config/params.py` — +18/-27 linhas (comentários honestos, remove iter_N WINNER)
- `config/engines.py` — +23/-0 linhas (EXPERIMENTAL_SLUGS + comments)
- `docs/methodology/anti_overfit_protocol.md` — +37/-0 linhas (meta-trigger log)
- `docs/audits/2026-04-17_runnable_status.md` — +113/-0 linhas (novo)
- `docs/days/2026-04-17.md` — +5/-0 linhas (sessão 10:00)

## Notas para o Joao

1. **BRIDGEWATER precisa de outro olhar.** Pós-fix sentiment:
   - BEAR 2022: Sharpe 3.03 (plausível, honesto)
   - Last-360d: Sharpe 13.11 (suspeito)
   Diferença pode ser regime atual (bull top = muitos setups contrarian
   de sentiment), OU leak residual (position sizing? aggregate cap?).
   Minha recomendação: Codex já conhece o código melhor que eu, delegar
   pra ele verificar ou fazer sweep multi-window quando tiver tempo.

2. **KEPOS oficialmente rodável**, mas edge negativo. Mecanismo "fade
   extensions" não se sustenta em regime atual. Opções pro futuro:
   (a) arquivar como GRAHAM; (b) reformular pra "ride extensions"
   (invert_direction=True) + walk-forward 6/6.

3. **DE SHAW** — próximo passo sugerido: regime filter via HMM/Chronos.
   Só operar em regime CHOP (evitar trend regime onde cointegração
   quebra). Não fiz hoje — escopo próxima sessão.

4. **MEDALLION** — escolha binária tua: arquivar como GRAHAM, ou
   reformular mecanismo (grid-tuned sem DSR não serve, precisa
   hipótese documentada primeiro per protocolo anti-overfit).

5. **EXPERIMENTAL_SLUGS** criado mas não consumido ainda por launcher/CLI.
   Próxima sessão: wire em launcher pra filtrar "production" vs
   "experimental" na view de engines.

6. **RENAISSANCE em FROZEN_ENGINES** — OOS 2.42 não justifica FROZEN
   (que exige robustness clara). Não removi hoje — deixei pra próxima
   sessão junto com outros cleanups de `params.py`.

7. **Codex tem trabalho ainda em working tree** (`core/cache.py`, `core/data.py`,
   `config/paths.py`, etc.) — não toquei, é dele commitar.

## Próximo passo sugerido

Próxima sessão: (a) investigar BRIDGEWATER 13.11, (b) wire
EXPERIMENTAL_SLUGS em launcher, (c) decidir DE SHAW regime filter vs
arquivar. Ou: escolha da próxima prioridade.
