# Session Log — 2026-04-11 18:33

## Resumo
Hub ARBITRAGE reestruturado: agora é um router MP3-player-style entre três
pernas de arbitragem. JANE STREET cockpit intacto; adicionadas telas
DEX↔DEX e CEX↔DEX alimentadas por um scanner novo que cobre 8 venues
(3 DEX + 5 CEX) via APIs públicas batch.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| _pendente_ | feat(arbitrage): funding scanner hub + 8-venue cross-DEX/CEX observer | core/funding_scanner.py, launcher.py, docs/sessions/2026-04-11_1833.md |

## Mudanças Críticas
Nenhuma mudança em lógica de trading. O scanner é puramente observacional
— não entra em sinais, não dimensiona posições, não toca no engine
JANE STREET. A rota do menu `"alchemy"` agora passa pelo hub em vez de
abrir o cockpit diretamente; o cockpit é acessado por `C` dentro do hub.

## Achados
- Hyperliquid paga funding por **hora** (vs 8h nas CEXs). APR anualizado
  naturalmente 8× mais sensível — top oportunidades observadas esta
  sessão: HEMI +172% APR (SHORT HL / LONG Bybit), WLFI +147%, TAO +126%.
- Gate.io: endpoint `contracts` não traz volume 24h; precisa ir em
  `tickers` pra obter funding + volume num único batch.
- Paradex: resposta mistura options e perps; filtrar por `*-USD-PERP`.
- BingX: mercados variam intervalo de funding (1h / 4h / 8h); o campo
  `fundingIntervalHours` precisa ser lido por linha, não hard-coded.
- Scanner roda 8 venues em paralelo em ~3.5s (ThreadPoolExecutor).

## Estado do Sistema
- Smoke test: **156/156 passed** (zero falhas)
- Scanner standalone: 1870 opps, 8/8 venues online, 0 erros HTTP
- Alerta Telegram: configurado e funcional (throttle 30min/par)
- Backlog restante: adicionar Drift/Lighter/Paradex options, ordenação
  por APR líquido descontando fees, detail panel com histórico por par
- Próximo passo sugerido: ligar o scanner ao JANE STREET engine como
  fonte de sinal pra CEX-DEX auto-execução (atualmente só CEX-CEX)

## Arquivos Modificados
```
 core/funding_scanner.py          | +611 (NOVO)
 launcher.py                      | +479 / -3
 docs/sessions/2026-04-11_1833.md | +NNN (este log)
```

## Notas para o Joao
1. **Menu ARBITRAGE agora é um hub de 3 cartas** (antes abria o cockpit
   direto). Navegação: `↑↓` ou teclas `C/D/X`, `ENTER` seleciona,
   `ESC` volta.
   - `C` CEX↔CEX → JANE STREET cockpit (inalterado)
   - `D` DEX↔DEX → scanner cross-DEX
   - `X` CEX↔DEX → scanner cross-mercado (geralmente o APR mais alto)

2. **Alertas Telegram disparam automaticamente** quando abre a tela do
   scanner e alguma oportunidade cruza **+100% APR**. Throttle de
   30 minutos por `symbol:venue`. Durante o teste desta sessão o bot
   mandou 3 alertas reais pra ti — se quiseres silenciar, é comentar
   `maybe_alert_telegram(...)` em `_funding_refresh()` (launcher.py).

3. **CEX-DEX é estatisticamente a perna mais lucrativa** — diferenças
   de funding entre Hyperliquid e CEXs costumam ficar na casa dos
   100-200% APR em altcoins. O scanner já filtra por volume $500k
   e OI $200k pra evitar armadilhas de liquidez.

4. **Nenhum engine foi tocado.** `engines/arbitrage.py`,
   `core/alchemy_ui.py`, `core/alchemy_state.py` continuam exatamente
   como estavam. O scanner é standalone — vive em `core/funding_scanner.py`
   e é consumido apenas pelo launcher.

5. **Pre-existing WIP não entrou no commit.** Quando a sessão começou já
   havia mudanças pendentes em `engines/arbitrage.py`, `config/*.json` e
   `.claude/settings.local.json` — essas ficaram intactas e não foram
   commitadas. Quando tu decidir o que fazer com elas, avisa.

6. **Para testar o scanner no terminal** (útil pra entender o que está
   rolando no mercado agora):
   ```
   python -m core.funding_scanner
   ```
