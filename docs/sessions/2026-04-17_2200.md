# Session Log — 2026-04-17 22:00

## Resumo

Sessão longa focada em MILLENNIUM: shadow runner + systemd deploy, 2 bugs
críticos do Codex detectados e corrigidos, otimização de gate via grid
fechado (D_liberal → B_cooldowns), overfit audit 6/6 PASS em 360d/720d/
1000d, rebuild do DB pra consistência com disco, e telegram alerting
institucional no shadow runner. Sharpe saiu de 5.69 → 7.78 com MDD
caindo de 4.77% → 3.39% e trades subindo de 117 → 527 em 360d native.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 6869d8d | feat(millennium): shadow loop runner + systemd deploy | tools/millennium_shadow.py, deploy/* |
| f9199cf | feat(launcher): shadow panel no cockpit + bucket EXPERIMENTAL | launcher_support/engines_live_view.py, tests |
| bf8509b | chore(launcher): commit working-tree (Codex VPS helpers + backtest fix) | launcher.py |
| baf9e69 | fix(core/indicators): defensive anti-downcast refactor em calm_prev | core/indicators.py, test |
| d0e3420 | docs(params): refletir RENAISSANCE weight 0.40 pos-recalibracao | config/params.py |
| 0baed1a | feat(phi): overfit audit followup + battery/forensics tools | engines/phi.py, 5 tools, doc |
| f6a50ab | feat(deshaw): pair selection + revalidation contracts | engines/deshaw.py, 4 tests |
| 72f5bc6 | feat(millennium): live bootstrap runner + ensemble weights tuning | engines/millennium.py, millennium_live.py, configs |
| e2b8a66 | feat(launcher/cli): bootstrap contracts + engine picker + run manager | launcher_support/*, core/*, tests |
| 98f7e48 | refactor(macro_brain): dashboard view polimento visual | macro_brain/dashboard_view.py |
| 45f4e7d | feat(tools): focus batteries pra jump / renaissance / weak engines | tools/*_focus_battery.py |
| 0db80ae | fix(launcher): NameError 'action_label' no _render_row_ready bloqueava ENGINES | launcher_support/engines_live_view.py |
| 4c5be0e | refactor(launcher/engines): compactar detail panel, remover ENVIRONMENT BANK | launcher_support/engines_live_view.py |
| 1819221 | fix(millennium): NameError 'any_kill' no print_portfolio_gate_stats | engines/millennium.py + audit doc |
| fb66224 | feat(tools): millennium_gate_grid — portfolio-gate param sweep | tools/millennium_gate_grid.py |
| b3ff3b4 | feat(millennium): afrouxar gate — D_liberal config validada em 360d | engines/millennium.py + audit doc |
| 4508c60 | feat(millennium): gate OFF + symbol/engine LOSS-streak cooldowns | engines/millennium.py |
| ffd5699 | docs(audits): overfit audit B_cooldowns — 6/6 PASS + HALF2 > HALF1 | docs/audits/ |
| 054edc0 | feat(deploy): one-shot installer pro shadow runner no VPS | deploy/install_shadow_vps.sh |
| 108e238 | fix(launcher): METRICS do Millennium abria run errado (CITADEL) | launcher.py |
| f185aa6 | docs(audits): millennium cooldowns validado em 720d/1000d (zero overfit) | docs/audits/ |
| 34e5b08 | docs(deploy): plano de deploy em camadas pro MILLENNIUM | docs/deploy/ |
| 1e4a4ba | feat(tools+shadow): rebuild_db utility + telegram alerting institucional | tools/rebuild_db.py, tools/millennium_shadow.py |

23 commits nesta sessão, todos pushed pro remote `feat/phi-engine`.

## Mudanças Críticas

**ATENÇÃO: CORE tocado** — `core/indicators.py` teve refactor defensivo
em `calm_prev` (substitui `fillna(True)` por `.where(notna, True).astype(bool)`
pra evitar FutureWarning pandas object-downcast). Semântica **idêntica**
(NaN→True, mantém bool). Verificado linha-a-linha. Não afeta nenhum
cálculo downstream. `config/params.py` só teve comentário atualizado.

**MILLENNIUM gate reescrito:**
- `PORTFOLIO_EXECUTION_ENABLED=False` (gate estava matando edge —
  CITADEL passava só 4.5% via ratio rule)
- `SYMBOL_COOLDOWN`: 24 bars após LOSS no mesmo symbol
- `ENGINE_LOSS_STREAK`: 3 LOSS consecutive → pula 2 trades
- Thresholds JUMP score afrouxados (WEAK 0.82→0.80, STRESSED 0.84→0.81)

Numbers 360d antes vs depois:
```
                 Codex original    B_cooldowns
  trades              117              527
  WR                 84.6%            72.3%
  Sharpe             5.69             7.78
  ROI                22.3%            82.9%
  MDD real            1.61%            3.39%
  MC worst_dd         1.63%            5.47%
```

Validado em 720d (MDD CAI pra 2.51%) e 1000d (Sharpe 5.69, MDD 5.69% —
vem 100% do Q1 2022-2024 bear; Q4 moderno tem Sharpe 6.59 MDD 1.78%).

## Achados

**Bugs do Codex (confirmados + corrigidos):**

1. `launcher_support/engines_live_view.py:707` — `_render_row_ready` usava
   `action_label`/`action_color` sem chamar `row_action_label()` antes.
   **NameError quebrava ENGINES inteiro no menu principal.** Commit `0db80ae`.

2. `engines/millennium.py:983` — `print_portfolio_gate_stats` tinha bloco
   órfão `if not any_kill:` copiado de `print_ensemble_stats` sem o loop
   que define a variável. **NameError quebrava qualquer run op=1.** Commit
   `1819221`.

3. `launcher.py _bt_resolve_run` não populava `report_json_path` pro
   Millennium (que salva em `reports/multistrategy_15m_v1.json`, não em
   `summary.json` raiz). **Fallback em `_show_results` pegava run do
   CITADEL.** Usuário via métricas do CITADEL chamadas de Millennium.
   Commit `108e238`.

**Desalinhamento DB↔disco:** 155 runs em `aurum.db` sem entry no
`index.json`, 102 entries no index sem row no DB. Rebuild via novo
`tools/rebuild_db.py`: 293→191 runs, 408k→362k trades, 455MB→411MB.
DB agora canonical com o disco.

**MAX_OPEN_POSITIONS_MS é dead variable** em `millennium.py` (linhas 69
+ 1461 só, nunca consumida em filtro). Controle real é
`MAX_OPEN_POSITIONS=3` em `config/params.py:327`. Descoberto ao testar
config "B" que não mudou absolutamente nada vs F.

**Temporal decay −91% no audit:** HALF2 (holdout-like) **dobra** PnL de
HALF1 (train-like) no audit do overfit. Q4 slice mais recente (2025-10
→ 2026-04) — totalmente OOS pro método — tem **Sharpe 6.59 e MDD 1.78%**.

## Estado do Sistema

- Suite: **1117 passed, 7 skipped**
- Branch: `feat/phi-engine` · **82 commits ahead de main hoje**
- DB: 191 runs · 362k trades · 411 MB · consistente com disco
- Working tree: 7 arquivos do Codex uncommitted (eu não mexi — são dele)
- Backlog restante:
  - PR `feat/phi-engine` → `main` (URL: https://github.com/JoaoCarretero/aurum/pull/new/feat/phi-engine)
  - Subir shadow no VPS (3 comandos em `deploy/install_shadow_vps.sh`)
  - Streaming adapters pra JUMP e RENAISSANCE (bloqueia camada 4 MILLENNIUM live)
- Próximo passo: João abre PR na web + roda install no VPS

## Arquivos Modificados

**Modified** (meus + herdados do Codex já commitados):
- `core/indicators.py` (+10/−5) defensive anti-downcast
- `config/params.py` (+2/−2) comment
- `engines/millennium.py` (+210/−8) gate OFF + cooldowns + fix any_kill + weights
- `engines/millennium_live.py` (new, 198 lines) bootstrap plan
- `engines/phi.py`, `engines/deshaw.py`, `engines/bridgewater.py` (via Codex earlier sessions)
- `launcher.py` (+322/−66) action_label fix + _bt_resolve_run report_json_path fix
- `launcher_support/engines_live_view.py` (+398/−148) shadow panel + 4 buckets + compact
- `launcher_support/bootstrap.py` (+95) VPS helpers

**New files:**
- `tools/millennium_shadow.py` (288 lines) shadow runner
- `tools/millennium_gate_grid.py` (341) grid sweep
- `tools/rebuild_db.py` (200) DB reconcile
- `deploy/millennium_shadow.service` (45) systemd unit
- `deploy/install_shadow_vps.sh` (64) one-shot installer
- `deploy/README.md` (62) deploy guide
- `docs/audits/2026-04-17_millennium_cooldowns_overfit_audit.md` (277)
- `docs/audits/2026-04-17_millennium_status_basic.md` (116)
- `docs/deploy/2026-04-17_millennium_live_plan.md` (132) deploy em camadas

## Notas para o Joao

1. **MILLENNIUM otimizado está pronto pra shadow live.** B_cooldowns
   passou por overfit audit 6/6 em 3 janelas (360d / 720d / 1000d) +
   walk-forward crescente. Q4 slice OOS (out-of-sample) confirma edge
   real, não ajuste a posteriori.

2. **Pra subir no VPS** (Camada 0 — sem capital, sem ordens):
   ```bash
   ssh <teu_vps>
   git clone https://github.com/JoaoCarretero/aurum.git /srv/aurum.finance
   cd /srv/aurum.finance && git checkout feat/phi-engine
   bash deploy/install_shadow_vps.sh /srv/aurum.finance $USER
   ```
   Opcionalmente, popula `config/keys.json` com bloco `telegram` pra
   alertas start/tick_fail/stop.

3. **PR pro main:** eu não consigo criar via `gh pr create` (auth não
   configurado no terminal). Clica:
   https://github.com/JoaoCarretero/aurum/pull/new/feat/phi-engine
   Os 82 commits descem em um único merge (branch passa audit, tests,
   e smoke completo).

4. **Capital real (Camada 3) exige aprovação explícita tua em chat.**
   MILLENNIUM full pod (Camada 4) depende de streaming adapters pra
   JUMP e RENAISSANCE — esforço 1–2 semanas por engine. Pra live real
   curto prazo, o caminho é CITADEL standalone que já tem live_ready=True.

5. **Codex deixou 7 arquivos modified** (`core/run_manager.py`,
   `launcher_support/bootstrap.py`, `launcher_support/engines_live_view.py`,
   tests novos, `docs/audits/2026-04-17_claude_codex_summary.md`,
   `tools/bridgewater_long_battery.py`) + 2 untracked. Não toquei —
   é escopo dele, provavelmente continua próxima sessão.

6. **Bugs do Codex (2 NameErrors + 1 path fix)** foram o tipo de coisa
   que só aparece em execução real. O smoke programático que adicionei
   (`launcher.App() → _strategies_live() → métrics`) deveria virar
   test fixture pra pegar antes de commit futuro.
