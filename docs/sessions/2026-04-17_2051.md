# Session Log — 2026-04-17 20:51

## Resumo

Salvage campaign completa: (1) diagnóstico de PHI como artefato
geométrico, (2) bateria em 4 engines fracos (PHI/DE SHAW/KEPOS/MEDALLION)
— todos arquivo, (3) campanha BRIDGEWATER com 5 bugs corrigidos + cache
backfill + bateria longa + hipóteses de edge. BRIDGEWATER ficou
funcional mas não operacional — edge marginal, regime-dependente, falha
holdout. Research gates implementadas como flags opcionais pra bateria
formal em 2026-07-17.

**ATENÇÃO:** mudanças em lógica de trading neste log:
- `core/sentiment.py` — 3 funções reescritas (funding_zscore, ls_ratio_signal,
  oi_delta_signal) + 2 fetchers endurecidos (OI/LS com endTime)
- `engines/bridgewater.py` — aligner com staleness guard + 4 research gates
- `engines/phi.py` — macro_bias tag + min_target_bp gate (off by default)

## Commits

(A listar após `git commit` — inclui todos os fixes desta sessão)

## Mudanças Críticas

**BRIDGEWATER (5 bugs corrigidos + 4 gates opcionais):**
1. Bug 1 — `funding_zscore`/`ls_ratio_signal` retornam `Series` com
   `DatetimeIndex` em vez de `RangeIndex`. Antes: alinhamento posicional
   fabricava signal sobre candles antigos. Depois: alinhamento temporal
   real via `searchsorted`.
2. Bug 2 — `fetch_open_interest`/`fetch_long_short_ratio` propagam
   `endTime` ao live fetch quando cache é insuficiente. Antes: live tail
   sem `endTime` = look-ahead em backtest. Depois: historical window
   correto.
3. Bug 3 — `ls_ratio_signal` usa z-score rolling (window=672 ≈ 1 semana
   a 15m) com thresholds simétricos ±1σ/±2σ em vez de absolutos
   1.5/0.67. Antes: 99% shorts estrutural em crypto bull. Depois:
   balanceamento 50.7/49.3 long/short.
4. Bug 4 — `_align_series_to_candles` e `_align_oi_signal_to_candles`
   recusam ticks com staleness > 2h. Antes: um tick de 2023 propagava
   por 2 anos de candles. Depois: gap maior que 2h → `default`.
5. Bug 5 — `oi_delta_signal` usa z-score rolling 200-bar simétrico em
   vez de thresholds absolutos 5%/1%. Antes: dormant (0% de sinal em
   low-vol 2026). Depois: regime-adaptivo, produz sinal em ~11% bars.

**PHI:**
- Tag `macro_bias` (BULL/BEAR/CHOP) no scan_symbol — overfit audit teste
  D sai de SKIP.
- Parâmetro `min_target_bp` (default 0 = off) — gate físico rejeitando
  setups com TP1 muito próximo de entry.

**Core:** `core/sentiment.py` tocado mas é core-adjacent (não
`indicators.py`/`signals.py`/`portfolio.py`/`params.py`). Fixes são
correções de bugs estruturais, não calibração.

## Achados

**PHI — edge era artefato geométrico.**
- Runs de 2026-04-17_1310 (pré-geomfix) tinham Sharpe 7.23/9.10/9.38 em
  3 janelas. Inflado por setups abrindo trade com entry fora do
  intervalo `sl..tp1` (wins de mesma barra espúrios).
- Runs pós-geomfix (13:37 em diante) revelaram Sharpe -0.69/-6.10/-1.94.
- Overfit audit pós-geomfix: 1/6 PASS nas 3 janelas. Edge não existe
  quando a física dos níveis é respeitada.
- Doc `docs/audits/2026-04-17_phi_overfit_followup.md` reescrito com
  números honestos.

**DE SHAW — edge é regime-BULL-only.**
- Janelas 2025 (BULL): Sharpe +2.47/+1.66. Pareceu promissor.
- Holdout em 2024-01 e 2023-01 (bear/recovery): Sharpe -0.85/-0.51.
- Coint de pairs em crypto quebra fora de BULL — arquivar.

**KEPOS/MEDALLION — sem edge.**
- KEPOS: invert/default/soft todos Sharpe <1 em 2 janelas. Memory
  confirma tese Hawkes não aplicável a candle data.
- MEDALLION: Sharpe -3 a -5 em todas configs. Engine nasceu overfit
  (grid-search fishing no design).

**BRIDGEWATER — funcional mas não operacional.**
- Pós-5-bugs-fix + cache estendido (5→31d contíguos): Sharpe 1.95 em
  BTC solo 90d, p-value permutação 0.38% (estatisticamente significativo).
- Mas multi-symbol (basket 10 símbolos 31d): Sharpe cai pra 0.48, ETH
  perde -$139, BTC carrega tudo.
- Sub-janelas early/mid/late: Sharpe +1.37 / +1.11 / **-1.17** — decay
  severo no 3º terço.
- Hipótese "filter direction matches macro_bias" passa no train set
  (Sharpe +2.32) mas FALHA no holdout (Sharpe -1.81, p-value 6.5%
  — não significativo). Todos 10 símbolos negativos no holdout.
- Conclusão: edge é BTC-specific, regime-specific, não sobrevive
  holdout rigoroso. Não promovido ao MILLENNIUM.

## Estado do Sistema

- **Suite**: 1117 passed, 7 skipped ✓
- **OPERATIONAL_ENGINES** (MILLENNIUM op=1): CITADEL + RENAISSANCE + JUMP
  (BRIDGEWATER segue FORA até OOS 2026-07-17).
- **Cache sentiment**: 10 símbolos × 31d contíguos pós-backfill (antes:
  1 sym × 5.5d). Cobertura cresce ~5d/dia com API.
- **Engines arquivo candidates** (não arquivados formalmente ainda):
  PHI, KEPOS, MEDALLION, DE SHAW. Pendente decisão do João.
- **BRIDGEWATER research gates**: 4 flags CLI implementadas
  (`--strict-direction`, `--min-components`, `--min-dir-thresh`,
  `--exit-on-reversal`). Defaults OFF. Testes de contrato kw-only +
  defaults-preservam-baseline.

## Arquivos Modificados

- `core/sentiment.py` — funding_zscore, ls_ratio_signal, oi_delta_signal,
  fetch_open_interest, fetch_long_short_ratio (+~80/-25 linhas)
- `engines/bridgewater.py` — aligners + scan_thoth gates + CLI flags
  (+~90/-15 linhas)
- `engines/phi.py` — macro_bias tag + min_target_bp gate (+~20/-5)
- `tests/test_sentiment_contracts.py` — novos testes para z-score LS/OI
  + endTime propagation (+~100 linhas)
- `tests/test_bridgewater_contracts.py` — testes staleness guard +
  gates kw-only (+~60 linhas)
- `tests/test_phi.py` — integração scan_symbol + macro_bias (+~90 linhas)
- `tools/bridgewater_cache_backfill.py` — NEW — backfill iterativo
- `tools/bridgewater_long_battery.py` — NEW — 9-run long battery
- `tools/weak_engines_battery.py` — NEW — 22-run salvage battery
- `tools/phi_overfit_audit.py` — propaga macro_bias no normalizer
- `docs/audits/2026-04-17_phi_overfit_followup.md` — reescrito com
  números honestos pós-geomfix

## Notas para o Joao

**Decisões pendentes (próxima sessão):**

1. **PHI, KEPOS, MEDALLION — arquivar formalmente?**
   - Todos falham AND em 2 janelas + holdout.
   - Consistent com protocolo anti-overfit (3+ consecutivos arquivados).
   - Precisa: update `config/engines.py` + memory `project_engine_status`
     + `CLAUDE.md` registry.

2. **DE SHAW — arquivar ou pensar em regime-filter?**
   - Edge só em BULL. Não é NO_EDGE mas é regime-conditional.
   - Pensar se faz sentido rodar só quando macro BULL confirmado (risco
     de fishing sem mecanismo defensável).

3. **BRIDGEWATER — agendar prewarm daily.**
   - `tools/prewarm_sentiment_cache.py --loop-minutes 60 --engine BRIDGEWATER`
   - Rodar via Windows Task Scheduler ou schedule skill.
   - Meta: 60-90d de cache contínuo nos 10 símbolos pra validação OOS
     formal em 2026-07-17.

4. **Mini-spec para bateria OOS 2026-07-17:**
   - Pre-registrar grid fechado dos 4 gates (strict_direction on/off,
     min_components 0/2/3, min_dir_thresh 0.20/0.30/0.40).
   - Decisão anti-fishing: 12 configs × 1 OOS holdout 30d.
   - DSR haircut explícito pro n_trials=12.
   - Regra de parada: se pior das 12 Sharpe <0.5 pós-DSR, arquiva.

**O que entregou hoje:**
- BRIDGEWATER deixou de ser "bug-suspect" pra virar "funcional mas
  não operacional — precisa OOS formal".
- 4 engines fracos diagnosticados (edge real / artefato / regime-only
  / sem edge).
- Cache sentiment cresceu 6x (5→31d) e basket completo.
- Código limpo (1117/1117 testes), research gates disponíveis pra
  bateria formal.

**O que NÃO entregou (honesto):**
- Nenhum engine ganhou promoção operacional esta sessão.
- BRIDGEWATER não ficou em MILLENNIUM (por pedido explícito João — e
  pelos dados de holdout que justificam essa cautela).
- Edge real no BRIDGEWATER não foi confirmado em holdout rigoroso.
