# Session Log — 2026-04-22 20:50

## Resumo
CITADEL, JUMP e RENAISSANCE ganharam runners paper+shadow próprios em
`data/{engine}_{mode}/` — isolados do pod MILLENNIUM mas usando o mesmo
collector `_collect_live_signals` pra garantir que signals batem
engine-a-engine. 6 services deployados no VPS e rodando em paralelo
com os 4 MILLENNIUM existentes. Launcher UI parametrizada pra mostrar
as 3 engines no sidebar LIVE bucket igual MILLENNIUM.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 2e065db | feat(runners): per-engine paper+shadow pra CITADEL, JUMP, RENAISSANCE | _paper_runner.py, _shadow_runner.py, 6 entry scripts, 6 systemd templates |
| 04311c8 | feat(runners): install script VPS unificado + contract tests | install_engine_vps.sh, test_per_engine_runners.py |
| e8f3dbf | feat(launcher): CITADEL/JUMP/RENAISSANCE visiveis no cockpit igual MILLENNIUM | engines.py, engines_live_view.py, 2 test files |

## Mudanças Críticas
**Nenhuma mudança em lógica de trading core.** indicators/signals/portfolio/
params/engines.millennium intocados. Os novos runners:
1. Delegam ao mesmo `engines.millennium._collect_live_signals` → zero
   drift em signal generation;
2. Filtram output por `strategy == ENGINE_UPPER` no nível Python
   (nenhum toque em `decide_direction` / `calc_levels`);
3. Usam `PaperAccount/PaperExecutor/PaperPositionManager/KSLiveGate/
   MetricsStreamer` existentes — zero fork.

**Mudança de flag em `config/engines.py`**:
- `jump.live_ready`: False → True (OOS edge já confirmado + smoke paper+shadow VPS passou)
- `renaissance.live_bootstrap`: True adicionado (runner funciona; OOS claim inflado ~2× mantém live_ready=False)
- `citadel.live_ready`: True (inalterado)

Essas flags só afetam em qual bucket da sidebar a engine aparece (LIVE/READY/RESEARCH). Nenhum efeito em backtest.

## Achados
- **Install script default user=aurum quebra** — o VPS roda MILLENNIUM como `root` (não `aurum`). Primeiro deploy deu `status=217/USER` no systemctl. Fix: passar `root` como 6º arg de `install_engine_vps.sh`. Documentar no README/install script.
- **keys.json no VPS não tem `vps_ssh` nem `cockpit_api`** → `verify_keys_intact.py` reporta falso positivo (exit 1) mas o conteúdo está intacto (telegram SET, binance EMPTY — paper mode correto). Esperado no VPS; essas seções são dev-machine only.
- **Sidebar do cockpit hardcoded a "millennium"** em 8+ lugares. Refatorado pra parametrizar por `selected_slug` sem quebrar o fluxo MILLENNIUM existente.
- **ShadowPoller legacy só segue 1 engine** — adicionado merge com `_COCKPIT_RUNS_CACHE` em `_shadow_active_slugs` pra cobrir N engines rodando em paralelo.

## Estado do Sistema
- Suite: **1682 passed, 72 skipped, 1 pre-existing fail** (`test_resolve_bridgewater_module_imports_from_repo_root` — worktree name assertion, não relacionado)
- VPS: **10 runners ativos** (4 MILLENNIUM + 6 novos), memória 2.1Gi / 11Gi total, disco 4% /
- CITADEL/JUMP/RENAISSANCE paper+shadow: todos `ticks_ok=1`, `status=running`, DB upsert OK
- Branch: `feat/per-engine-runners` em `origin`, ahead 3 commits de `feat/phi-engine`
- Backlog top:
  1. Rodar por algumas horas e comparar signals do solo vs MILLENNIUM slice pros mesmos candles (cross-check honesto)
  2. Abrir PR `feat/per-engine-runners → feat/phi-engine` quando cross-check validar
  3. Considerar `--account-size` menor pros 3 solos (5k em vez de 10k) pra não saturar a UI de positions se todos entrarem ao mesmo tempo

## Arquivos Modificados
Novos (14):
- `tools/operations/_paper_runner.py` (594 linhas — core paramétrico)
- `tools/operations/citadel_paper.py` (26 linhas — entry)
- `tools/operations/jump_paper.py` (25 linhas)
- `tools/operations/renaissance_paper.py` (28 linhas)
- `tools/maintenance/_shadow_runner.py` (572 linhas — shadow core)
- `tools/maintenance/citadel_shadow.py` (25 linhas)
- `tools/maintenance/jump_shadow.py` (24 linhas)
- `tools/maintenance/renaissance_shadow.py` (27 linhas)
- `deploy/{citadel,jump,renaissance}_{paper,shadow}@.service` (6 × 35 linhas)
- `deploy/install_engine_vps.sh` (111 linhas)
- `tests/test_per_engine_runners.py` (168 linhas, 15 tests)

Modificados (4):
- `config/engines.py` (+2 flags: jump→live_ready, renaissance→live_bootstrap)
- `launcher_support/engines_live_view.py` (+50 -35 linhas, parametrização engine)
- `tests/integration/test_engines_live_view.py` (adapted test contracts)
- `tests/test_engines_live_view_cockpit.py` (adapted test contracts)

## Notas para o Joao

### O que ta rodando no VPS
```
citadel_paper@desk-a       run=2026-04-22_183410p_desk-a
citadel_shadow@desk-a      run=2026-04-22_183454s_desk-a
jump_paper@desk-a          run=2026-04-22_183432p_desk-a
jump_shadow@desk-a         run=2026-04-22_183504s_desk-a
renaissance_paper@desk-a   run=2026-04-22_183443p_desk-a
renaissance_shadow@desk-a  run=2026-04-22_183515s_desk-a
```

### Cross-check manhã seguinte
```bash
# Compara CITADEL slice do MILLENNIUM vs CITADEL solo no mesmo período
grep "CITADEL" /srv/aurum.finance/data/millennium_shadow/*/reports/shadow_trades.jsonl | wc -l
cat /srv/aurum.finance/data/citadel_shadow/2026-04-22_183454s_desk-a/reports/shadow_trades.jsonl | wc -l
# Se os números baterem (dentro de 1-2 trades de tolerância por deduplication timing), logic bate.
```

### Launcher local
Abre o launcher (`python launcher.py`), vai em ENGINES LIVE. Você deve ver CITADEL, JUMP, RENAISSANCE aparecendo no bucket **LIVE** ao lado do MILLENNIUM quando o modo for paper ou shadow. Click numa delas → RUNNING NOW picker mostra a instância desk-a da engine certa. Se o detail pane ainda mostra dados millennium, esperar ~60s pra o cache do cockpit /v1/runs refresh.

### Trocar RENAISSANCE pra live_ready=True?
Manteria mais consistente com JUMP. Não fiz porque o OOS audit 2026-04-17 marca RENAISSANCE ⚠️ inflado 2×. Se rodar bem por alguns dias no shadow, podemos flippar com confiança. Por ora `live_bootstrap=True` já faz aparecer no LIVE bucket quando rodando.

### Install em outros slots
```bash
# Pra mais instâncias (desk-b, desk-c, ...):
bash deploy/install_engine_vps.sh citadel paper desk-b 10000 /srv/aurum.finance root
bash deploy/install_engine_vps.sh jump paper desk-b 5000 /srv/aurum.finance root
# etc.
```

CORE de trading intocado. keys.json no VPS não foi tocada (telegram SET). Branch `feat/per-engine-runners` pushed.
