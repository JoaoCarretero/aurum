# Session Log — 2026-04-25 21:50

## Resumo

Auditoria do trade XRPUSDT/JUMP que entrou em `live_trades` (paper
desk-paper-a/b, 2026-04-25 16:16 UTC) descobriu 3 problemas no caminho
de persistência DB. TDD nos 3, deploy VPS, verify telegram-vs-JSONL pra
shadow signals (XRP 18:00 + LINK 20:15) — match exato. CORE intocado.
1 commit cirúrgico + push + VPS pull + restart 4 services + livre.

## Commits

| Hash    | Mensagem                                                                   | Arquivos |
|---------|----------------------------------------------------------------------------|----------|
| 0a9c77b | fix(db+paper): close 3 audit findings — double-wrap, Timestamp bind, paper signals | core/ops/db_live_trades.py · tools/operations/millennium_paper.py · tools/maintenance/normalize_live_trades_details.py (new) · tests/{test_db_live_trades, test_paper_runner_db_persist, test_normalize_live_trades_details}.py |

## Mudanças Críticas

**ATENÇÃO — runtime live trading mexido:**

- `tools/operations/millennium_paper.py` ganhou
  `_persist_signal_to_db(record, observed_at=...)` análogo ao
  `_persist_trade_to_db` existente. Wirado no tick loop **logo após**
  `live_count += 1` (antes dos gates), persiste todo signal LIVE
  não-stale em `live_signals` com score/struct/entropy/hurst/primed.
  Falha non-fatal (try/except + log warning). Antes do fix, paper só
  tinha trade-lifecycle no DB; signal context vivia só em
  `signals.jsonl` do run_dir VPS.

- `core/ops/db_live_trades._norm_trade` e `_norm_signal` agora coercem
  `pd.Timestamp` / `datetime` em ISO string via novo `_coerce_ts`
  applied a `ts`/`exit_ts` (trade) e `observed_at`/`signal_ts`
  (signal). Antes do fix, qualquer payload com `t["timestamp"]` como
  pandas Timestamp (millennium_shadow standard) explodia
  `Error binding parameter 3: type 'Timestamp' is not supported` —
  só WARNING (non-fatal), então toda row de shadow signal foi
  silenciosamente dropada do `live_signals` no VPS apesar de
  `novel_count>0` em 4+ shadow runs.

CORE protegido (`core/indicators.py`, `core/signals.py`,
`core/portfolio.py`, `config/params.py`) **NÃO tocado**. Sem mudança em
lógica de decisão / sizing / custo / stop / target. Backtests
calibrados não invalidados.

## Achados

1. **`details_json` double-wrap em sync round-trip**. `sync_vps_db.py`
   lia `live_trades` row do VPS (já com `details_json` populado) e
   chamava `upsert_trade(local_conn, run_id, _row_to_trade_payload(row))`.
   `_row_to_trade_payload` mantinha o campo `details_json`, e
   `_norm_trade` (whitelist `_TRADE_COLS` não inclui `details_json`)
   jogava ele em `extras`, re-serializando como
   `{"details_json": "<json original>"}`. Bug local (VPS source-of-truth
   sempre canonical). 2 rows afetadas no DB local (XRP id 23/24).
   **Fix**: `_norm_trade` e `_norm_signal` agora `pop("details_json")`
   antes de `extras` e usam `_merge_details_json` (preserva existente,
   mescla com novidades, defende parse error com `_legacy_details`).

2. **`Timestamp` bind silenciava todo shadow signal no VPS**. Confirmado
   via `journalctl -u millennium_shadow@desk-shadow-a` linha 22:44:27:
   `live_signals upsert failed (non-fatal): Error binding parameter 3:
   type 'Timestamp' is not supported` — exatamente quando o
   LINKUSDT BEARISH 9.307 signal saiu pro telegram. millennium_shadow
   passa `record["timestamp"]` direto pro upsert (vem do
   df.index do engine, é `pd.Timestamp` não-string). SQLite explodiu,
   warning silencioso, signal NUNCA chegou em `live_signals` apesar do
   hook `_persist_signal_to_db` existir desde commit `71f72b0` (sessão
   1700). VPS DB mostrava 0 rows na tabela. **Fix**: `_coerce_ts`
   helper aplicado em `_norm_*` antes do upsert.

3. **Paper runner não persistia signals**. Diferente do shadow
   (`_shadow_runner._persist_signal_to_db` desde sessão 1700),
   millennium_paper só tinha hook pro trade lifecycle
   (`_persist_trade_to_db`). Signal context (score/struct/entropy/
   hurst/primed) só vivia em `signals.jsonl`. **Fix**: novo helper
   `_persist_signal_to_db` + chamada após `live_count += 1` (antes dos
   gates de portfolio).

4. **Telegram bate com JSONL nos shadows.** Cross-check direto via
   `cat /srv/aurum.finance/data/millennium_shadow/<run>/reports/shadow_trades.jsonl`:

   | Telegram | JSONL VPS |
   |---|---|
   | SHADOW JUMP BEARISH XRPUSDT entry 1.42 stop 1.427 tgt 1.4 RR 3 size 5,587.54 ts 18:00 | entry=1.41997388 stop=1.4268 target=1.3996 rr=3.0 size=5587.542 timestamp="2026-04-25 18:00:00" ✅ |
   | SHADOW JUMP BEARISH LINKUSDT entry 9.307 stop 9.364 tgt 9.137 RR 3 size 525.9 ts 20:15 | entry=9.307207 stop=9.364 target=9.1369 rr=3.0 size=525.9307 timestamp="2026-04-25 20:15:00" ✅ |

   PAPER opens (FET 0.2061/0.2062 + 2× XRP 1.4184/1.4197) ainda
   abertos — vão pra `live_trades` quando exit. As leves diferenças de
   entry entre shards a/b são esperadas (slippage timing num replay
   determinístico paralelo).

## Estado do Sistema

- Smoke: **172/172**
- Suite área DB+paper: **69/69 passed**
- Local HEAD = Origin HEAD = VPS HEAD = `0a9c77b`
- VPS services after restart:
  - `millennium_shadow@desk-shadow-a` — active
  - `millennium_shadow@desk-shadow-b` — active
  - `millennium_paper@desk-paper-a` — active
  - `millennium_paper@desk-paper-b` — active
- DB local: 2 live_trades (id 23/24, ambas desempacotadas via
  `normalize_live_trades_details --apply`), 9 live_signals (todos
  pré-fix, esperando próximo signal pra confirmar fix de Timestamp ao
  vivo), 206 live_runs.
- Backup pré-unwrap: `data/aurum.db.pre_unwrap_20260425_184041.bak`.
- Backlog top: fix #3 (cost decomposition slippage/commission/funding)
  pendente — toca `core/signals.label_trade`, **CORE protegido**,
  precisa de aprovação explícita do Joao + plano de re-calibração.

## Arquivos Modificados

```
 core/ops/db_live_trades.py            +83 -3
 tools/operations/millennium_paper.py  +49 -0
 tools/maintenance/normalize_live_trades_details.py   +108 (new)
 tests/test_db_live_trades.py          +134 -0
 tests/test_paper_runner_db_persist.py +145 -0
 tests/test_normalize_live_trades_details.py          +183 (new)
```

## Notas para o Joao

1. **Próximo shadow signal vai validar fix #2 ao vivo.** Os 4 services
   foram restartados, agora rodam o código com `_coerce_ts`. Quando
   o próximo JUMP/CITADEL/RENAISSANCE BEARISH/BULLISH for emitido,
   `live_signals` deve ganhar uma row (antes ficava em 0 silenciosamente).
   Espera ~15min (próximo tick).

2. **Fix #3 (cost decomposition) PRECISA da sua decisão.** Decompor
   `pnl_after_fees − pnl` em colunas tipadas (slippage_usd /
   commission_usd / funding_usd) requer tocar `core/signals.label_trade`
   ou criar helper em `core/portfolio` — ambos são CORE. Sem isso, as
   colunas continuam None mesmo com tudo o resto fixed. Sua chamada:
   - **Aprovar**: ok mexer em CORE + checklist de re-calibração
     (CITADEL/JUMP backtests reprocessados pra confirmar bytes-exatos).
   - **Adiar**: mantém None nas colunas, operador continua olhando
     `pnl_after_fees` agregado em vez de breakdown.
   - **Cancelar**: backlog removido, schema mantém colunas pra futuro.

3. **PAPER trades XRP/FET ainda abertos** vão fechar nos próximos
   ticks. Quando exit, `_persist_trade_to_db` grava em `live_trades`
   E (com o fix de hoje) `_persist_signal_to_db` já gravou o signal
   no momento do open — primeira vez que paper terá signal+trade
   pareados em SQL.

4. **DB backup criado**: `data/aurum.db.pre_unwrap_20260425_184041.bak`.
   Posso deletar quando confirmar que normalize não corrompeu nada
   (já validei via testes idempotentes + 9 testes de migration).
