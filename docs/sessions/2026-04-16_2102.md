# Session Log — 2026-04-16 21:02

## Resumo
Ajuste honesto de KEPOS e GRAHAM "até ter edge". Descoberto que o
Sharpe 1.53 do KEPOS era cherry-pick temporal (365d) — em 730d colapsava
pra 0.68. Sweep de `eta_sustained_bars` (3→15) revelou que 10 é sweet
spot: Sharpe 1.08, DD 11.6%. Default do engine atualizado. GRAHAM 4h
confirmado sem edge (baseline sem gate η dá ROI -1380%, com gate -144%)
— arquivado como experimental. `analysis/overfit_audit.py` ganhou
detecção de schema Hawkes (testes B e D agora honestos, não PASS
trivial).

## Commits
_Pendente — vai commitar após este log._

## Mudanças Críticas
**ATENÇÃO: alteração em engine de trading (KEPOS default).**

- `engines/kepos.py:KeposParams.eta_sustained_bars` — default 5 → 10.
  Justificativa empírica 730d bluechip/15m/invert:
    - sustained=3  → Sharpe 0.68, DD 24.6%
    - sustained=5  → Sharpe 0.83, DD 16.5%
    - sustained=7  → Sharpe 0.91, DD 17.1%
    - sustained=10 → **Sharpe 1.08, DD 11.6% (peak)**
    - sustained=15 → Sharpe 1.08, DD 15.5% (plateau)
  Knob de "quantos bars consecutivos de η≥critical antes de disparar".
  Mais rigoroso → menos trades ruim de curta duração.
- `engines/graham.py` — docstring actualizado com "Edge status 2026-04-16"
  documentando que 1h e 4h não têm edge. Sem mudança em lógica.
- `analysis/overfit_audit.py` — três ajustes:
  1. Test B (sensitivity): detecta schema. CITADEL usa `score`
     (thresholds 0.50-0.56), Hawkes usa `eta_at_entry` (quartis
     data-driven). Sem campo reconhecido → SKIP honesto.
  2. Test D (regime): SKIP explícito se nenhum trade tem `macro_bias`.
     Antes: tudo virava UNKNOWN e passava trivialmente.
  3. Counters: adicionado `skipped` e `failed` separados no summary
     pra não confundir SKIP com FAIL no header.
- Core protegido (`core/indicators`, `core/signals`, `core/portfolio`,
  `config/params`) NÃO foi tocado.

## Achados
1. **Sharpe 1.53 do KEPOS era temporal cherry-pick**. Os 131 trades
   (365d scan) aconteceram em apenas 34 dias (11 jan → 13 fev 2026).
   Janeiro: 91 trades WR 48%. Fevereiro: 40 trades WR 55%. O warmup
   Hawkes + `eta_critical` relaxado só atinge threshold em períodos
   específicos de alta atividade. Em 730d a amostra vira 309 trades
   distribuídos, Sharpe despenca pra 0.68.
2. **KEPOS sustained=10 é robusto**. Em 2 anos, Sharpe 1.08, DD 11.6%,
   WR 47.2%. Melhor que sustained=3 em todos os quesitos. Audit:
   1 PASS, 3 WARN, 1 FAIL, 1 SKIP (vs 1/1/2/1 em sustained=3).
3. **SOL carrega KEPOS**. Em 2 anos, SOL = 60% do PnL com
   sustained=10 (vs 86% em sustained=3). Ainda é concentração alta,
   mas diluída. Sem SOL, engine fica marginal.
4. **Cliff em η alto (B FAIL)**: eta>=0.775 sempre mata PnL,
   independente de sustained. Em KEPOS modo invert (ride the
   extension), η muito alto NÃO ajuda — pode ser que η alto marca
   momentum já esgotado, invertendo o raciocínio. Característica
   estrutural do framework Hawkes em candle data.
5. **GRAHAM 4h não tem edge**. Defaults (η_lower=0.80) nunca disparam
   — η em candles não passa de ~0.84 empiricamente. Com η relaxado
   (0.50-0.95), 1836 trades em 1460d dão ROI -144%, Sharpe -3.02.
   Baseline (gate desabilitado): 7059 trades, ROI -1380%. H2
   formalmente "passa" (gate > baseline em 3 métricas) mas ambos
   negativos. Trend-follow 4h crypto nesse universo = sem edge.
6. **Test B e D do overfit_audit mentiam**. CITADEL schema assumido.
   KEPOS/GRAHAM passavam B trivialmente (nenhum trade com `score`
   ≥ 0.50 → zero rows → no cliff → PASS). Passavam D trivialmente
   (nenhum `macro_bias` → tudo UNKNOWN → 1 regime > 100 trades →
   PASS). Agora B usa eta_at_entry com quantis e D vira SKIP honesto.

## Estado do Sistema
- Branch atual: **`feat/phi-engine`** (Codex companion trabalhando
  em engine PHI em paralelo; não mexi nas mudanças dele)
- Smoke test: 176/176 pass
- Pytest: 953 pass, 7 skip, **3 fail** em `tests/test_data_contracts.py`
  (pre-existentes, não relacionados — o pytest anterior do stash mostrou
  4 fail antes das minhas mudanças, 3 depois; minhas mudanças não
  pioram nada e acidentalmente fixam um)
- Arvore: 8 files modified (3 meus, 5 Codex), 3 untracked (1 meu, 2 Codex)
- Arquivos de hoje (meus): engines/kepos.py, engines/graham.py,
  analysis/overfit_audit.py
- Próximo passo sugerido: (a) overfit 6/6 formal no KEPOS sustained=10
  com walk-forward 6/6 via analysis/walkforward.py; (b) arquivar
  GRAHAM ou tentar redesenhar signal base (não EMA cross); (c) avaliar
  rotar XRP fora do universo KEPOS (-$730 em 2 anos)

## Arquivos Modificados (nesta sessão — só meus)
- `engines/kepos.py` — eta_sustained_bars 5→10 + comment calibração (+8/-2)
- `engines/graham.py` — docstring edge status 2026-04-16 (+11/-0)
- `analysis/overfit_audit.py` — schema Hawkes + SKIP honesto (+72/-18)
- `docs/sessions/2026-04-16_2102.md` — este log (novo)
- `docs/days/2026-04-16.md` — daily log (novo)

## Notas para o Joao
1. **KEPOS está melhor, não pronto**. Sharpe 1.08 em 2 anos é honesto,
   mas a dependência de SOL (60% do PnL) continua. Antes de live,
   pensar em: (a) basket filter que exclua underperformers persistentes
   (XRP), (b) testar se sustained=10 aguenta em 3+ anos de dados
   quando tiver cache mais profundo em todos os ativos.
2. **Sharpe 1.53 era mentira**. Run único em 365d pegou fev 2026
   (mês excepcional). Em 2 anos de amostra, nem o sustained=10
   ultrapassa 1.08. Cautela com narrativas de "engine tunado" baseadas
   em períodos curtos.
3. **GRAHAM deve ser arquivado ou redesenhado**. 4h foi última chance
   dentro do framework atual (EMA cross + Hawkes ENDO gate) e o
   baseline sem gate já é catastrófico. Se mantiver o engine, precisa
   trocar o signal base — Hawkes não vai salvar um trend-follower
   estruturalmente fraco em crypto 1h/4h.
4. **Codex está trabalhando em feat/phi-engine**. Vi 10+ commits dele
   na branch (engine PHI + medallion.py novo + launcher refactor
   grande). Não toquei nos arquivos dele. Pode ter conflitos se eu
   ou ele fizer algo em kepos/graham (improvável mas possível).
5. **3 tests em test_data_contracts.py estavam falhando antes**.
   Não causado pelo meu trabalho (confirmado via git stash). Cache
   escrito durante o test polui execuções subsequentes. Bug de
   isolamento de test fixture, não de lógica.
6. **overfit_audit agora é honesto pra engines Hawkes**. Qualquer
   novo KEPOS/GRAHAM audit vai retornar schema-adequado em vez de
   PASS fake. Inclui para MEDALLION quando Codex finalizar (se usar
   schema Hawkes similar).
