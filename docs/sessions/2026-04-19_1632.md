# Session Log — 2026-04-19 16:32

## Resumo

Rebuild completo do site de apresentação AURUM (software quant + fundo
family office). Três rodadas de iteração: primeira versão cru/minimal →
gold accent + bento + terminal fake → paleta Half-Life 2 / Source
Engine VGUI alinhada ao launcher + tipografia compacta institucional.
Frontend only, backend não tocado.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 9ff2cf5 | feat(site): rebuild institucional (estrutura modular inicial) | 21 files (+2332/-1013) |
| 3988c65 | feat(site): paleta HL2 + tipografia compacta | 15 files (incl. 3 novos) |

## Mudanças Críticas

Nenhuma mudança em lógica de trading. Zero linhas em CORE PROTEGIDO
(indicators/signals/portfolio/params). Backend (api/, engines/,
core/, launcher) não foi tocado.

## Achados

- Primeira versão tinha `useTransform` em `opacity [1,0]` no scroll do
  hero — conteúdo sumia ao rolar. Removido, parallax mantido só nos
  orbs de fundo.
- Paleta gold (#D4AF37) não convergia com launcher (#D08F36 AMBER HL2).
  Corrigido pra 1:1 com SSOT `core/ui/ui_palette.py`.
- Background estava `#1B1B1B` — mais escuro que o charcoal `#2A2A2A`
  do launcher. Alinhado.
- Tipografia inicialmente grande demais: hero até 148px. Compactado
  pra 72px máx. Body 15 → 13.5. Section title 76 → 48.

## Estado do Sistema

- **Site build**: 1256 modules / 36kB CSS / 701kB JS (3.25s)
- **Server**: `npm run dev` em `localhost:3000` · OK
- **Backend suite**: não rodado nesta sessão (não toquei backend)
- **CORE PROTEGIDO**: intocado
- **Backlog pro site**:
  - Code-split vendor chunks (recharts + framer-motion = maior parte do bundle)
  - Content real dos research notes (hoje placeholder)
  - OG image custom
  - Favicon alinhado ao novo logo
  - A11y audit (contraste, keyboard nav, aria)
  - Responsive QA em mobile real
- **Próximo passo**: validar no browser nos 3 breakpoints (desktop wide
  1440+, laptop 1280, mobile 390) e ajustar o que não caber.

## Arquivos Modificados

**Frontend rebuild:**
- `server/website/index.html` — meta tags institucionais
- `server/website/package.json` — +framer-motion ^12.38.0
- `server/website/src/App.jsx` — compose (859 linhas monolíticas → 22)
- `server/website/src/lib/tokens.js` — HL2 palette tokens (novo)
- `server/website/src/lib/data.js` — engines, métricas OOS reais, principles, research (novo)
- `server/website/src/styles.css` — design system (~1500 linhas)
- `server/website/src/components/`
  - `Logo.jsx` — chevron "A" gradiente amber HL2
  - `Nav.jsx` — scroll-aware, ticker offset
  - `Footer.jsx` — 4-col institutional
  - `Reveal.jsx` — framer scroll reveal wrapper
  - `Metric.jsx` — counter animado
  - `StatusTicker.jsx` — marquee live no topo (novo)
  - `Terminal.jsx` — fake $ aurum status com typing (novo)
  - `charts/EquityCurve.jsx` — recharts area
- `server/website/src/sections/`
  - `Hero.jsx` — eyebrow + title + lede + CTA + 5-metric band + terminal
  - `Bento.jsx` — 5 cards asymmetric (novo)
  - `Thesis.jsx` — 3 pillars
  - `Methodology.jsx` — 5 anti-overfit gates + graveyard 5 engines arquivadas
  - `Performance.jsx` — 3 engine cards + cross-engine OOS table
  - `CodeShowcase.jsx` — 3 code cards com engine snippets (novo)
  - `Technology.jsx` — 5 engines stack + 10-step pipeline
  - `Principles.jsx` — 7 mandamentos translated
  - `Research.jsx` — 4 research notes placeholder
  - `Contact.jsx` — form mailto: fallback

## Notas para o Joao

1. **Validação visual pendente** — abri o server no `localhost:3000` pra
   você avaliar. Três coisas que quero teu olho:
   - Hero cabe agora no primeiro load sem scroll? (era o pain point)
   - Paleta charcoal + amber HL2 tá convergindo com a sensação do
     launcher/cockpit?
   - Tipografia compacta ficou institucional ou ainda tá dramática em
     algum ponto?

2. **Performance do bundle**: 701kB JS = recharts (charting) + framer-motion
   (motion). Pra produção vale code-split em 2 chunks — não urgente, mas
   anote.

3. **Responsive**: testei breakpoints no CSS mas não abri mobile real.
   Se quiser mando ajuste quando você testar.

4. **Conteúdo placeholder**: os 4 "research notes" têm títulos + abstracts
   escritos mas NÃO linkam pra notas reais. O form de contato usa
   `mailto:` (funciona) em vez de API.

5. **Backend não tocado** — toda lógica de trading / engines / api
   permanece exatamente como estava antes desta sessão.
