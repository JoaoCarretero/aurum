# Session Log — 2026-04-20 23:21

## Resumo

ENGINE LOGS refatorado pra LIVE RUNS via subagent-driven development: 13
tasks TDD cobrindo Fase 1 (infra: migration + DB API + backfill + runtime
hooks em paper/shadow + cleanup reversível) e Fase 2 (UI: LiveRunsScreen
espelhando BACKTESTS via ScreenManager). Execution gate aplicado ao `data/`
real: migration 001 no `aurum.db`, 12 cleanup moves, 408 rows backfilled.
Zero toque em CORE PROTEGIDO.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 7f5160a | perf(launcher): cache DataCenterScreen counts within 10s TTL | launcher_support/screens/data_center.py + test |
| f7df15a | docs(plans): live runs refactor — 13-task TDD plan | docs/superpowers/plans/2026-04-20-live-runs-refactor.md |
| 49d0981 | docs(specs): live runs refactor — 3-phase design | docs/superpowers/specs/2026-04-20-live-runs-refactor-design.md |
| 64ff2c6 | feat(db): migration 001 — live_runs table with mode check + indexes | tools/maintenance/migrations/* + tests |
| aeae855 | refactor(db): clarify migration 001 autocommit + stronger mode check test | idem |
| 959aea9 | feat(db): core.ops.db_live_runs — upsert/list/get API | core/ops/db_live_runs.py + shim + tests |
| 6c2f0f1 | refactor(db): live_runs upsert rejects immutable-on-update + since test | idem |
| fb777d4 | feat(db): backfill live_runs table from millennium dirs | tools/maintenance/backfill_live_runs.py + tests |
| 8e4b009 | refactor(db): backfill — single source of truth + status override test | idem |
| 3731adc | feat(paper): upsert live_runs row per tick + on stop | tools/operations/millennium_paper.py + test |
| 0124fc0 | fix(paper): handle restart on same RUN_ID (check-then-split) | idem |
| 8a112a9 | feat(shadow): upsert live_runs row per tick + on stop | tools/maintenance/millennium_shadow.py + test |
| b392137 | feat(maintenance): reversible cleanup — research + legacy runs/ + nexus.db | tools/maintenance/cleanup_data_layout.py + tests |
| aef0511 | fix(cleanup): CRITICAL audit/ is live order trail + active proc guard + sidecar consistency | idem |
| 59f3b4a | ops: apply migration 001 + cleanup + backfill to real data/ | docs/migrations/applied/001_live_runs.md |
| 10e43b7 | feat(launcher): LiveRunsScreen skeleton — filter bar + list render + TTL cache | launcher_support/screens/live_runs.py + test |
| 56fed1e | fix(live_runs): scrollbar + keybind cleanup on exit + reset selection on filter | idem |
| c2b522b | feat(launcher): LiveRunsScreen detail panel — IDENTITY/TIMELINE/PERFORMANCE/ACTIVITY | idem |
| a9e3998 | feat(launcher): LiveRunsScreen actions — OPEN DIR + ARCHIVE | idem |
| 9ff69e8 | refactor(live_runs): consistent db_live_runs ref + _open_dir no-op | idem |
| 606fc78 | feat(launcher): wire LIVE RUNS into DATA CENTER menu (key L) + registry | registry.py + launcher.py + data_center.py + test |
| eed890f | fix(launcher): live_runs focus_set + section comment + TTL tests hermetic | idem |
| 41e8b4b | test(integration): LIVE RUNS screen end-to-end via ScreenManager | tests/integration/test_launcher_live_runs.py |

Total: 23 commits (+1 `7f5160a` do inicio da sessão com data_center TTL fix).

## Mudanças Críticas

**ATENÇÃO:** Mudanças que afetam RUNTIME de paper/shadow (mas não lógica
de trading):

- **Nova tabela `live_runs` em `aurum.db`** (15 cols, 2 indexes). 408 rows
  backfilled a partir de `data/millennium_{live,paper,shadow}/` e `data/live/`.
- **Runtime hooks em `millennium_paper.py` e `millennium_shadow.py`** —
  cada tick chama `db_live_runs.upsert(...)` (best-effort, try/except,
  nunca crasha runner). Check-then-split handles restart on same RUN_ID
  sem gerar log noise.
- **`_archive/` criado em `data/`**: research dirs + legacy `data/runs/` +
  `nexus.db` (com sidecars) movidos pra lá. Reversível via comandos `[undo]`
  impressos no apply.

**ZERO mudança em CORE PROTEGIDO** (`core/indicators.py`, `core/signals.py`,
`core/portfolio.py`, `config/params.py`). Backtests calibrados continuam
válidos.

## Achados

- **`data/audit/` é live order trail** (`core.risk.audit_trail.AuditTrail`),
  NÃO research. Code reviewer pegou isso em review de Task 6; quase foi
  arquivado por engano. Removido da `_RESEARCH_DIRS` antes de apply.
- **6 dirs `multistrategy_*` em `data/runs/`** não foram movidos porque
  `multistrategy` não está em `_ENGINES_PRESERVED`. Decisão pendente do
  Joao: adicionar à lista + consolidar em `data/multistrategy/`, ou
  arquivar, ou deixar.
- **`core.ops.proc.list_procs()` não tem campo `run_dir`** — active-proc
  guard do cleanup ficou nível engine (skip `data/runs/<engine>_*` se
  engine tem proc alive). Subagent adaptou e documentou a limitação.
- **Restart bug em paper hook (review catch)**: processo morria e reiniciava
  no mesmo RUN_ID, stale row fazia `upsert` raise ValueError em loop. Fix:
  check-then-split (`get_live_run` antes de decidir INSERT vs UPDATE).

## Estado do Sistema

- **Smoke test:** 178/178 passed
- **Live_runs DB:** 408 rows (299 live + 48 paper + 61 shadow), todos
  `status=stopped` (backfill de dirs históricos, VPS hooks ainda não
  rodaram tick com o DB ligado)
- **Tests novos:** 4 (migration) + 9 (db_live_runs, inc fixes) + 5 (backfill)
  + 2 (paper+shadow hook existence) + 9 (cleanup) + 6 (live_runs_screen)
  + 1 (integration) = **36 tests novos**
- **Suite launcher + integration + tools + core relevant:** 160 passed, 0 fail
- **Backlog restante:**
  - Decidir destino dos 6 `multistrategy_*` dirs em `data/runs/`
  - Reiniciar paper/shadow no VPS pra ativar runtime hooks (novo código
    precisa deploy)
  - Fase 3 (opcional): retenção 90d, sparkline equity, Telegram deep link
- **Próximo passo sugerido:** visualizar a LIVE RUNS screen navegando no
  launcher — abrir DATA CENTER > L. Com 408 rows no DB, o filtro + detail
  panel devem aparecer populados.

## Arquivos Modificados

**Novos (Fase 1 infra):**
- `tools/maintenance/migrations/__init__.py` (+1)
- `tools/maintenance/migrations/migration_001_live_runs.py` (+44)
- `tools/maintenance/backfill_live_runs.py` (+~150)
- `tools/maintenance/cleanup_data_layout.py` (+~180)
- `core/ops/db_live_runs.py` (+~110)
- `core/db_live_runs.py` (+6 shim)
- `tests/tools/test_migration_001.py` (+~80)
- `tests/tools/test_backfill_live_runs.py` (+~130)
- `tests/tools/test_cleanup_data_layout.py` (+~150)
- `tests/tools/test_shadow_runner_db_hook.py` (+10)
- `tests/core/test_db_live_runs.py` (+~130)
- `tests/integration/test_paper_runner_db_hook.py` (+13)
- `docs/migrations/applied/001_live_runs.md` (+66)

**Novos (Fase 2 UI):**
- `launcher_support/screens/live_runs.py` (+~250)
- `tests/launcher/test_live_runs_screen.py` (+~180)
- `tests/integration/test_launcher_live_runs.py` (+~55)

**Modificados:**
- `tools/operations/millennium_paper.py` (+~50)
- `tools/maintenance/millennium_shadow.py` (+~80)
- `launcher_support/screens/registry.py` (+~5)
- `launcher_support/screens/data_center.py` (+~18)
- `launcher.py` (+~15)
- `tests/launcher/test_data_center_screen.py` (+~5)

**Docs:**
- `docs/superpowers/specs/2026-04-20-live-runs-refactor-design.md` (+303)
- `docs/superpowers/plans/2026-04-20-live-runs-refactor.md` (+~2200)

## Notas para o Joao

1. **Deploy VPS necessário pra ativar hooks.** `millennium_paper.py` e
   `millennium_shadow.py` agora chamam `db_live_runs.upsert(...)` por
   tick. Isso só roda quando o VPS puxa o novo código. Reiniciar os
   serviços paper/shadow no VPS depois do próximo git pull.

2. **6 dirs `multistrategy_*` em `data/runs/` pendentes.** Não foram
   movidos pelo cleanup. Decidir:
   - (a) adicionar `multistrategy` em `_ENGINES_PRESERVED` e rodar
     cleanup de novo → vão pra `data/multistrategy/` (consistente)
   - (b) arquivar em `data/_archive/multistrategy/`
   - (c) deixar como estão

3. **`data/audit/` ficou intocado** — é o live order trail, não research.
   Review salvou.

4. **Next step imediato pra validar:** abrir launcher → MAIN → DATA CENTER
   → `L`. Deve aparecer 408 rows (299 live + 48 paper + 61 shadow). Filtro
   por mode, auto-select newest, detail panel com 4 sections + OPEN DIR +
   ARCHIVE buttons.

5. **Fase 3 marcada como opcional** (retenção 90d, sparkline equity,
   Telegram deep link, hook em `engines/live.py`). Qualquer uma pode virar
   task futura se aparecer necessidade.
