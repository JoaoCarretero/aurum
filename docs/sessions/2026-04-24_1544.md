# Session Log — 2026-04-24 15:44

## Resumo
Sessão focada em dois eixos do cockpit: (1) sidebar ENGINES redesigned pra linha única densa com alinhamento per-pixel entre bucket headers e rows, (2) trade history operacional em paper mode — fetch via VPS API + aggregator cross-run pra ver trades de runs stopped (ex: OPUSDT fechado 13:48 UTC que tinha sumido do cockpit após restart 17:40). Também: audit completo VPS↔DB↔disco, backfill `live_runs` local com 149 runs novos do VPS (DB 8 → 157), archive de 588 dirs órfãos (smoke-test leftovers, reversível em `data/_archive/`).

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 9630f4e | style(cockpit): widen sidebar 24→30 + 260px floor | launcher_support/engines_live_view.py + 2 research-desk (carona do index) |
| d6a2390 | style(cockpit): single-line sidebar rows — 4 cols, denser | launcher_support/engines_live_view.py |
| 2f8b38c | feat(cockpit): paper pane now renders trade history | engines_live_view.py + 2 test files |
| fc0e863 | fix(cockpit): paper TRADE HISTORY section always renders | launcher_support/engines_sidebar.py |
| ca6d6df | fix(cockpit): sidebar — uptime for VPS engines + header align | launcher_support/engines_live_view.py |
| e54adb5 | fix(cockpit): paper_extras — force remote fetch for VPS-only runs | launcher_support/engines_live_view.py |
| ad68046 | style(cockpit): bucket headers use the row's 4-col grid | launcher_support/engines_live_view.py |
| 17c97bb | feat(cockpit): paper TRADE HISTORY aggregates across all runs | engines_live_view.py + test + tools/diag/paper_trades_probe.py |
| c6deb4a | fix(audit): backfill live_runs from VPS + cover all engines | tools/maintenance/backfill_live_runs.py + test + tools/diag/live_engines_audit.py |
| da52f86 | chore(maintenance): cleanup_orphan_run_dirs.py — archive stale shells | tools/maintenance/cleanup_orphan_run_dirs.py |
| 9590ba5 | fix(cockpit): aggregator covers runs beyond top-10 by recency | engines_live_view.py + test |

## Mudanças Críticas
Nenhuma mudança em lógica de trading — o trabalho foi cockpit/UI + audit tooling + data hygiene. `core/indicators.py`, `core/signals.py`, `core/portfolio.py`, `config/params.py` intactos.

## Achados
- **VPS↔DB sync não existia** pra máquina dev: runners rodam no VPS e escrevem na aurum.db do VPS; a máquina local tinha só 8 rows históricas de Apr 21-22 e nada dos 156 runs ativos. O fix `backfill_live_runs.py --from-vps` preenche esse buraco (e o backfill antigo só cobria `millennium_*`, ignorava citadel/jump/renaissance/probe — ampliado).
- **Aggregator cross-run era cego a runs idle recentes**: top-10 por `started_at` ficava cheio de restarts sem signal, empurrando pra fora os runs antigos que tinham trade real. Agora filtra por `novel_total > 0` antes de capar em 15.
- **Early-return no `_fetch_paper_extras_sync`**: a síntese de `account` a partir do HB fazia o local-path satisfazer a condição de retorno mesmo sem nenhum arquivo de estado no disco, pulando o remote fetch pros runs VPS-only. Tracked com `had_disk_data` agora.
- **Posição órfã em `2026-04-21_012204`** (millennium paper): VPS heartbeat ainda reporta status=running com 1 posição LINKUSDT SHORT entry 9.35 aberta há 3 dias; DB já reconciliou pra stopped. Sem endpoint de flatten em `cockpit_api.py` — cleanup requer SSH + systemctl na VPS, fora do escopo dessa sessão.
- **SSH tunnel flaky**: durante o audit primeiro rodou com tunnel caindo no meio, marcando 9 mismatches falsos como "tunnel dropped". O audit reporta erros separadamente agora (não conta como "0 trades").

## Estado do Sistema
- **Tests**: 2034 passed, 72 skipped (full suite excluding test_arb_hub_v2 test-order flake). 101 targeted cockpit tests.
- **Tunnel**: UP (port 8787 → VPS 8787)
- **DB sync**: 157 live_runs (VPS 156 + 1 legacy), 0 VPS-running-sem-DB mismatches
- **Órfãos**: 0 dirs pendentes; 588 archived em `data/_archive/`
- **Backlog restante**: posição órfã LINKUSDT do 2026-04-21; automação do sync VPS→DB (hoje é manual, precisa cron a cada 5-10min)
- **Próximo passo sugerido**: rodar `launcher.py` e validar visualmente sidebar + TRADE HISTORY em cada engine; depois decidir se quer agendar cron pra `backfill_live_runs.py --from-vps --apply` (ver /schedule).

## Arquivos Modificados
- `launcher_support/engines_live_view.py` (+350 / -150 ao longo dos 11 commits)
- `launcher_support/engines_sidebar.py` (+15 / -6)
- `tools/maintenance/backfill_live_runs.py` (+120 / -30)
- `tools/maintenance/cleanup_orphan_run_dirs.py` (novo, 184 linhas)
- `tools/diag/paper_trades_probe.py` (novo, ~95 linhas)
- `tools/diag/live_engines_audit.py` (novo, ~250 linhas)
- `tests/test_engines_live_view_cockpit.py` (+105)
- `tests/tools/test_backfill_live_runs.py` (+70)
- `tests/integration/test_engines_live_view.py` (+1)

## Notas para o Joao
- **O ticker que você tinha esquecido era `OPUSDT`** (millennium paper, 13:48 UTC hoje, LONG, entry 0.12266, exit 0.12270, pnl +$0.58). Também tem um `RENDERUSDT` LONG pnl +$7.10 às 04:50 UTC (millennium + renaissance paper). Tudo visível agora via `python tools/diag/paper_trades_probe.py` e no cockpit TRADE HISTORY após restart do launcher.
- **Abra o cockpit e confirma visualmente** que a sidebar tá alinhada (bucket headers RUNNING/READY colados na mesma coluna que os badges RN/RD/LB) e que TRADE HISTORY mostra os trades. Se ver algo torto em monitor menor/maior, me manda screenshot numa próxima sessão — as larguras estão em chars, algumas fontes podem renderizar diferente.
- **SSH tunnel hoje caiu 2x durante a sessão** — parece instável. Se repetir muito, vale investigar: (a) VPS tá com ServerAliveInterval enforcement? (b) rede residencial cortando conexões long-lived? (c) Windows TCP keepalive defaults? Não é blocker, só irritante — o launcher principal tem `ssh_tunnel.py` que auto-respawna, então rodando pelo launcher deve mitigar.
- **Posição órfã LINKUSDT SHORT** da run `2026-04-21_012204` precisa de atenção quando sobrar tempo. Não afeta trades novos (run stopped no DB, não entra no rotation), mas a posição "fantasma" no VPS state file pode gerar ruído em métricas históricas.
- **588 dirs foram archived em `data/_archive/`** (27MB). Podem ser deletados de vez quando você quiser — só rodar `rm -rf data/_archive/<subdir>`. Deixei reversível porque alguns podem ter conteúdo útil que não inspecionei individualmente.
- **Tooling novo pra auditoria** pós-sessão: `live_engines_audit.py` roda read-only, pode virar cronjob/health-check; `paper_trades_probe.py` é útil quando precisa ver estado bruto da API VPS rápido; `cleanup_orphan_run_dirs.py --apply` reciclável.
