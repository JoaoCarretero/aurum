# Session Log — 2026-04-22 18:16

## Resumo
Sessão de polimento do `/engines` launcher screen (flicker fix + detail panel
refactor, audit via subagent, 2 regressões caçadas e corrigidas) e criação
da **PROBE engine** — runner diagnóstico standalone que prova que o pipeline
(fetch → indicators → omega → regime) está vendo o mercado quando todas as
engines estão com `novel=0`. Deployed no VPS, visível no cockpit.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 328583c | fix(engines-view): dedup guard elimina flicker de 2s no ENGINE LOGS | launcher_support/engine_logs_view.py, launcher_support/runs_history.py |
| 0a69233 | feat(probe): standalone diagnostic engine pra validar pipeline em mercado mormo | tools/maintenance/probe_runner.py, deploy/aurum_probe@.service, deploy/install_probe_vps.sh |

## Mudanças Críticas
Nenhuma mudança em lógica de trading. PROBE é read-only: chama `decide_direction` +
`score_omega` em modo diagnóstico (nunca posiciona ordem, nunca registra trade,
nunca toca portfolio). Zero toque em `core/signals.py`, `core/portfolio.py`,
`core/indicators.py`, `config/params.py` — protocolo CORE PROTECTED respeitado.

## Achados
- **Flicker root cause:** `engine_logs_view.refresh_list` fazia `destroy-all + rebuild`
  a cada 2s, independentemente do conteúdo ter mudado. Dedup signature por
  `(rid, pid, alive, engine, novel, selected_key, mode_filter)` resolve.
- **Regressão pega no audit:** selection highlight não disparava repaint sob dedup
  porque clicar na row não mudava content signature. Fix: `_eng_selected_key`
  entra na signature.
- **Regressão 2:** `runs_history` dedup só comparava `run_id` — transição
  `running→stopped` não repintava o dot verde quando processo morria. Fix:
  signature agora inclui `status` + `roi_bucket` (round 2 casas evita floating
  point flicker).
- **PROBE tick 1 no VPS (23:15:37 CEST):** top_score=0.355 FETUSDT LONG em
  regime BULL com threshold 0.62 — n_above_60pct=0, n_above_threshold=0.
  Mercado **realmente morno**, não é filtro quebrado.

## Estado do Sistema
- Smoke test launcher: 45/45 passando nos arquivos tocados (test_runs_history,
  test_screen_entrypoints, test_data_center_screen, test_live_runs_screen)
- 10 failures pré-existentes em tests/integration (arbitrage_hub + engines_live_view
  paper mode) — confirmado via stash que NÃO são causados por mim
- VPS: 11 services active (10 engines + cockpit_api + **nova probe@desk-a**),
  load 0.48/0.12/0.08, 2.3GB/12GB RAM, zero ERRORs em 1h
- Backlog restante: pre-existing arbitrage_hub test failures (fora de escopo)
- Próximo passo sugerido: monitorar probe por 1-2h pra ver se top_score sobe
  quando mercado mexer; se sim, é benchmark confirmado pro que as engines
  "veem"

## Arquivos Modificados
| Arquivo | Delta |
|---|---|
| launcher_support/engine_logs_view.py | +45/-17 (dedup guard) |
| launcher_support/runs_history.py | +170/-97 (dedup + detail refactor) |
| tools/maintenance/probe_runner.py | +375/0 (novo) |
| deploy/aurum_probe@.service | +27/0 (novo) |
| deploy/install_probe_vps.sh | +54/0 (novo) |

## Notas para o Joao
- **PROBE no cockpit:** `run_id=2026-04-22_211537s_desk-a engine=probe mode=shadow`.
  Vai aparecer no launcher `/engines LIVE RUNS` e `LOGS` automaticamente — não
  precisa reiniciar launcher.
- **Heartbeat da probe:** `top_score, mean_score, n_above_{threshold,80pct,60pct},
  top_symbol, top_direction, macro, threshold`. Dá pra ler direto pra saber se
  pipeline tá vendo mercado ou morto.
- **Probe em logs do VPS:** `journalctl -u aurum_probe@desk-a -f` mostra uma
  linha por tick com `top=X.XXX top_sym=YYY mean=Z.ZZZ macro=BULL above_thr=N`.
  Instante-diagnóstico: em mercado ativo o above_thr sobe; em morno fica 0.
- **VPS está em `feat/per-engine-runners`, não `feat/phi-engine`.** Fiz
  cherry-pick só dos 3 arquivos da probe via `git checkout origin/feat/phi-engine -- ...`
  pra não switch branch e afetar as engines ativas. Se for mergear essas branches
  depois, sem problema — probe vai junto.
- **VPS usa `User=root` nos services**, não `aurum`. Install script aceita
  argumento. Passei `root` como service_user.
