# Session Log — 2026-04-22 19:55

## Resumo
Sessão extensa de UX polish do cockpit — porta métricas SCAN/HEALTH/
PROBE/ERROR pra LIVE RUNS + HL2 pane, fix de lazy-fetch heartbeat (antes
ficava None sempre), rename RUNNING NOW → ENGINES, fix close-button 1-click,
merge 2 tabelas ENGINES em 1 (sidebar rail absorvida pelo master list),
remove chip bar do `/engines`, separa LIVE/FINISHED em runs_history, fix
margens brancas via `tk_setPalette`, e async lazy-fetch pro audit. Revert
catastrófico recuperado cleanly.

## Commits (13, excluindo revert/docs intermediários)
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 299f861 | feat(live-runs): porta SCAN/HEALTH/PROBE/ERROR sections | screens/live_runs.py, engines.py, registry.py |
| 4457572 | fix(runs-view): lazy-fetch heartbeat VPS no click | runs_history.py, screens/live_runs.py |
| 9a4568a | feat(live-cockpit): 3 secoes novas no HL2 detail pane | engines_sidebar.py |
| db8af70 | feat(cockpit): rename RUNNING NOW → ENGINES + flicker fix + font bump | engines_live_view, engines_sidebar, runs_history, engines_live_helpers, test |
| c51e60a | fix(launcher): close button 1-click via daemon thread | launcher.py |
| 98f989c | feat(cockpit): merge 2 tabelas ENGINES em 1 | engines_sidebar.py, engines_live_view.py |
| d9e5db6 | refactor(cockpit): 1 tabela ENGINES — master list absorve sidebar | engines_live_view.py |
| be7d283 | refactor(data-engines): remove chip bar | screens/engines.py |
| 33815aa | feat(runs-view): separa LIVE em cima, FINISHED embaixo | runs_history.py |
| 8602903 | Revert 557978f (que regrediu ~1000 linhas acidentalmente) | launcher.py |
| 9a38f24 | fix(launcher): tk_setPalette força BG default (margens brancas) | launcher.py |
| 01436ce | fix(runs-view): lazy_fetch_heartbeat async (audit fix) | runs_history.py, screens/live_runs.py |

## Mudanças Críticas
Nenhuma mudança em lógica de trading. Todas as 13 mudanças são UI/UX
ou wiring de heartbeat fetch. Zero CORE tocado (`core/signals.py`,
`core/indicators.py`, `core/portfolio.py`, `config/params.py`).

## Achados
- **collect_vps_runs não popula heartbeat** — foi optimizado (2*N
  round-trips eliminados) mas detail pane precisa dos campos ricos.
  Solução: `lazy_fetch_heartbeat` sob demanda no click.
- **Flicker 2× por tick em runs_history** — local+db sempre pintava
  ANTES do VPS async retornar, e depois VPS paint comparava contra
  state com formato diferente. Fix: `state["last_sig"]` rastreia
  último sig pintado, bootstrap só na 1ª refresh.
- **Close button 2 clicks** — `tunnel.stop(1s)` + `poller.stop(0.5s)`
  bloqueavam main thread até 3s após clicar X. Fix: daemon thread.
- **Regressão catastrófica no 557978f** — minha Edit aplicou em
  cima de estado stale do arquivo, reverteu 1000+ linhas de
  launcher.py (incluindo arb_hub + close button fix). Recuperado
  via `git revert` + reaplicação limpa. Lição: `Read` antes de
  cada `Edit` em launcher.py (arquivo grande, multi-agent).
- **"Margens brancas"** reportadas 3×. Raiz: `SystemButtonFace` do
  Windows em qualquer widget Tk sem `bg=` explícito (inclui
  messagebox, Menu, Toplevel criados internamente). `tk_setPalette`
  força BG em todos defaults.
- **Audit async** — confidence 85 no 5s freeze; fix via
  `on_complete` callback + daemon thread.

## Estado do Sistema
- Testes: 115/115 passando em launcher/runs_history após todos os
  commits. 8 failures pré-existentes em tests/integration
  (arbitrage_hub + engines_live_view paper mode) confirmados via
  stash NÃO causados por mim.
- VPS: 11 services ativos (10 engines + cockpit + probe@desk-a).
- Backlog: async pattern pode ser expandido pra engines_sidebar
  HL2 strip se necessário (não pegado no audit como must).

## Arquivos Modificados (sessão inteira)
| Arquivo | Delta |
|---|---|
| launcher.py | +62/-20 (close button daemon + tk_setPalette) |
| launcher_support/runs_history.py | +180/-50 (lazy fetch + async + live/finished + safe float) |
| launcher_support/screens/live_runs.py | +100/-15 (enrichment + async + client_factory) |
| launcher_support/screens/engines.py | +42/-160 (chip bar removido) |
| launcher_support/screens/registry.py | +8/-2 (client_factory pra live_runs standalone) |
| launcher_support/engines_sidebar.py | +180/-10 (3 HL2 sections + font bump) |
| launcher_support/engines_live_view.py | +90/-50 (master list absorve sidebar + sub-rows) |
| launcher_support/engines_live_helpers.py | +3/-3 (RUNNING NOW → ENGINES) |
| launcher_support/engine_logs_view.py | +45/-17 (dedup guard flicker) |
| tests/integration/test_engines_live_view.py | +1/-1 (bucket_title assert) |

## Notas para o Joao
- **Feche e reabra o launcher** pra ver:
  1. Margens escuras (antes brancas) em tudo
  2. X fecha no 1º click
  3. `/engines`: 1 só pane unificado (sem chip bar HISTORY/LIVE/LOGS)
  4. RUNS HISTORY: `● LIVE · N` em cima, gap, `○ FINISHED · N` embaixo
  5. Detail pane (click numa row): SCAN + HEALTH + PROBE + ERROR + LOG TAIL
  6. Live Cockpit: RUNNING NOW → ENGINES label
  7. Master list expandido: clicar CITADEL mostra sub-rows `└ PAPER desk-a 10t`
  8. HL2 detail pane: telemetry strip + ERROR banner + SCAN strip + PROBE strip
  9. Click numa run: detail pinta instantâneo; heartbeat completa async
- **Recuperação de revert** — commit 557978f reverteu ~1000 linhas
  acidentalmente; 8602903 restaurou; 9a38f24 reaplicou só o palette
  fix (+14 linhas, clean). Código intacto, confirmado.
- **LiveRunsScreen + engine_logs_view** continuam registrados em
  `registry.py` pra atalhos R/L e splash quick-links. Entrada
  principal via DATA > ENGINES é o unificado agora.
- **Próximo passo se achar lento no 1º click de uma run**: agora
  é async, não trava. Se heartbeat não aparecer após 2-3s, VPS
  tunnel provavelmente down; verificar footer.
