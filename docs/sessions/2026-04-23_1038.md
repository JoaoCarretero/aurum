# Session Log — 2026-04-23 10:38

## Resumo
Sessão de pós-migração OneDrive→local: recriação do venv (Py 3.11), fix
de atalho AURUM.lnk quebrado, auditoria completa (configs/runs/DB/VPS),
dois fixes de bug (2x Ctrl+C nos runners + colisão de PK em live_runs),
deploy VPS com merge limpo + restart de 11 procs, migração dos 11
nohups pra systemd, httpx add ao [dev] e baseline de testes full.

## ATENÇÃO: Bug afetou observabilidade de live_runs
O fix de RUN_ID (f1a4ce4) corrige colisão de PK no `live_runs` que
silenciava per-engine paper/shadow runners quando rodavam com o mesmo
label no mesmo segundo. `db_live_runs.upsert` não levanta erro visível
nesse caso — só os campos mutáveis (tick_count, equity, novel_count)
sobrescreviam-se mutuamente no mesmo registro. **Logs de live_runs
anteriores a 2026-04-23 12:58 UTC de `citadel_paper` e `renaissance_paper`
rodando junto com `jump_paper` no mesmo desk estão ausentes do DB**
— os ticks correram mas o DB não registrou por engine.

## Commits
| Hash | Branch | Mensagem | Arquivos |
|------|--------|----------|----------|
| b65ee47 | chore/repo-cleanup | fix(runners): 2o SIGINT forca sys.exit imediato | tools/operations/millennium_paper.py, tools/maintenance/millennium_shadow.py, tools/maintenance/probe_runner.py |
| 8155891 | feat/per-engine-runners | fix(runners): 2o SIGINT forca sys.exit imediato | tools/operations/_paper_runner.py, tools/operations/millennium_paper.py, tools/maintenance/_shadow_runner.py, tools/maintenance/millennium_shadow.py |
| f1a4ce4 | feat/per-engine-runners | fix(per-engine): engine no RUN_ID evita colisao em live_runs PK | tools/operations/run_id.py, tools/operations/_paper_runner.py, tools/maintenance/_shadow_runner.py, tests/tools/test_run_id.py |
| cfe29d1 | feat/per-engine-runners (VPS) | wip(vps): deploy scripts + probe_runner snapshot pre-merge | deploy/aurum_probe@.service, deploy/install_engine_vps.sh, deploy/install_probe_vps.sh, tools/maintenance/probe_runner.py |
| 3da995f | feat/per-engine-runners (VPS) | Merge origin/feat/per-engine-runners into feat/per-engine-runners | merge commit (30 files reconciled, zero conflitos) |
| 0a25031 | chore/repo-cleanup | chore(deps): add httpx>=0.27,<1 to [dev] extras | pyproject.toml |

Pushed: ambos branches `chore/repo-cleanup` e `feat/per-engine-runners`.

## Mudanças Críticas
**Sim, mudanças em lógica operacional dos runners (não em sinais/custos/sizing):**

1. **SIGINT escalonado em 7 arquivos** (_paper_runner + _shadow_runner do
   per-engine + millennium_paper/shadow standalone + probe_runner):
   primeiro Ctrl+C → shutdown limpo via flag + `finally:` (≤2s + cleanup);
   segundo Ctrl+C → `sys.exit(130)` imediato. Não afeta mecânica de
   trading, apenas UX de desligamento.

2. **RUN_ID inclui engine** em per-engine paper/shadow (via novo param
   opcional em `build_run_id`). **Muda formato do RUN_ID e portanto do
   caminho `data/<engine>_paper/<RUN_ID>/`** quando per-engine rodam
   com mesmo label no mesmo segundo. Millennium inalterado (não colide).
   Backward compat: `engine=None` preserva formato antigo.

## Achados
1. **Atalho `AURUM.lnk` apontava pro OneDrive antigo** — target, args,
   workdir e icon todos em `C:\Users\Joao\OneDrive\aurum.finance\...`.
   Corrigido pra `C:\Users\Joao\projects\aurum.finance\...` com
   `.venv\Scripts\pythonw.exe` (Py 3.11) como interpretador.

2. **Python 3.14 do sistema sem wheels** pra `hmmlearn`/`arch` (requer
   MSVC). Solução: venv com Py 3.11.5 — wheels tudo OK, install limpo.

3. **Screen `aurum` x 2 stuck no VPS desde 10/04** — engines.live
   demo travado no prompt interativo de basket por 13 dias. Mortos.
   Screen `hermes` 24173 era só bash vazio, morto.

4. **Bug de colisão de PK em live_runs** — ver seção ATENÇÃO acima.
   Confirmado empiricamente: citadel e renaissance paper ticks não
   apareciam no DB; só jump (que vencia a race do upsert inicial)
   ficava registrado. Fix testado em produção após restart.

5. **Lentidão de boot do launcher (~2074ms)** — 375ms Tk chrome +
   115ms splash + ~1580ms gap não-instrumentado (Tk DPI + iconbitmap
   + first-time .pyc compile + Defender scanning pasta nova).
   Mitigação: Defender exclusion adicionada em
   `C:\Users\Joao\projects\aurum.finance` via elevated PowerShell.

6. **11 procs no VPS rodavam via nohup** apesar de haver 13 unit
   files systemd já instalados com `User=root`. Enabled + started
   via systemctl — agora auto-restart em failure e sobrevivem reboot.

## Estado do Sistema
- **Suite de testes**: 1740 passed / 8 skipped / 0 failed em 108.85s
  (baseline completo restaurado). `test_cockpit_paper_endpoints.py`
  skipado via `--ignore` por Windows fatal exception em GC de anyio/
  asyncio TestClient — issue de lib, não do código.
- **Testes de engines apenas**: 137/137 passed em 13.46s
- **VPS**: 12 services systemd active (11 per-engine + cockpit_api),
  enabled + auto-restart. Heartbeats saudáveis, zero errors em logs.
- **DB `aurum.db`**: integrity OK, 11 `running` rows correspondendo
  1-pra-1 com os 11 procs, 6 `stopped` rows das antigas sessões
  (nohup + pre-fix) com `ended_at` correto.
- **Backlog restante**:
  - Investigar `test_cockpit_paper_endpoints.py` se virar critical path
  - Investigar `novel=0` em shadow runners se persistir por 5+ ticks
  - `.aurum_procs.json` resetado; launcher pode popular naturalmente
    no próximo boot
- **Próximo passo sugerido**: observar VPS 2-4h pra ver se os 11
  systemd services mantêm saúde (tick counts, novel signals aparecem)
  e se restart automático funciona se algum proc falhar.

## Arquivos Modificados
### Branch `chore/repo-cleanup`
- `tools/operations/millennium_paper.py` (+3)
- `tools/maintenance/millennium_shadow.py` (+3)
- `tools/maintenance/probe_runner.py` (+3)
- `pyproject.toml` (+1)

### Branch `feat/per-engine-runners`
- `tools/operations/_paper_runner.py` (+4)
- `tools/operations/millennium_paper.py` (+3)
- `tools/maintenance/_shadow_runner.py` (+4)
- `tools/maintenance/millennium_shadow.py` (+3)
- `tools/operations/run_id.py` (+14/-2)
- `tests/tools/test_run_id.py` (+32)

### VPS side-effects
- `/etc/systemd/system/*.service` — 11 instâncias enabled + started
  (citadel/jump/renaissance paper+shadow @desk-a, millennium
  paper @desk-paper-a/b, millennium shadow @desk-shadow-a/b, probe @desk-a)
- `data/.aurum_procs.json` — resetado pra `{"procs":{}}`
- Ambos `aurum` screens mortos, `hermes` screen morto

### Windows side-effects (não-git)
- `C:\Users\Joao\Desktop\AURUM.lnk` — reescrito (target/args/workdir/icon)
- Defender exclusion adicionada: `C:\Users\Joao\projects\aurum.finance`

## Notas para o Joao
- **Ações manuais restantes**: nenhuma. Tudo que precisava UAC ou
  admin já foi feito (Defender OK).
- **Observação pendente**: `novel=0` nos shadow runners por 5-6 ticks
  consecutivos antes do restart — pode ter sido bug de colisão de PK
  + mercado chop. Pós-restart com fix aplicado, vale monitorar 2h
  pra ver se novelty começa a aparecer conforme esperado.
- **Se o VPS rebootar agora**, os 11 services voltam sozinhos (enabled).
  Teste opcional: `sudo reboot` e confirmar em 2-3 minutos que procs
  estão up via `systemctl is-active citadel_paper@desk-a.service` etc.
- **Para parar tudo**: `systemctl stop citadel_paper@desk-a jump_paper@desk-a ...`
  ou um loop (lista das 11 units está na seção systemd acima).
- **Commits em 2 branches** — `chore/repo-cleanup` é o branch de dev
  local, `feat/per-engine-runners` é o branch que o VPS usa. A fix do
  SIGINT foi aplicada em ambos (mesmo problema, arquivos diferentes).
  A fix do RUN_ID é per-engine-only (só existe no branch que tem o
  `_paper_runner`/`_shadow_runner` compartilhados).
