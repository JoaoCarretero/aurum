# Session Log — 2026-04-16 22:01

## Resumo
Bateria forense em 4 engines (KEPOS, GRAHAM, JUMP, DE SHAW) com meta
"edge em todos, só arquivar se não tiver jeito". Resultado: **JUMP 6/6
PASS confirmado em 730d** (vencedor real, não cherry-pick). **KEPOS
melhorou pra Sharpe 1.50 em layer1** (vs 1.08 em bluechip, vs 1.53
cherry-pick). **DE SHAW encontrou primeiro break-even da história**
(z=3.0 pvalue=0.05, 18 iters prévias todas negativas) — ROI +3.09%,
Sharpe +0.14. **GRAHAM arquivado oficialmente** — sem edge em 1h, 4h,
1d (insufficient bars), bluechip, layer1. Baselines sem Hawkes
catastróficos (ROI -1380%). CLI `--z-entry/--z-exit/--pvalue/--hl-max/
--max-hold/--size-mult` adicionados ao DE SHAW pra sweeps sem tocar
config.params.

## Commits
_Pendente — vai commitar após este log._

## Mudanças Críticas
**ATENÇÃO: alteração em engines de trading (KEPOS default, DE SHAW CLI).**

- `engines/kepos.py:KeposParams.eta_sustained_bars` — default 10 mantido.
  Docstring atualizado com sweep em **layer1** (Sharpe 1.50 DD 10.75%)
  vs bluechip (Sharpe 1.08). Layer1 é recomendado.
- `engines/deshaw.py:__main__` — adicionados 7 CLI overrides:
  `--z-entry`, `--z-exit`, `--z-stop`, `--pvalue`, `--hl-max`,
  `--max-hold`, `--size-mult`. Reassign das globals NEWTON_* feito
  explicitamente ao parsear args. Nenhuma mudança em config.params.
- `engines/deshaw.py` docstring: adicionado "Edge status 2026-04-16"
  documentando a primeira config break-even (z=3.0 exit=0.5 pvalue=0.05
  hl=300).
- `engines/graham.py` docstring: expandido edge status com findings
  de 4h layer1 e 1d (too_few_bars). Recomendação de arquivamento.
- Core protegido (`core/indicators`, `core/signals`, `core/portfolio`,
  `config/params`) NÃO foi tocado. Overrides DE SHAW usam apenas
  reassign de globals dentro do `__main__`.

## Achados
1. **JUMP é vencedor real**: 730d bluechip 1h → ROI +50.52%, WR 62.9%,
   MaxDD 1.7%, Sharpe +4.56. **Audit 6/6 PASS** (walkforward 5/5 win,
   sensitivity, concentration, 3 regimes profitable, decay -11%,
   breakeven 27bp). 18+ iters prévias já tinham calibrado params.
   Regra "não mexe em vencedor" aplicada. Candidato pra FROZEN_ENGINES.
2. **KEPOS: layer1 > bluechip**. Sweep de baskets em 730d com
   sustained=10:
     bluechip   : Sharpe 1.08, DD 11.6% (309 trades)
     layer1     : Sharpe 1.50, DD 10.75% (236 trades) ✓
     majors     : Sharpe 0.64, DD 17.1% (pior — XRP dreno amplificado)
   Layer1 tem melhor relação símbolos líquidos / sem XRP. SOL ainda
   carrega 51% do PnL, mas diluído vs 60% em bluechip.
3. **KEPOS eta cliff confirma em todos baskets**: eta_critical > 0.78
   quebra edge. Sweep 0.75/0.78/0.80 em layer1 mostra 0.75 vence por
   larga margem. k_sigma=2.0 filtra todos os trades (0 ativos).
4. **DE SHAW primeiro break-even histórico**: 18 iterações prévias
   (comment em params.py: "NÃO USAR EM LIVE, melhor -2.3%"). Sweep
   hoje descobriu:
     defaults z=2.0:                     ROI -15.4% (2/6 PASS)
     z=3.0 exit=0.0 pvalue=0.05:         ROI +3.09% (3/6 PASS) ✓
     z=3.0 exit=0.5 hl=300:              ROI +1.21%
     z=3.5 pvalue=0.05:                  ROI -4.77% (over-selective)
     z=3.0 exit=0.8:                     ROI -10.52% (over-protective)
   Break-even não é edge forte, mas quebra o padrão histórico. Só BEAR
   regime é profitable — consistente com último ciclo crypto 2024-2026
   sendo bearish.
5. **DE SHAW 4h e layer1 não funcionam**: 4h dá 1/6 PASS (pior que
   1h). Layer1/majors não produzem pares cointegrados suficientes com
   pvalue=0.05 (0 pares). Bluechip é o único basket viável.
6. **GRAHAM confirmed dead**: testado em 1h (sem edge), 4h bluechip
   (ROI -144%), 4h layer1 (ALGO só dreno -$2485), 1d (too_few_bars
   com hawkes warmup=2000 vs 1460 bars disponíveis). Baseline sem
   Hawkes gate em 4h dá ROI -1380%. Arquivado oficialmente.
7. **JUMP tem cap de capacity**: 28503 vetos "vol_extreme" em 730d.
   Engine filtra agressivamente — só opera em regimes "normais".
   Isso contribui pra DD baixo mas limita escalabilidade.

## Estado do Sistema
- Branch: **`feat/phi-engine`** (Codex ainda em paralelo)
- Smoke test: 176/176 pass
- Testes KEPOS+GRAHAM: 56/56 pass
- Arvore: meus arquivos prontos pra commit
- Backlog restante:
  1. Walk-forward 6/6 formal KEPOS layer1 sustained=10 (analysis/walkforward.py)
  2. Decidir se JUMP vai pra FROZEN_ENGINES (Sharpe 4.56 em 730d, 6/6 audit)
  3. DE SHAW com amostra maior (1095d+) pra confirmar break-even
     z=3.0/pvalue=0.05 antes de declarar stable
  4. GRAHAM: arquivar formalmente ou redesign com novo signal base

## Arquivos Modificados (nesta sessão — só meus)
- `engines/kepos.py` — docstring layer1 finding (+13/-5)
- `engines/graham.py` — docstring 4h layer1 + 1d + arquivamento (+10/-3)
- `engines/deshaw.py` — 7 CLI overrides + docstring break-even (+45/-0)
- `docs/sessions/2026-04-16_2201.md` — este log
- `docs/days/2026-04-16.md` — daily log (atualizar)

## Notas para o Joao
1. **JUMP está pronto pra FROZEN_ENGINES**. Sharpe 4.56 em 730d com 6/6
   PASS é sólido. Único engine que passa todos os filtros de overfit
   em amostra extendida. Recomendo mover pra lista FROZEN quando quiser.
2. **KEPOS recomendado: layer1, sustained=10, k_sigma=1.0, eta_crit=0.75,
   invert=True**. Sharpe 1.50 em 2 anos, DD 10.75%. SOL 51%
   concentration persiste — monitorar. Precisa walk-forward 6/6 formal
   antes de live.
3. **DE SHAW: primeira vez em 18+ iters que vira positivo**. A config
   vencedora é:
   `--z-entry 3.0 --z-exit 0.5 --pvalue 0.05 --hl-max 300`
   ROI +3.09% em 730d é break-even, não edge. Mas é sinal de que o
   engine pode ter valor em amostra maior / tuning fino. Precisa
   testar em 1095d+ pra confirmar. Não usar em live ainda.
4. **GRAHAM arquivado de vez**. 4 tentativas (1h, 4h bluechip, 4h layer1,
   1d) todas falharam. Baseline sem gate catastrófico. O framework
   Hawkes + EMA cross não dá edge em crypto candle. Se quiser revisitar,
   precisa novo signal base — não salvar com tuning de params.
5. **Codex segue em feat/phi-engine**. Novo engine medallion.py + tools
   apareceram. Não mexi. Meu trabalho tá isolado em kepos/graham/deshaw.
6. **Novos CLI do DE SHAW são úteis pra Codex/you futuro**: qualquer
   sweep de Z thresholds ou pvalue pode ser feito sem mexer em
   config.params (core protegido).
