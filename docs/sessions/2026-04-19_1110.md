# Session Log — 2026-04-19 11:10

## Resumo

Sessão longa de **varredura + organização + hardening do cockpit shadow**.
Limpou 29 ghost dirs, 5 temp batteries, 3 worktrees mergeados. Cockpit
shadow ficou institucional: notional em vez de size crua, signal detail
inline (sem popup Toplevel), sidebar mais enxuta (18/82 + width 180→140),
LAST SIG age metric (filtra primed), skip-no-op re-render. Investigou
Telegram "não notificou" — achou que era silent-fail por design: código
antigo só logava falhas. Agora `_tg_send` loga cada send. Endpoints
novos na cockpit API: `/log`, `/telegram-diag`, `/shadow/start`. Shadow
runner ganhou `primed` flag + `last_novel_at` no heartbeat + cálculo
full de métricas (Sharpe/Sortino/WR/PF/MaxDD/ROI) no stop. VPS redeployed
end-to-end, novo shadow run `2026-04-19_1405` ativo via botão START no
cockpit (primeiro start via API). Suite 1266 passed.

## Commits

| Hash    | Mensagem                                                                 | Arquivos |
|---------|--------------------------------------------------------------------------|----------|
| d854221 | fix(cockpit): stream spawn off UI thread + pending flag                  | launcher.py, tests/integration/test_launcher_main_menu.py |
| 92cd2d8 | docs(specs): Lane 2b HMM speedup — pre-register H7/H8/H9 + invariantes   | docs/superpowers/specs/... (Codex) |
| ddc3b86 | docs(specs): Lane 2b v2 — hardening pos-review Joao                      | docs/superpowers/specs/... (Codex) |
| b303edf | docs(plans): Lane 2b HMM speedup implementation plan                     | docs/superpowers/plans/... (Codex) |
| a8e21c6 | feat(cockpit): shadow profissional — notional, inline detail, sidebar enxuta, LAST SIG age, skip-no-op | launcher_support/engines_live_view.py, engines_sidebar.py, signal_detail_popup.py + tests |
| 6159e89 | feat(cockpit): /log tail + /telegram-diag endpoints + log each tg send   | tools/cockpit_api.py, tools/maintenance/millennium_shadow.py + tests |
| 2ee0fd8 | feat(shadow): primed flag + last_novel_at + full metrics on stop + /v1/shadow/start | millennium_shadow.py, cockpit_api.py, engines_live_view.py, engines_sidebar.py + tests |

Worktrees ativos com commits próprios (não em feat/phi-engine):
- `feat/quantum-engine` → `426b1fb` feat(quantum): experimental WIP
- `feat/chronos-entropy` → `f4f1ade` feat(chronos): HMM diagnostics aditivo

## Mudanças Críticas

Nenhuma mudança em lógica de trading. Zero linhas em `core/indicators.py`,
`core/signals.py`, `core/portfolio.py`, `config/params.py`.

Mudanças observáveis no shadow runner (`tools/maintenance/millennium_shadow.py`)
são puramente de contabilidade (primed flag, novel_since_prime counter,
last_novel_at timestamp) e relatório (summary.json com métricas backtest-style
no shutdown). Nenhuma alteração em `_collect_operational_trades` ou
nas regras de geração de sinal — engines continuam rodando idêntico.

## Achados

- **TIME-BOMB defused**: systemd unit `millennium_shadow.service` apontava
  pra `tools/millennium_shadow.py`, mas Lane 1 moveu pra
  `tools/maintenance/millennium_shadow.py`. Service rodando em memória
  (PID 597417 da run antiga), mas próximo restart falharia. Path
  corrigido via `sed -i` no unit + `daemon-reload`.
- **Telegram nunca falhou** — log tinha 0 menções a "telegram" porque
  código antigo só logava WARNINGS em falha. Todos os sends funcionavam
  silencioso. HTTP 200 no ping manual confirmou. User provavelmente
  recebeu `MILLENNIUM shadow START` de 18/04 02:29 UTC mas não notou
  no meio do chat.
- **LAST SIG ilusão de vivacidade**: mostrava "2m" porque o ÚLTIMO trade
  no universo backtest 180d tinha timestamp de 2min atrás, não porque
  shadow detectou signal ao vivo. Bug cosmético corrigido com
  `primed` flag + `last_novel_at` em heartbeat.
- **Position size "absurda"**: size `27633` era qty de TOKENS
  (ARBUSDT) = $5334 notional, $51 de risk real. Parecia absurdo por
  falta de coluna NOTIONAL. JUMP OPUSDT size 73782 → $9.1k notional (91%
  de conta $10k), Kelly matematicamente correto mas alto — visível agora
  na LAST SIGNALS pra operador julgar.
- **`data/runs/citadel_*` NÃO é legacy**: é o path canônico de
  CITADEL+multistrategy (report_html, oos_revalidate, reconcile_runs
  dependem). Assimetria intencional com outros engines.
- **Website reorg não autorizada**: Codex pivotou
  `server/website/src/App.jsx` de dashboard pra marketing site sem pedido
  (pages/HomePage+FundPage+SoftwarePage). Descartado.

## Estado do Sistema

- **Suite completa:** 1266 passed, 7 skipped
- **Shadow VPS:** run `2026-04-19_1405` active, 1 tick ok, 626 primed,
  Telegram START ping enviado, heartbeat com `novel_since_prime=0`,
  `last_novel_at=null`
- **Cockpit API:** endpoints novos deployed (`/log`, `/telegram-diag`,
  `/shadow/start`)
- **Worktrees ativos:**
  - `.worktrees/quantum-engine` (WIP committed, grid não rodado)
  - `.worktrees/chronos-entropy` (aditivo committed, pronto pra PR)
  - `.worktrees/safety-tests` (10 contract tests parkados)
- **Backlog top:**
  1. Flicker ainda pode aparecer em transições de estado (skip-noop
     pula só quando content é IDÊNTICO)
  2. Phase 4 Lane 1 (launcher split 13k → módulos, 2-3h GUI manual)
  3. Abrir PR de safety-tests (herdado)
  4. 5 engines sem teste (jump, renaissance, twosigma, aqr, medallion)
  5. VPS settings UI dentro do launcher (editar host/port/tokens sem sair)
- **Próximo passo sugerido:** observar o novo shadow run. Primeiro novel
  signal AO VIVO vai:
  1. Disparar Telegram (visível no chat)
  2. Logar `telegram sent: <b>SHADOW · ENGINE</b>...`
  3. Mover `last_novel_at` de null pra timestamp atual
  4. LAST SIG no cockpit muda de '—' pra 'now'/'1m' com cor verde
  5. `novel_since_prime` incrementa

## Arquivos Modificados

**Direto no feat/phi-engine (via sessão):**
- `launcher.py` — +169/-55 (cockpit stream spawn off UI thread)
- `launcher_support/engines_live_view.py` — +180/-45 (skip-noop refresh,
  inline signal, START button, toast helper)
- `launcher_support/engines_sidebar.py` — +100/-15 (notional, LAST SIG
  age, sidebar width 140, inline detail param)
- `launcher_support/signal_detail_popup.py` — +110/-50 (`render_inline`
  extracted, notional + risk rows)
- `tools/cockpit_api.py` — +117 (`/log`, `/telegram-diag`,
  `/shadow/start` endpoints)
- `tools/maintenance/millennium_shadow.py` — +210/-13 (primed flag,
  last_novel_at, novel_since_prime, `_compute_trade_metrics`,
  `_write_run_summary`, tg_send per-send log)
- `tests/integration/test_launcher_main_menu.py` — +62 (cockpit stream
  off-ui test)
- `tests/test_engines_sidebar.py` — +40 (LAST SIG tests + notional)
- `tests/test_signal_detail_popup.py` — +3 (notional/risk rows)
- `tests/test_cockpit_api.py` — +100 (log tail + telegram-diag + shadow
  start tests)
- `tests/test_shadow_metrics.py` — NEW +62 (5 unit tests)

**Limpezas (sem commit — operacional):**
- 29 ghost dirs em `data/millennium_shadow/` removidos (só 3 tinham dados)
- `data/.cockpit_cache/tunnel.log` zerado (erros stale Codex SSH)
- 5 temp battery dirs removidos (`_bridgewater_battery{,_2003}`,
  `_bridgewater_compare`, `_deshaw_battery`, `_weak_salvage`)
- 3 worktrees mergeados removidos: `lane-1-organizacao`, `cockpit-api`,
  `launcher-tunnel`

**VPS-side (via SSH root@37.60.254.151):**
- `/etc/systemd/system/millennium_shadow.service` — path corrigido
  (`tools/maintenance/...`)
- `aurum_cockpit_api.service` restartado 2× (primeira pra endpoints
  `/log` + `/telegram-diag`; segunda pra `/shadow/start`)
- `millennium_shadow.service` restartado 2× (nova run `2026-04-19_1354`
  depois `2026-04-19_1405`)

## Notas para o Joao

- **Telegram foi entregue 3x hoje:** diagnostic ping manual, STOP ping do
  run 2026-04-19_1354, START ping do run 2026-04-19_1405. Se não
  recebeu, verifica se `@aurumfinance_bot` não tá silenciado/arquivado.
- **Próximo shadow run preservado com novo código** — quando você parar
  pelo cockpit, `data/millennium_shadow/2026-04-19_1405/reports/summary.
  json` vai ter Sharpe/WR/PF/MaxDD/ROI/per-engine. Igual backtest.
- **BOTÃO START SHADOW no cockpit** — se shadow parar, no empty state
  aparece botão verde que chama `/v1/shadow/start` (admin-scoped).
  Toast com feedback. Sem SSH.
- **LAST SIG metric vai mostrar '—'** até um signal AO VIVO aparecer.
  Isso é CORRETO agora (filtro de primed). Primeiro signal → 'now' → '1m'
  → '2m' com cor verde.
- **`_tg_send` agora loga cada send** (`telegram sent: <first-line>`).
  Consultar histórico:
  ```
  curl -H "Bearer $READ" "http://127.0.0.1:8787/v1/runs/$RUN/log?grep=telegram&tail=50"
  ```
- **Local launcher tunnel caiu** durante os 2 restarts do cockpit API.
  Watchdog pode ou não reconectar — se o cockpit não mostrar dados
  frescos, relança o launcher.
- **Worktrees quantum + chronos-entropy**: ambos com commits isolados em
  branches próprias. Pendente de decisão: rodar grid quantum completo
  (60-90min) OU arquivar; chronos é aditivo e seguro pra merge se quiser
  integrar em engines research.
