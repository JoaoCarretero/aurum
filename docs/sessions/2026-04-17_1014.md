# Session Log — 2026-04-17 10:14

## Resumo
Audit estrutural full-stack em 3 ondas: segurança/core/qualidade/engines (wave 1), BRIDGEWATER root-cause + perf/concurrency/resilience (wave 2), arquitetura + estado/config/paridade backtest↔live (wave 3). 9 commits meus no branch `feat/phi-engine`, ~20 itens corrigidos, test suite estável.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 35a3642 | docs(audit): wave 3 addendum — structural audit | docs/audits/2026-04-17_full_audit.md |
| 250a69e | docs(audit): wave 2 addenda — BRIDGEWATER + second-pass | docs/audits/2026-04-17_full_audit.md |
| e5f0b3f | fix(live,telegram): race, order timeout, WS stacktrace, telegram visibility | engines/live.py, bot/telegram.py |
| 77e4088 | fix(bridgewater): scale sentiment limits + pre-series fill | engines/bridgewater.py |
| ae6dbde | docs(audit): full system audit + CLAUDE.md engines table | CLAUDE.md, docs/audits/2026-04-17_full_audit.md |
| c1ab62d | refactor(core): remove v3.7 dead code + label_trade TODO | core/portfolio.py, core/signals.py, core/__init__.py, engines/citadel.py, engines/millennium.py, docs/contracts/sacred-logic-contract.md |
| 1085c32 | fix(security): gitignore, risk gates, telegram allowlist, live REST URL, audit chain | .gitignore, config/risk_gates.json, bot/telegram.py, core/audit_trail.py, core/connections.py, core/exchange_api.py, engines/live.py |
| (+ citadel UTC — foi commitado pelo Codex junto com outras mudanças) | | engines/citadel.py |

## Mudanças Críticas

**ATENÇÃO — core de trading:**
- `core/portfolio.py`: removidas funções órfãs `_omega_risk_mult` e `_global_risk_mult` (dead desde v3.7)
- `core/signals.py::label_trade`: adicionada nota sobre off-by-one no range-start (loop começa em `entry_idx` mas entrada real é `open[idx+1]`) — **NÃO alterei comportamento** (shiftaria todos backtests calibrados)
- `config/params.py`: removidos `OMEGA_RISK_TABLE` e `CHOP_SIZE_MULT` (dead code)

**ATENÇÃO — live trading:**
- `engines/live.py:place_order` — agora roda em executor com `asyncio.wait_for(10s)` + 2 retries. Elimina risco de order hanging.
- `engines/live.py:_reconciliation_loop` — snapshot de `self.positions` via `list()` pra eliminar race condition com `_open_position`.
- `engines/live.py` — URL Binance hardcoded `https://fapi.binance.com` + override explícito de FUTURES_URL.

**ATENÇÃO — cálculo de sinal BRIDGEWATER:**
- `engines/bridgewater.py`: limits do sentiment escalados com `window_days`. Sharpe OOS 11.04 era espúrio (artefato de truncamento quando `limit=100` cobria só 33d numa janela de 360d). **Precisa re-rodar OOS BEAR** — Sharpe real provavelmente cai drasticamente.

## Achados

### BRIDGEWATER — bug real
- `fetch_funding_rate(limit=100)` + `_align_series_to_candles` propagando `values[0]` nas barras pré-série fabricou sinal determinístico em ~90% das barras de qualquer janela OOS longa.
- Root cause documentado em `docs/audits/2026-04-17_full_audit.md §5`.

### Structural — CRITICAL pra fix pós-sessão
- **`AURUM.spec` bundles `config/keys.json` em plaintext no `.exe`** — fix local aplicado (exclude via glob) mas `AURUM.spec` é gitignored, não entrou no commit. Copiar manualmente pro VPS build.

### Structural — itens abertos
- 🟠 `analysis/live_replay_test.py:34` viola camada importando `engines.live.LiveEngine`
- 🟠 Sem reconciliation automático de entry_price no startup do live
- 🟡 ~200 linhas duplicadas entre 5 engines (graham, kepos, live, medallion, phi) — candidato a `core/engine_utils.py`
- 🟡 Pipeline `indicators → swing_structure → omega` duplicado em 8 engines → `core.data.apply_core_features()`
- 🟡 Cache `data/.cache/*.pkl.gz` sem TTL cleanup
- 🟡 9 env vars `AURUM_*` sem registrador central
- 🟡 Funding discreto (backtest) vs contínuo (live) — diverge em trades ≥8h
- 🟡 `core/engine_base.py::EngineRuntime` nunca adotado — ou migra 5 engines ou mantém como interface futura (tem contract test, não é dead)

### Errata do audit original
- Agente flagou `pickle.load` em `core/cache.py` como HIGH — **falso positivo**. Cache usa JSON dentro de gzip (extensão `.pkl.gz` é legacy). Zero pickle no codebase.
- Agente flagou `vol_regime` ignorado em `position_size` como HIGH — intencional por v3.7 (session log 2026-04-12 registrou drop de 8→3 fatores). Funções órfãs já removidas.

## Estado do Sistema
- **pytest:** 1011 passed / 0 failed (aumentou de 1007 — Codex adicionou tests)
- **smoke test:** 178/178 passed
- **Branch `feat/phi-engine`:** 68 commits ahead de main
- **Backlog restante:** ver "Structural — itens abertos" acima
- **Próximo passo sugerido:** rodar OOS BEAR pro BRIDGEWATER após o fix pra confirmar Sharpe real (provavelmente cai pra NO_EDGE).

## Arquivos Modificados

**Meus commits:**
- `.gitignore` (+2) — keys.json.enc, connections.json
- `config/risk_gates.json` (tighten testnet/arbitrage_testnet)
- `bot/telegram.py` (allowlist user_ids, _post error visibility)
- `core/audit_trail.py` (hash_chain=True default)
- `core/connections.py` (UTC last_ping)
- `core/exchange_api.py` (differentiated error logs)
- `engines/live.py` (Binance URL hardcode, allowlist chain, race fix, order timeout+retry, WS stacktrace)
- `core/portfolio.py` (-12 linhas dead code)
- `core/signals.py` (+10 comment label_trade off-by-one)
- `core/__init__.py` (-2 exports)
- `engines/citadel.py` (-imports mortos, UTC no setup_run)
- `engines/millennium.py` (+11 docstring exceção arquitetural)
- `docs/contracts/sacred-logic-contract.md` (atualizado)
- `CLAUDE.md` (tabela engines com Status OOS + phi/kepos/medallion/graham)
- `engines/bridgewater.py` (_sentiment_limits helper, default fill fix)
- `docs/audits/2026-04-17_full_audit.md` (NOVO — 8 seções)

**Fora do commit (AURUM.spec gitignored):**
- `AURUM.spec` (exclude keys.json do bundle — fix CRITICAL mas não versionado)

**Uncommitted do Codex/WIP seu:**
- ~18 arquivos modificados + 6 untracked (api, core/cache, core/data, launcher, tests/*, tools/*)

## Notas para o Joao

### Urgente
1. **`AURUM.spec` fix local aplicado mas não commitado** (gitignored). Copie manualmente pro build do VPS antes de distribuir qualquer `.exe`:
   ```python
   datas=[
       (p, 'config')
       for p in __import__('glob').glob('.../config/*')
       if not p.endswith(('keys.json', 'keys.json.enc'))
   ],
   ```

2. **BRIDGEWATER precisa re-rodar OOS BEAR** pra medir Sharpe real sem o artefato de truncamento. Se cair abaixo de 1.5, arquivar junto com DE SHAW/MEDALLION/KEPOS.

### Decisões pendentes
- **`label_trade` off-by-one** — documentei como TODO mas não mudei. Fix shiftaria todos backtests calibrados de CITADEL/JUMP/RENAISSANCE/PHI. Você decide se/quando rodar walk-forward novo.
- **11 engines sem teste dedicado** — só phi/kepos/graham têm. Decidir se prioriza.
- **Refactor de duplicação** (5 engines → `core/engine_utils.py`) — ~10h trabalho, sem risco mas grande diff.

### Ativo no branch
- 68 commits ahead de main (seus + meus + Codex). Agente audit sugeriu split em 3 PRs pra merge cirúrgico: Bloco 0 OOS, PHI engine, cockpit/launcher.
