# Session Log — 2026-04-20 17:22

## Resumo

Sessão longa de **paper backtest-parity + cockpit stability + UX** que
começou com "voltar de onde parei" (changes uncommitted após crash da
sessão anterior) e virou em cadeia de fixes: test paper trailing stop,
cockpit API auto-zombie-detect, launcher cleanup em screen-switch,
colapsáveis de buckets no cockpit, cancel de timers em mode-switch
(shadow↔paper). Fechou com design doc pra multi-instance runs (6-phase
plan, decisões fechadas, código na próxima sessão).

## Commits

| Hash    | Mensagem                                                                      | Arquivos |
|---------|-------------------------------------------------------------------------------|----------|
| 4f59d7e | docs(specs): multi-instance runs design — 6 fases pra local + DB + VPS        | docs/superpowers/specs/ |
| 1184520 | fix(cockpit): cancel prior mode refresh timers on mode switch                 | launcher_support/engines_live_view.py |
| 9d7c6e9 | feat(cockpit): collapsible READY LIVE / RESEARCH / EXPERIMENTAL buckets       | launcher_support/engines_live_view.py |
| df391b0 | fix(launcher): tear down ENGINES LIVE handle on screen switch                 | launcher.py |
| 0f1ad47 | fix(cockpit): derive effective status to unmask zombie runs                   | tools/cockpit_api.py, tests/test_cockpit_api.py |
| 18fe497 | feat(paper): backtest-parity — BE + trailing + liquidation + canonical direction | tools/operations/paper_*.py, tests/test_paper_position_manager.py, tests/integration/test_paper_runner_tick.py |
| 688f5a9 | feat(paper): backtest-parity — mensagem errada, renames de meanrev (race com Codex) | engines/_archive/, tests/_archive/, tools/_archive/ |

7 commits nesta sessão (6 meus + 1 que ficou com mensagem errada por race
com Codex que mexia em paralelo).

## Mudanças Críticas

**Paper runner agora espelha `label_trade` do backtest.** Antes:
stop/target naive. Agora: liquidation → BE → trailing → stop → target,
mesma ordem de prioridade. Paper que abre um pod com Kelly vai
fechar com vocabulário backtest (`trailing`, `breakeven`, `stop_initial`,
`target`, `liquidation`) em vez do genérico `stop`/`target`.

**Impacto:** nenhum backtest calibrado foi tocado. Zero linhas em
`core/indicators.py`, `core/signals.py`, `core/portfolio.py`,
`config/params.py`. Mudança isolada em `tools/operations/paper_*`.

Testes expandidos (15 casos cobrindo simple + BE+trail+liq + funding).

## Achados

- **Commit 688f5a9 ficou com mensagem errada.** Codex rodou `git mv` dos
  meanrev → `_archive/` entre meu `git add` e `git commit`, consumindo
  meus arquivos staged. Recriei o commit certo (18fe497) com as mesmas
  mudanças + nota sobre o incidente. 688f5a9 permanece no history com
  mensagem incorreta mas conteúdo (renames) OK.
- **Paper VPS 2026-04-20_1139 virou zombie** (killed por systemd às
  13:28 por bug `_fetch_new_bars got 'limit' kwarg`). Heartbeat ficou
  congelado em `status=running` até eu editar a mão. O fix permanente
  (0f1ad47) evita recorrência: `_effective_status` no cockpit API
  reporta `stopped` quando `last_tick_at` > `max(tick_sec*3, 600s)`.
- **Paper rejeitou 25 novel signals com `stale_bar`** em 5h de run.
  Comportamento CORRETO: scan do MILLENNIUM emite trades retroativos
  (late-crystallizing) com timestamps de 2026-01 a 2026-04-18; `_is_live_signal`
  filtra. Precisa novel com open_ts do bar corrente pra passar — não
  aconteceu em 5h (estratégias sem setup fresh no mercado atual).
- **Launcher trava ao trocar shadow↔demo↔paper** — timers `after()`
  do modo anterior continuavam firing, stackando destroy+rebuild.
  Fix 1184520 cancela timers (shadow_refresh_aid, paper_refresh_aid,
  shadow_after_id) antes de trocar modo.
- **Ghost dirs locais acumulando** em `data/millennium_shadow/` (29 hoje).
  Launcher cria run dir em import de `tools/maintenance/millennium_shadow.py`
  mesmo sem rodar — alguma lógica local tenta spawn e falha, deixa o
  dir vazio. Não investiguei a fundo; dirs sem heartbeat são skippados
  pelo `_find_latest_shadow_run` então não quebram UI, só sujam FS.
  **Não consegui limpar** (launcher segura file handles abertos em
  `logs/`). Próxima sessão: close launcher, rodar cleanup script.

## Estado do Sistema

- **Suite:** `smoke_test.py --quiet` = 178/178. Full suite passa menos
  2 chronos tests (stream outro Claude) + 1 splash test
  (launcher_support.screens.splash ainda não criado pelo outro Claude).
- **VPS (37.60.254.151) — pronto pra 24h run:**
  - `millennium_paper.service`: active, run 2026-04-20_1225, **20 ticks**,
    primed, 25 novel (all stale_bar), equity $10k, DD 0%, KS NORMAL.
    Uptime 4h57m.
  - `millennium_shadow.service`: active, run 2026-04-19_1937, **87 ticks**,
    24 novel pós-prime. Uptime 21h45m.
  - `aurum_cockpit_api.service`: active. Novo código deployado (zombie detect).
- **Launcher local:** tunnel próprio funcionando (ssh spawn do
  TunnelManager) após eu matar meus tunnels residuais. Porta 8787
  em TIME_WAIT (liberando).
- **keys.json:** OK, intacto. Verify-guard passou. Pre-commit hook
  continua armado.
- **Backlog top:**
  1. **Multi-instance runs Fase 1** — runners aceitam `--label`.
     Spec pronta (`docs/superpowers/specs/2026-04-20-multi-instance-runs-design.md`).
     Joao aprovou decisões-chave (local-first, label nominal, coluna DB).
  2. **Investigar fonte dos ghost dirs** — 29 hoje em `millennium_shadow/`,
     alguns com file handles abertos pelo launcher. Causa provavelmente
     em background thread que importa `tools/maintenance/millennium_shadow.py`.
  3. **Stream 2 Chronos** — outro Claude no ar com HMM integration em
     BRIDGEWATER/DE SHAW/KEPOS/MEDALLION. 2 contract tests falhando
     nesses engines (esperado — middle of integration).
  4. **ScreenManager refactor** — outro Claude fez 14 commits (design →
     implementação parcial) em `launcher_support/screens/`. `splash.py`
     ainda falta criar. Não mexi.

## Arquivos Modificados Nesta Sessão

### Commits meus
- `tools/operations/paper_executor.py` — `canonical_direction()`,
  liq thresholds via `core.signals._liq_prices`, novos campos Position
  (cur_stop, be_done, trail_done, liq_long/short)
- `tools/operations/paper_position_manager.py` — streaming `_walk_bars`
  mirror do label_trade
- `tests/test_paper_position_manager.py` — 15 testes (BE/trail/liq/funding)
- `tests/integration/test_paper_runner_tick.py` — target tight
- `tools/cockpit_api.py` — `_effective_status()` + uso em `_summarize_run`
- `tests/test_cockpit_api.py` — 2 testes novos (zombie + fresh)
- `launcher.py` — cleanup em `_clr()` do `_engines_live_handle`
- `launcher_support/engines_live_view.py` — collapsible buckets +
  `_cancel_refresh_timers()` em `set_mode()`
- `docs/superpowers/specs/2026-04-20-multi-instance-runs-design.md` — NEW

### VPS (via scp + systemctl)
- `/srv/aurum.finance/tools/cockpit_api.py` — redeployed (zombie detect)
- `/srv/aurum.finance/data/millennium_paper/2026-04-20_1139/state/heartbeat.json`
  — manual fix: `status: running` → `status: stopped, stop_reason: killed_by_systemd`

### Uncommitted (não mexi — outro agente)
- `engines/bridgewater.py | deshaw.py | kepos.py | medallion.py` —
  Chronos HMM integration em progresso
- `launcher.py` — `@timed_legacy_switch` decorators sendo adicionados
  pelo outro Claude (ScreenManager integration)
- `launcher_support/screens/splash.py` + `tests/launcher/test_splash_screen.py`
  — outro Claude, ainda incompletos

## Notas para o Joao

**VPS rodando limpo pra 24h de monitoramento.** Paper e shadow
ambos healthy, primed, last_tick recente. Na volta:
- `curl /v1/runs` (via launcher ou SSH tunnel) vai mostrar ticks_ok
  ~20+96=116 em paper (se tick=15min por 24h) e ~183 em shadow.
- Se paper abrir algum signal fresh (não stale_bar), Telegram
  notifica e equity muda.
- Se KS triggers (fast_halt em -$100 = -1%), paper para sozinho.
  Verificar `/v1/runs/<id>/account` pra ver `ks_state` = HALTED.

**Próxima sessão (quando a budget de usage reabrir):**
1. Multi-instance Fase 1 (30-45min) — spec já fechada.
2. Investigar ghost dirs em `data/millennium_shadow/`.
3. Se outro Claude terminou ScreenManager, trocar `_strategies`
   etc pra usar o novo sistema (o meu fix de `_clr()` vira redundante
   mas não estorva).

**Comandos úteis pra conferir VPS amanhã:**
```bash
# Via SSH direto
ssh -i C:/Users/Joao/.ssh/id_ed25519 root@37.60.254.151 \
    'systemctl is-active millennium_paper millennium_shadow aurum_cockpit_api'

# Via tunnel + cockpit
ssh -N -f -i C:/Users/Joao/.ssh/id_ed25519 -L 8787:127.0.0.1:8787 root@37.60.254.151
curl -s -H "Authorization: Bearer $(python -c 'from core.risk.key_store import load_runtime_keys; print(load_runtime_keys()["cockpit_api"]["read_token"])')" \
  http://127.0.0.1:8787/v1/runs | python -m json.tool
```

**Commit 688f5a9 com mensagem errada** — não vale mexer (history
dirty), deixa assim.

**Multi-instance spec em `docs/superpowers/specs/2026-04-20-multi-instance-runs-design.md`**
— 217 linhas, tudo decidido, só falta codar. Fase 1 são ~20 linhas
em 2 arquivos (millennium_paper.py + millennium_shadow.py) + 1 test file.
