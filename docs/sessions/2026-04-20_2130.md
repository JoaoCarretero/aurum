# Session Log — 2026-04-20 21:30

## Resumo

Fix completo do fluxo shadow+paper na VPS: (1) **root cause do paper nunca abrir** foi scan skipando os últimos 50h de bars (engine loop para em `len-MAX_HOLD-2` pra label WIN/LOSS em backtest); (2) multi-instance Fase 1-3 implementada ponta-a-ponta (runners aceitam `--label`, cockpit API expõe, launcher picker); (3) série de correções UX (stop button responsivo, sidebar collapse lateral, botão `+ NEW INSTANCE`, RUNNING count integrado com VPS, tunnel auto-boot com badge de erro actionable).

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 24b6a7f | feat(multi-instance): paper+shadow aceitam --label pra rodar N runs em paralelo | tools/operations/run_id.py + millennium_paper/shadow + core/shadow_contract + tools/cockpit_api + launcher_support/cockpit_client + engines_live_view + 4 test files |
| 9c84822 | feat(engines): live_mode pra scan produzir signals no tail (paper nunca abria) | engines/citadel.py + jump.py + core/harmonics.py + engines/millennium.py + millennium_paper.py + millennium_shadow.py + test fix |
| 9dfc644 | fix(paper): responsive stop — chunked 2s sleep mirror shadow | tools/operations/millennium_paper.py |
| 00ad375 | feat(launcher): collapse lateral da sidebar ENGINES | launcher_support/engines_sidebar.py + engines_live_view.py |
| a4af058 | feat(launcher): botao + NEW INSTANCE + RUNNING count reflete VPS real | launcher_support/engines_sidebar.py + engines_live_view.py |
| 4ea39bc | fix(launcher): detect placeholder vps_ssh config + actionable UI badge | launcher.py + launcher_support/tunnel_registry.py + engines_live_view.py |
| d06fc1f | perf(engines): live_mode scan so ultimos 4 bars (~60min) em vez de 50h | engines/citadel.py + jump.py + core/harmonics.py |

Codex (paralelo): 7f5160a perf DataCenterScreen cache · 49d0981 specs live-runs-refactor · f7df15a plans · 64ff2c6 migration 001 live_runs · aeae855 refactor migration clarity · 959aea9 core.ops.db_live_runs API.

## Mudanças Críticas

**ATENÇÃO — afeta lógica de trading:**

1. **`engines/citadel.scan_symbol`, `engines/jump.scan_mercurio`, `core/harmonics.scan_hermes` ganharam `live_mode=False` + `live_tail_bars=4` params.** Default (live_mode=False) preserva backtest bit-idêntico. Com live_mode=True, loop scans `[len-live_tail_bars-1, len-1]`, skipa `label_trade`, emite `result="LIVE"`. Decision gates (vetoes, decide_direction, score_omega, calc_levels) preservadas **exatamente** — live match com calibração. **Gap documentado:** streak_cooldown, dd_scale, portfolio_allows usam state-evoluído em backtest; em live rodam com defaults (account=ACCOUNT_SIZE, losses=0, open_pos=[]). Runtime (paper_executor + KS gate + MAX_OPEN_POSITIONS) cobrem parcialmente. Consequência: live pode ser ligeiramente mais agressivo que backtest em cenários de DD/correlação acumulados.

2. **`engines/millennium._collect_live_signals`** (novo) chama os 3 engines com `live_mode=True`. Paper e shadow runners trocaram `_collect_operational_trades` por essa versão. Signal dedup + stale_bar filter (`_is_live_signal` em `millennium_signal_gate.py`) continuam ativos — na prática sinais do tail tight (60min) já caem dentro do filtro.

**Não-trading (infra / UX / runner):**

- Paper runner: sleep chunked 2s (antes: sleep(tick_sec) bloqueante — STOP demorava 15min).
- Multi-instance: RUN_ID agora seconds-precise (`YYYY-MM-DD_HHMMSS[_<label>]`) via `tools/operations/run_id.py`. Manifest/heartbeat/summary carregam campo `label`.
- Manifest model ficou `extra="allow"` pra compat com novos campos futuros.

## Achados

- **Shadow pré-fix emitiu 664 Telegrams fake em 26h:** 0 com signal_ts do último 30min, apenas 1 do último 24h (SAND 04:30, 13h velho quando observado). Todos os "SHADOW · JUMP BEARISH BTCUSDT" eram signals de Jan 2026 flipando por state-shift de indicador. Fix resolve na raiz.
- **Tunnel auto-boot falhava silencioso** com placeholders do wipe 2026-04-19: `cole_aqui_o_host` em config/keys.json gerava 27 linhas de `Could not resolve hostname` no tunnel.log. UI mostrava "TUNNEL —" sem contexto.
- **Scan é determinístico dentro do mesmo processo** (635 == 635 em 2 scans consecutivos). A variação observada em shadow (664 novels acumulados em 26h) vem do rolling 90d window que avança a cada tick, surface ocasional de bars antigas com state-shift.
- **Live sem signal em mercado quieto é CORRETO, não bug.** Scan tight (4 bars) não gera signal se conditions não firarem. Paper open=0 = engine honesto.

## Estado do Sistema

- **Smoke test:** 178/178
- **Contracts:** 835/835 (jump_contracts tem 3 failures pré-existentes, `'module' object is not callable` no `indicators(df)` do fixture — não tocado hoje)
- **Integration + tools + contracts:** 1044 passed, 7 skipped
- **VPS serviços:** paper active (171MB), shadow active (161MB), cockpit_api active (47MB). 0 errors em 10min de logs.
- **Cockpit API:** 10 runs total, 3 running (1 paper real + 1 shadow real + 1 paper zombie que vira stopped em ~40min via _effective_status threshold).
- **Prime count after tight:** 1 signal (antes era 21) — reflete 60min de tail real.
- **Backlog restante:**
  1. Codex' live_runs refactor (migration 001 aplicar em aurum.db, Fase 2+3 UI)
  2. Live_mode gap de gates dinâmicos (streak_cooldown / dd_scale / portfolio_allows em runtime)
  3. Tunnel auto-heal mid-run quando keys.json é consertado
  4. Shadow instance picker no launcher (hoje só paper)

**Próximo passo sugerido:** validar 24h de run live com fix deployado. Se dispara trade real, valida pipeline end-to-end. Se zero trades, investigar se market + filtros geram setup com frequência esperada.

## Arquivos Modificados

Sessão: +774 / -38

| Arquivo | Mudança |
|---|---|
| `tools/operations/run_id.py` | novo, +55 linhas (sanitize_label + build_run_id) |
| `tools/operations/millennium_paper.py` | +60 (label + chunked sleep + _collect_live_signals wire) |
| `tools/maintenance/millennium_shadow.py` | +55 (label + _collect_live_signals wire) |
| `engines/citadel.py` | +30 (live_mode + live_tail_bars branches) |
| `engines/jump.py` | +28 (live_mode + live_tail_bars branches) |
| `core/harmonics.py` | +35 (live_mode + live_tail_bars branches) |
| `engines/millennium.py` | +78 (_collect_live_signals) |
| `core/shadow_contract.py` | +3 (Manifest/RunSummary label) |
| `tools/cockpit_api.py` | +5 (label surfaced em _summarize_run) |
| `launcher_support/cockpit_client.py` | +22 (active_runs_for method) |
| `launcher_support/engines_sidebar.py` | +42 (collapsed + on_new_instance params) |
| `launcher_support/engines_live_view.py` | +150 (picker + instance dialog + VPS running integration + CFG ERR badge) |
| `launcher_support/tunnel_registry.py` | +15 (set/get_tunnel_boot_error) |
| `launcher.py` | +20 (_vps_block_has_placeholders) |
| `tests/tools/test_run_id.py` | novo, 22 tests |
| `tests/test_cockpit_api.py` | +47 (label surface tests) |
| `tests/test_cockpit_client.py` | +80 (active_runs_for tests) |
| `tests/tools/test_millennium_shadow_helpers.py` | ajuste pra patchar _collect_live_signals |

## Notas para o Joao

1. **Paper pode ficar horas sem trade.** Isso NÃO é regressão. Engine agora enxerga o bar atual; se mercado não fizer setup, zero trades. Antes o "zero trade" era bug (engine sequer via bar atual). Agora é reflexão honesta do mercado.

2. **Tunnel SSH: placeholder detectado cedo agora.** Se você algum dia ver "TUNNEL CFG ERR" vermelho no launcher, é isso — `config/keys.json` precisa de ajuste e restart do launcher. Sem esse fix, silêncio + log poluído.

3. **Botão `+` na sidebar spawna LOCAL.** Run local NÃO aparece no cockpit VPS (cockpit lê `/v1/runs` do VPS, não disco local). Pra ver, vai em Runs History (lê disco). Pra multi-instance REAL na VPS com label, precisa Fase 6 da spec (systemd template unit) — fora de escopo hoje.

4. **Codex fez trabalho paralelo no DB.** Migration 001 (live_runs table) + core.ops.db_live_runs API + docs refactor — tudo on origin mas não integrado com meu trabalho. Quando o refactor dele completar, pode ser que os runs live/paper/shadow ganhem first-class schema no aurum.db. Hoje eles gravam só em arquivos JSON no disco.

5. **Zombie detection funcionando.** Paper run 2026-04-21_005021 ainda lista como "running" no cockpit porque foi killed por systemctl restart sem escrever stopped. Vai flipar pra stopped automaticamente em ~40min via `_effective_status(hb)` threshold (max(tick_sec*3, 600s)).

6. **24h de observação recomendada.** Paper deployed com fix às 03:22 CEST. Se até 23:22 CEST amanhã o paper abrir posição real (equity ≠ $10,000), confirma pipeline end-to-end. Senão, investigar frequência de setup.
