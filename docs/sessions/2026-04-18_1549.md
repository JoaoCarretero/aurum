# Session Log — 2026-04-18 15:49

## Resumo

Avaliação estratégica do "engine QUANTUM v1.0" (spec enviada pelo Joao):
análise em 5 fases, veredito `(B) extrair só entropia + conf-gap`. Joao
escolheu `(A) implementar standalone + (B) Fase 2 chronos`. Criado
worktree `.worktrees/quantum-engine` com docs anti-overfit (hypothesis /
split / grid 27 configs), `engines/quantum.py` (~870 linhas), tool
`tools/quantum_grid_search.py`. Criado worktree `.worktrees/chronos-entropy`
com extensão aditiva em `core/chronos.py` (3 colunas + smoothing opt-in).
Grid QUANTUM interrompido em config 1/27 a pedido do Joao pra fechar a
sessão.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| — | Nenhum commit feito nesta sessão. | — |

## Mudanças Críticas

**ATENÇÃO:** Sessão adicionou código novo em duas frentes, ambas
**não-commitadas**. Core protegido (`config/params.py`, `core/indicators.py`,
`core/signals.py`, `core/portfolio.py`) **NÃO** tocado.

- **Novo engine QUANTUM v1.0** (`engines/quantum.py`, worktree
  `feat/quantum-engine`). Stage `experimental`, `live_ready=False`.
  Inspirado em spec externa mas traduzido pra mecânica fuzzy-regime
  honesta: 6 regime scores (trend / meanrev / breakout / panic / squeeze
  / chop) via heurísticas documentadas, softmax β=6 pra colapso do
  estado, entropia + conf-gap como sizing/veto. Split hardcoded train
  2020-2023 / test 2024 / holdout 2025. Grid fechado de 27 configs em
  `docs/engines/quantum/grid.md`.
- **core/chronos.py estendido (aditivo puro)** (worktree
  `feat/chronos-entropy`). 3 colunas novas: `hmm_entropy`,
  `hmm_entropy_norm`, `hmm_conf_gap`. Parâmetro `smoothing_lambda` opcional
  (default `0.0` = desligado — engines calibrados inalterados). Idempotente.
- **`config/engines.py`** (worktree quantum-engine): 1 linha nova registrando
  QUANTUM. Registry é aditivo, não é core protegido.

Nenhuma mudança em custos, thresholds CITADEL, Kelly, omega, RSI, ATR,
ou qualquer fórmula do pipeline de trading existente.

## Achados

- **Spec original do QUANTUM tinha bug de mecanismo**: normalização linear
  `p_i = s_i / Σs_j` produz H_norm ~0.9 quase sempre → 95% das barras
  vetadas em `high_entropy`. Substituído por softmax com temperatura
  `β=6.0`, coerente com a metáfora "colapso" da própria spec. Ajuste
  documentado em `docs/engines/quantum/hypothesis.md §Ajuste pós-smoke`.
- **QUANTUM com defaults em holdout 2025** (2 símbolos BNBUSDT+LINKUSDT,
  395 trades): Sharpe **−1.89**, WR 33%, PF 0.78, todos 4 setups perdem
  (momentum_long −0.18R, momentum_short −0.11R, meanrev_long −0.66R,
  meanrev_short −0.38R). Isso NÃO é o veredito final — era pra rodar grid
  em train primeiro; o número serve como baseline de "defaults falham".
- **Binance Futures BNBUSDT-PERP histórico efetivo começa 2022-03**, não
  2020. Train "2020-2023" da `split.md` é na prática ~21 meses. Pode afetar
  poder estatístico do grid — não muda a regra de parada, mas muda
  interpretação de "n_trials".
- **Extensão chronos funciona como esperado** (BNBUSDT 2000 bars 15m):
  entropy_norm μ=0.31 σ=0.27 (regime mostly confident), conf_gap μ=0.75.
  Smoothing λ=0.3 reduz vol de prob_bull em 21%, λ=0.7 em 56%, λ=0.9 em
  80% (comportamento exponencial correto).
- **Tensão protocolo vs desejo do Joao**: pedido inicial foi "refinar até
  achar params certos" — que é fishing expedition proibida pelo
  `anti_overfit_protocol.md` que ele mesmo escreveu. Puxei o freio,
  distingui "refinamento honesto" (grid fechado + DSR + regra de parada)
  de "refinamento fishing" (iterar parâmetros até achar edge). Ele
  escolheu honesto.

## Estado do Sistema

- Suite: não rodei teste unitário nesta sessão (worktrees novos,
  feat/phi-engine com uncommitted changes no CWD principal)
- Grid QUANTUM: **interrompido em config 1/27** durante fetch de
  INJUSDT. Pid 10932 killed.
- Worktrees ativos:
  - `.worktrees/quantum-engine` (branch `feat/quantum-engine`, uncommitted)
  - `.worktrees/chronos-entropy` (branch `feat/chronos-entropy`, uncommitted)
- Backlog restante (desta frente):
  - Retomar grid completo QUANTUM em train (~60-90 min background)
  - Aplicar critérios `hypothesis.md §Falsificação` no holdout
  - Decidir sobre espalhar entropia/conf-gap em engines research (Joao
    disse "esperar veredito do grid antes de espalhar")
- Próximo passo sugerido: retomar o grid numa sessão com ~2h disponíveis
  pra observar o resultado final. Sem observação, rodar parcial e ver
  melhor config até onde der.

## Arquivos Modificados

**Worktree `.worktrees/quantum-engine` (branch `feat/quantum-engine`):**

| Arquivo | Status | Linhas |
|---------|--------|-------:|
| `docs/engines/quantum/hypothesis.md` | novo | +79 |
| `docs/engines/quantum/split.md` | novo | +73 |
| `docs/engines/quantum/grid.md` | novo | +95 |
| `engines/quantum.py` | novo | +870 |
| `config/engines.py` | edit (aditivo 1 linha) | +1 |
| `tools/quantum_grid_search.py` | novo | +195 |
| `data/quantum/grid_search/2026-04-18_1502_train/` | parcial (interrompido) | — |
| `data/quantum/grid_search/2026-04-18_1500_test/` | sanity run completo (1 config) | — |
| `data/quantum_grid_train.log` | log do background killed | — |

**Worktree `.worktrees/chronos-entropy` (branch `feat/chronos-entropy`):**

| Arquivo | Status | Linhas |
|---------|--------|-------:|
| `core/chronos.py` | edit (aditivo) | +~60 |

## Notas para o Joao

- **Dois worktrees com uncommitted changes** — decide como commit:
  - Opção simples: commit atômico por worktree, merge depois separado
  - Opção conservadora: commit só Fase 2 chronos (baixo risco, alto valor
    futuro), deixa QUANTUM uncommitted até o grid dar veredito
- **Grid foi interrompido em config 1/27** — nada de estatisticamente útil
  saiu ainda. Pra retomar, no worktree `.worktrees/quantum-engine`:
  ```
  python tools/quantum_grid_search.py --window train --basket default
  ```
  Estima ~60-90 min se o `.cache/` do fetch sobreviveu (deve ter sobrevivido).
- **Defaults do QUANTUM em holdout 2025 = Sharpe −1.89**. Isso sugere que a
  hipótese v1 pode não ter edge em NENHUMA região do grid. Se o grid completo
  confirmar (nenhuma config com Sharpe DSR-adj > 0.25 acima do baseline),
  arquivo em `engines/_archive/quantum_v1.py` sem segunda chance — protocolo.
- **Extensão chronos é ortogonal** — vale commit e merge independente do
  que acontecer com QUANTUM. Aditiva, opt-in, backward compat. Nenhum
  engine consome ainda. Posso integrar em engines research (PHI / ORNSTEIN /
  MEANREV / KEPOS / GRAHAM / BRIDGEWATER) numa próxima frente.
- **Calibração importante a lembrar**: QUANTUM usa `state_softmax_beta=6.0`
  (fixo no dataclass, NÃO varre no grid). Spec original com normalização
  linear é inviável — documentado no `hypothesis.md`.
- **Split efetivo de dados**: Binance Futures não tem 2020 pra maioria dos
  11 símbolos. "Train 2020-2023" efetivo ≈ 2022-03 a 2023-12. Considerar
  se vale reformular o split pra "2022-2023 train / 2024 test / 2025 holdout"
  numa v2 — mas mudar o split.md agora exigiria commit novo + justificativa.
- **MILLENNIUM shadow VPS** e **Fase 1b cockpit** não foram tocados. Frente
  live segue intacta. QUANTUM e chronos-entropy são paralelos.
