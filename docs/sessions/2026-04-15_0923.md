# Session Log — 2026-04-15 09:23

## Resumo
Audit total do software (5 agentes paralelos) + organização da casa:
11 commits atômicos cobrindo gitignore runtime, UX reboot do launcher,
SSOT de params/paleta, portabilidade e housekeeping. Zero mudança em
lógica de trading. Smoke test 172/172 verde.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| c006fcd | chore(gitignore): runtime lockfiles, settings locais, pytest tmp dirs | .gitignore, .claude/ |
| 5389839 | refactor(ui): UX reboot — paleta neutra + launcher inline + markets dinâmicos | launcher.py, alchemy_ui.py, macro_brain/*, website/* |
| fac018a | perf(proc): cache list_procs() com TTL 1.5s | core/proc.py, test_launcher_main_menu.py |
| 0bb41da | test(infra): isolamento de tmp dirs + skipif condicional phase C | tests/*, pyproject.toml |
| 31d1022 | feat(ui): engine picker cards + ping cache TTL 8s | core/engine_picker.py (novo), core/connections.py |
| 8dee3d7 | tools: backfill disk→DB + normalize run_ids | tools/backfill_db_from_disk.py, normalize_run_ids.py |
| 3d6896b | docs+refactor: entry points + alchemy_state portável | README.md, live_replay_test.py, alchemy_state.py |
| aa8829a | chore(gitignore): pytest tmp dirs extras | .gitignore |
| 6006d31 | refactor(params): SSOT overfit_audit + janestreet defaults | analysis/overfit_audit.py, config/janestreet_defaults.py (novo), alchemy_state.py, janestreet.py |
| 488d526 | refactor(ui): design tokens em core/ui_palette.py (SSOT) | core/ui_palette.py (novo), launcher.py, alchemy_ui.py |
| 8983af1 | fix(tools): engine_validation usa sys.executable | tools/engine_validation.py |

## Mudanças Críticas
Nenhuma mudança em lógica de trading (sinais, custos, sizing, risco).

Mudanças relevantes mas não de trading:
- `core/ui_palette.py` (novo) — SSOT da paleta consumida por launcher + alchemy_ui
- `config/janestreet_defaults.py` (novo) — SSOT dos 9 params runtime-tunáveis do JANE STREET
- `core/proc.py` ganhou cache TTL 1.5s em `list_procs()` (perf UI)
- `core/connections.py::ping()` ganhou cache TTL 8s por provider (reduz chamadas de rede)
- `analysis/overfit_audit.py` agora importa COMMISSION/LEVERAGE/FUNDING_PER_8H de params.py

## Achados (audit)
Audit total disparou 5 agentes paralelos (arquitetura, segurança, testes,
lógica de trading, UI/deps). Resumo dos achados:

### Críticos
- `config/keys.json` em plaintext (gitignored, mas Windows não protege permissões)
- Telegram bot auth frágil — só compara `chat_id` sem double-check
- Live engine sem prompt robusto pra ligar LIVE

### Altos
- `engines/millennium.py` importa 5 sibling engines (exceção documentada no CLAUDE.md, mas expansiva)
- Paper slippage ≠ backtest: `live.py:156` PAPER_SLIPPAGE=0.0003 vs params SLIPPAGE=0.0002
- `core/risk_gates.py` com defaults em "off" (100.0/999), kill-switch passivo
- **Core de trading sem teste**: `core/signals.py`, `core/indicators.py`, `core/portfolio.py` → zero cobertura
- `launcher.py` = 11.160 linhas, monolito; VPS IP hardcoded em :95

### Médios (resolvidos nesta sessão)
- ✅ Hardcodes em `engines/janestreet.py:56-61` → `config/janestreet_defaults.py`
- ✅ `analysis/overfit_audit.py` duplicava custos → importa de params.py
- ✅ Duplicação de paleta em launcher + alchemy → `core/ui_palette.py`
- ✅ Path Windows hardcoded em `tools/engine_validation.py` → `sys.executable`
- ✅ 25 PermissionError na coleta pytest → pyproject.toml norecursedirs
- ✅ Lixo runtime em disco (15+ tmp dirs) → deletado via `core.fs.robust_rmtree`

### Limpo (validado pelo audit)
- SIZE_MULT tuning recente (THOTH/NEWTON/MERCURIO/CITADEL): 6/6 walk-forward PASS
- Custos C1+C2 aplicados consistentemente nos engines de backtest
- Sem shell=True, sem eval/exec, sem secrets em git history
- Run dirs seguem padrão `data/{engine}/{YYYY-MM-DD_HHMM}/`
- Deps npm/pip atuais, sem CVE conhecida

## Estado do Sistema
- Smoke test: 172/172 passando
- Working tree: limpo
- Backlog restante (prioritário):
  1. **Segurança** — ativar `KeyStore(encrypted=True)` obrigatório + rotacionar chaves expostas (Binance, Telegram, NewsAPI, FRED)
  2. **Testes core** — escrever testes para `core/signals.py`, `indicators.py`, `portfolio.py`
  3. **Modularizar launcher.py** — quebrar os 11k linhas em módulos
- Próximo passo sugerido: **Segurança** (crítico, menor escopo, bloqueia tudo que seja LIVE)

## Arquivos Modificados
(ver tabela de commits acima; +3944 / -1004 linhas ao todo)

Novos arquivos:
- `core/engine_picker.py` (+1390)
- `core/ui_palette.py` (+82)
- `config/janestreet_defaults.py` (+42)
- `tools/backfill_db_from_disk.py` (+157)
- `tools/normalize_run_ids.py` (+169)

## Notas para o Joao
1. **Chaves expostas localmente** — o audit CRÍTICO #1 é `config/keys.json`
   em plaintext com credenciais reais (Binance live/testnet/demo, Telegram
   bot token, NewsAPI, FRED). Gitignored, mas OneDrive sincroniza sem
   proteção de permissão. Ação recomendada: rotacionar as chaves e ativar
   o `KeyStore(encrypted=True)` que já existe no `core/key_store.py`.

2. **Três engines sem teste core** — `signals.py`, `indicators.py`,
   `portfolio.py` são o coração da lógica de trading e têm zero cobertura.
   Antes de tocar em live, faz sentido caracterizar esses módulos.

3. **Launcher monolítico** — 11k linhas num único arquivo. Não é urgente
   (funciona), mas manutenção tá ficando cara. Sugestão: extrair em
   `launcher/` como package (main, menu, strategies, markets, etc).

4. **UI UX reboot** (commit 5389839) foi maior do que o audit previu —
   além da paleta, tem markets dinâmicos, strategies inline, engine picker,
   logo canvas. Tudo validado pelo smoke test mas se algum comportamento
   visual parecer estranho, checar lá.

5. **Codex companion** rodou em paralelo nesta sessão; alguns diffs
   (README, live_replay_test, alchemy_state, pyproject) apareceram no
   working tree sem contexto — absorvidos no commit 3d6896b e 0bb41da.
   Próxima sessão: coordenar branch/commits com ele antes de começar.
