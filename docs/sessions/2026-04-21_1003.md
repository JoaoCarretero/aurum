# Session Log — 2026-04-21 10:03

## Resumo
Diagnóstico total do ciclo paper/shadow após notificações suspeitas no Telegram
(runs vazios, sinais com ts futuro/stale, posições opostas no mesmo ativo).
Identificados e corrigidos 4 bugs — 3 de operação e 1 arquitetural em portfolio
gate — com TDD + evidência end-to-end via 1 tick real contra Binance.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 6cf3a7a | fix(paper+shadow): priming, ts futuro e crash-loop do service | tools/operations/millennium_paper.py, tools/maintenance/millennium_shadow.py, deploy/millennium_paper.service, tests/integration/test_paper_runner_tick.py, tests/tools/test_millennium_shadow_helpers.py |
| b6b64a7 | fix(paper): portfolio gate V2 — bloquear direções opostas cross-engine | tools/operations/millennium_paper.py, tests/integration/test_paper_runner_tick.py |

## Mudanças Críticas

**ATENÇÃO:** Três mudanças afetam comportamento de entrada/risco — revisar antes
de rodar live:

1. **Priming não-bloqueante (millennium_paper.py:478-503)**
   - Antes: 1º tick nunca abria posição (pulava todos sinais). Paper precisava
     15min (default tick_sec=900s) pra começar a operar.
   - Depois: priming só popula `seen_keys`; `is_live_signal()` passa a ser
     o único gate stale. Sinal fresco no boot abre imediatamente.
   - Proteção mantida: o gate `is_live_signal` já rejeita sinais fora da janela
     `[-tick_sec, +2*tick_sec]`, o que cobre os 628 sinais históricos do
     backscan 90d (bug original que motivou priming em 2026-04-19).

2. **Portfolio gate V2 (millennium_paper.py:519-538)**
   - Novo gate após MAX_OPEN_POSITIONS — rejeita abrir posição em símbolo
     que já tem posição aberta em direção oposta (reason=`direction_conflict`).
   - Policy: first-come-first-served dentro do tick. Mesma direção passa
     (confluence legítima, sujeita a MAX_OPEN_POSITIONS).
   - Previne hedge acidental cross-engine (JUMP SHORT + RENAISSANCE LONG
     mesmo ativo = exposição líquida zero + custos dobrados).

3. **Clamp cosmético de `ts` futuro (millennium_shadow.py:_tg_signal)**
   - Telegram do shadow clampa ts pra `now()` se > agora. Dedup intocado
     (`open_ts` bruto). Só display.

Fix operacional adicional (service config, não trading): `deploy/millennium_paper.service`
removeu `--account-size ${AURUM_PAPER_ACCOUNT_SIZE}` do ExecStart — evitava
crash-loop se `/etc/aurum/paper.env` ausente (argparse tem default via env ou 10k).

## Achados

### Bugs corrigidos

- **Paper sempre abria 0 posições no 1º tick** — runs <15min (todos os runs
  recentes, por causa do SIGTERM) produziam 0 trades. Arquivos `state/` e
  `reports/` vazios em todos os runs recentes confirmam.
- **Sinais stale (ts janeiro 2026)** viajando pro Telegram — já corrigido pelo
  commit `ccc2e46` (2026-04-20) via `is_live_signal()`, mas mensagens antigas
  ainda aparecem no histórico do chat (sem ação necessária).
- **Sinais com ts futuro** (ex: ARBUSDT ts 11:00 notificado às 02:30) — causado
  pelo `live_mode` escanear tail bars cuja `open_time > now`. Clamp cosmético
  aplicado.
- **Crash-loop potencial do service paper** se env ausente — fix defensivo no
  ExecStart.
- **Hedge acidental cross-engine** — bug arquitetural descoberto via testes:
  paper abre LONG+SHORT no mesmo ativo se engines diferentes firam direções
  opostas. Gate V2 implementado.

### Bug latente identificado (não fixado nesta sessão)

- **Import do `millennium_paper.py` cria RUN_DIR automaticamente** (linhas 62-68).
  Cada import via pytest/CLI/launcher cria dir vazio em `data/millennium_paper/`.
  Limpeza feita: 11 dirs vazios removidos. Considerar mover criação de dirs
  pra dentro de `run_paper()` em sessão futura.

### Não-bugs

- **SIGTERM origin no VPS** não 100% confirmado — precisa `journalctl -u
  millennium_paper` + `-u millennium_shadow` pra discriminar entre
  `MemoryMax=1G`, `systemctl stop` externo, ou outro. Fix 2B mitiga o impacto
  prático (paper abre trade desde o tick 1).
- **Shadow ≠ paper estruturalmente** — confirmado via Phase 3 que shadow é
  "sinal puro" (sem custos, sem equity contínuo), paper é o proxy correto
  pra DD/ROI. **Não é bug** — é design. Não tocado.

## Estado do Sistema

- **Smoke test:** 178/178 passed
- **Suite integration + tools + metrics + cockpit:** 238 passed, 7 skipped
- **Erros em `test_launcher_main_menu.py`** (59): SSL/HTTP fails em arbitrage
  hub (Vertex/Aevo/Bybit/Drift) — **pré-existentes**, tocam redes externas,
  não relacionados aos fixes desta sessão.
- **Teste end-to-end real:** paper rodou 1 tick contra Binance em 60s, abriu
  JUMP SHORT LINKUSDT (notional $317.75, commission $0.127 = 0.04%), equity
  snapshot OK, state populado. **Fix 2B confirmado em produção local.**
- **Evidência preservada:** `data/millennium_paper/2026-04-21_125102/` (6 files)

## Arquivos Modificados

- `tools/operations/millennium_paper.py` (+30 / -7) — Fix 2B + Fix 5
- `tools/maintenance/millennium_shadow.py` (+12 / -1) — Fix 3 (clamp ts)
- `deploy/millennium_paper.service` (+8 / -1) — Fix 1 (service resiliente)
- `tests/integration/test_paper_runner_tick.py` (+195 / 0) — 3 testes novos
- `tests/tools/test_millennium_shadow_helpers.py` (+59 / -2) — 2 testes novos

Total: 5 arquivos, +304 / -11 linhas.

## Notas para o Joao

1. **No VPS, atualizar e reiniciar:**
   ```bash
   cd /srv/aurum.finance && git pull
   sudo systemctl daemon-reload
   sudo systemctl restart millennium_paper.service
   sudo systemctl restart millennium_shadow.service
   sudo journalctl -u millennium_paper -f
   ```

2. **Observar journalctl por ~20 min** — com Fix 2B, paper deve abrir a
   primeira posição no 1º tick (se houver sinal fresco). Se não houver sinal,
   o log do tick 1 mostra `PAPER PRIMED tick=1 signals_seen=N`.

3. **Se SIGTERM voltar a matar runs, o journalctl agora vai revelar a causa**
   (OOM, stop manual, timeout). Me mandar o último minuto antes do kill.

4. **Sobre hedge acidental** — você reportou "long e short em estratégias
   diferentes no mesmo ativo". Era paper abrindo ambos (bug). Gate V2 agora
   garante first-come-first-served. Se quiser mudança de policy (ex: engine
   de maior peso vence, ou veto total cross-engine no mesmo símbolo), me avisa.

5. **Shadow não abre trade nunca** — é detector puro. As notificações `SHADOW
   · <engine> <dir> <symbol>` são sinais registrados pra auditoria, não posições.
   Paper e live são quem executam (simulado / real).

6. **Backlog dessa sessão:** bug do `mkdir` em import-time do millennium_paper
   (cria dir vazio a cada pytest) — fixar em sessão futura; não é crítico.

7. **Verificação final no VPS que confirma que está rodando 100%:**
   - `heartbeat.json` tem `ticks_ok` subindo
   - `equity.jsonl` cresce a cada tick
   - `account.json` mostra equity ≠ 10000 após primeiro trade fechado
   - Se uma posição conflitante chegar, `signals.jsonl` terá `reason=direction_conflict`
