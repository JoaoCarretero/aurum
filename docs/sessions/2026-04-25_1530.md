# Session Log — 2026-04-25 15:30

## Resumo

Investigação engines rodando + arquitetura DB impecável + paper runner DB
persistence + per-engine scripts trazidos pra research-desk. 6 commits em
sequência: ssh-tunnel `-F NUL` fix, _fetch_trades shadow_trades.jsonl path
fix, live_trades+live_signals tables (migration 002), paper runner
_persist_trade_to_db hook, per-engine paper runners merged into research-desk
(crisis recovery após git pull bricar 3 services no VPS), e VPS deploy.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| fd6f705 | fix(ssh-tunnel): empty config file ao inves de NUL/dev/null pro -F flag | launcher_support/ssh_tunnel.py, tests/test_ssh_tunnel.py |
| 61b332e | fix(engine_detail): _fetch_trades reads shadow_trades.jsonl too | launcher_support/engine_detail_fetchers.py, tests/test_engine_detail_view.py |
| b17bb10 | docs(session): post-restart 11 engines aligned + AVAXUSDT investigation | docs/sessions/2026-04-25_1130.md |
| 908f0a4 | feat(db): live_trades + live_signals tables — paper/shadow → SQL | tools/maintenance/migrations/migration_002_live_trades.py, core/ops/db_live_trades.py, tools/maintenance/backfill_live_trades.py, tests/test_db_live_trades.py |
| cbbd43a | feat(paper): persist closed trades to live_trades on close | tools/operations/millennium_paper.py, tests/test_paper_runner_db_persist.py |
| 5dfa479 | feat(paper): bring per-engine paper runners + DB persistence into research-desk | tools/operations/{_paper_runner,citadel_paper,jump_paper,renaissance_paper,run_id,tick_schedule}.py |

## Mudanças Críticas

**Restart sincronizado dos 11 engines no VPS** em 2026-04-25 15:09:35 UTC.
Stop+start simultâneo via systemctl em todos os 11 services (paper + shadow
+ probe). Variação de start <500ms entre services. Resultado: paper e shadow
agora compartilham timestamp de start (era drift de 3h11min — shadow desde
24/04 17:40 UTC, paper desde 20:51 UTC). Sinal único do dia (AVAXUSDT
BULLISH RENAISSANCE em 18:31 UTC) ficou no histórico das runs antigas; novas
runs tick #1 em todas, novel=0 baseline limpo.

**Mudanças em código operacional de live trading** (paper, não live):
- `_persist_trade_to_db` adicionado em `tools/operations/millennium_paper.py`
  + `tools/operations/_paper_runner.py` — chama `core.ops.db_live_trades.upsert_trade`
  ao fechar trade. Failure isolation: catch all exceptions, log WARNING,
  nunca raise. Trade lifecycle não depende de DB availability.
- Wired em ambos close sites: `_flatten_all` (kill-switch flatten) +
  `run_one_tick` (normal exit on bar check).

Não toquei em `core/{indicators,signals,portfolio}.py` ou `config/params.py`
(CORE protegido). Toquei `tools/operations/*` e `core/ops/db_live_trades.py`
(novo módulo, não-CORE) e `launcher_support/ssh_tunnel.py` (UI).

## Achados

- **AVAXUSDT BULLISH (RENAISSANCE Gartley, score 0.6598) em 24/04 18:31 UTC**
  é o ÚNICO signal do dia em todas 11 engines. Foi millennium shadow tick #5
  que detectou. Paper missed por timing (paper started 20:51, signal foi 18:31
  — 2h20min antes). Não é bug — `_collect_live_signals` é o mesmo source pra
  ambos, mas seen_keys foram diferentes porque shadow começou antes.
- **Filenames mismatch**: VPS shadow runner escreve `reports/shadow_trades.jsonl`
  (não `trades.jsonl`). engine_detail_fetchers só procurava `trades.jsonl`. Fix
  em `61b332e`.
- **`-F NUL` quebra em git-bash**: ssh.exe (MSYS-based) trata "NUL" como
  filename literal. TunnelManager.start() ficava em `status=starting` infinito.
  Fix: empty file real em `log_dir/empty_ssh_config`. End-to-end test confirma
  status=up agora.
- **3 per-engine paper services órfãos**: `citadel_paper@`/`jump_paper@`/
  `renaissance_paper@` no VPS dependem de scripts que vivem só em
  `feat/per-engine-runners`. Meu `git pull origin feat/research-desk`
  apagou + bricado os 3. **Crisis recovery**: pulled scripts via
  `git checkout origin/feat/per-engine-runners -- tools/operations/...`,
  depois committei eles em research-desk pra próximo pull não bricar (5dfa479).
- **Engines silenciosos há 18h+**: 80+ ticks consecutivos `scanned=0
  engines_ok=0` em todas. Não é silent failure (PRIME tick #1 antes do
  AVAXUSDT signal mostra mesmo padrão); é mercado quiet pra esses filtros.

## Estado do Sistema

- **Smoke test**: 172/172
- **Tests novos hoje (acumulados)**: 23 (16 db_live_trades + 7 paper_runner_db_persist)
- **CORE intact**: ✓ (zero diff vs antes do trabalho)
- **`config/keys.json`**: ✓ intact
- **DB local** (`data/aurum.db`):
  - live_runs: 175 rows (11 running fresh + 164 historical)
  - live_trades: 0 (nenhum close pós-restart ainda)
  - live_signals: 9 reais (5 RENAISSANCE + 4 JUMP de runs históricas)
  - runs (backtest): 15 rows
  - trades (backtest): 409 rows
- **DB VPS** (`/srv/aurum.finance/data/aurum.db`):
  - live_trades + live_signals tables criadas via migration_002
  - 169 live_runs (slightly behind local devido a backfill timing)
- **VPS state**: HEAD @ `5dfa479`, todos 11 services active
- **Tunnel SSH**: UP (`127.0.0.1:8787` LISTENING)
- **Backlog restante**:
  - feat/per-engine-runners merge formal pra research-desk (5dfa479 trouxe
    arquivos críticos mas não é merge full)
  - Stashes órfãs (1-3 ainda lá do branch deletado)
  - Live trades vão começar a aparecer em DB conforme paper engines fechem
    posições

## Arquivos Modificados

```
launcher_support/ssh_tunnel.py                         (-F NUL fix)
launcher_support/engine_detail_fetchers.py             (shadow_trades path)
core/ops/db_live_trades.py                             (NEW, 152 lines)
tools/maintenance/migrations/migration_002_live_trades.py (NEW)
tools/maintenance/backfill_live_trades.py              (NEW)
tools/operations/millennium_paper.py                   (+50 _persist_trade_to_db)
tools/operations/_paper_runner.py                      (NEW + _persist_trade_to_db)
tools/operations/citadel_paper.py                      (NEW from per-engine-runners)
tools/operations/jump_paper.py                         (NEW)
tools/operations/renaissance_paper.py                  (NEW)
tools/operations/run_id.py                             (NEW)
tools/operations/tick_schedule.py                      (NEW)
tests/test_ssh_tunnel.py                               (+1 test -F portability)
tests/test_engine_detail_view.py                       (+2 shadow_trades tests)
tests/test_db_live_trades.py                           (NEW, 16 tests)
tests/test_paper_runner_db_persist.py                  (NEW, 7 tests)
docs/sessions/2026-04-25_1130.md                       (extension append)
```

Total: 17 arquivos (10 novos + 7 modificados), ~700 linhas adicionadas.

## Notas para o Joao

**Tudo pushed em `origin/feat/research-desk` @ `5dfa479`.**

**O que muda agora pra você:**

1. **Próximo paper close vai aparecer no DB** automaticamente. Tanto na
   tabela `live_trades` no VPS (`/srv/aurum.finance/data/aurum.db`) quanto
   no seu local (após `python -m tools.maintenance.backfill_live_trades
   --from-vps --apply`).

2. **Query SQL pra trades de hoje funciona agora**:
   ```sql
   SELECT t.strategy, t.symbol, t.direction, t.pnl_usd, t.r_multiple
   FROM live_trades t
   JOIN live_runs r ON t.run_id = r.run_id
   WHERE r.engine = 'citadel' AND r.mode = 'paper'
     AND t.ts >= '2026-04-25';
   ```

3. **DATA > ENGINES drill-down ❻ TRADES** vai mostrar shadow signals corretamente
   (era empty antes pelo `shadow_trades.jsonl` filename mismatch — fix em 61b332e).

4. **Tunnel SSH** sobe sozinho do launcher agora mesmo de git-bash (era bug
   `-F NUL` que travava em "starting" infinito).

5. **Engines TODAS rodando alinhadas** desde 15:09:35 UTC. Próximo signal
   visto em paper + shadow simultâneo (não há mais drift de seen_keys).

**Pendências não-críticas:**

- `feat/per-engine-runners` ainda tem trabalho não-mergeado em research-desk
  (eu trouxe os 6 scripts críticos mas pode ter mais). Merge formal numa
  sessão dedicada.
- Stashes 1-3 órfãs (de cleanup-phase-3 deletada). Triar/dropar.
- Manual visual checklist DATA > ENGINES (19 pontos do session log
  2026-04-25_1055) ainda pendente — abrir launcher.py + clicar.

**Risco zero de regressão:** smoke 172/172 antes e depois de cada commit,
CORE files com `git diff` vazio em todas as 6 mudanças, paper runner mudança
testada com 7 tests cobrindo happy path + idempotência + DB ausente +
DB corrompido + invalid record + multi-trade + dict não-mutado.
