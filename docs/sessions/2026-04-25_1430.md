# Session Log — 2026-04-25 14:30

## Resumo

Audit completo da entrada "JUMP" reportada no Telegram: identificado
que MILLENNIUM-paper VPS abriu 2 trades JUMP XRPUSDT (1 stoppada
-$17.85, 1 aberta), enquanto JUMP/CITADEL/RENAISSANCE standalone +
todos shadows estavam **silenciosamente quebrados** (scan retornando
0 em todo tick). Cinco fixes shipped em 4 commits: (1) IndentationError
local, (2) `_scan_one_engine_live` missing helper, (3) shadow services
restart pra carregar 5dfa479 (estavam em 667614b cacheado), (4)
SYM_LOSS_COOLDOWN runtime, (5) WAL-safe VPS DB mirror via SSH.

## Commits

| Hash    | Mensagem                                                              | Arquivos                                                |
|---------|-----------------------------------------------------------------------|---------------------------------------------------------|
| be4dd99 | fix(paper-runner): per-engine runners — indent + missing scan helper  | engines/millennium.py · tools/operations/_paper_runner.py · tests/test_scan_one_engine_live.py |
| 7fd22e3 | feat(paper): SYM_LOSS_COOLDOWN no runtime live — bloqueia re-entry pos-stop | tools/operations/millennium_paper.py · tests/integration/test_paper_runner_sym_cooldown.py |
| a2b2cea | feat(db-sync): VPS->local mirror de live_trades + live_signals (WAL-safe) | core/ops/db_live_trades.py · tools/maintenance/sync_vps_db.py · tests/test_db_live_trades_cockpit_aliases.py · tests/test_sync_vps_db.py |
| (ops)   | systemctl restart × 6 shadow services no VPS                          | (no diff — process restart)                              |

(commit `7f16d34` test(paper-runner): extend _isolate_runner_db helper já estava
no momento do trabalho, e `c74e77b chore(cleanup)` veio em paralelo do Joao.)

## Mudanças Críticas

**ATENÇÃO — runtime live trading mudou:**

- `tools/operations/millennium_paper.py` agora arma cooldown por símbolo
  após `stop_initial`/`trailing` exits. Bloqueia opens no mesmo símbolo
  por `SYM_LOSS_COOLDOWN_BARS=24` × tick_sec (= 6h em 15m tf). Override
  via `AURUM_SYM_LOSS_COOLDOWN_BARS=N` (0 desliga). Sem isso, o run de
  hoje viu pos_000001 stoppar em 13min e pos_000002 abrir 17min depois
  no mesmo XRP LONG — backtest aplica cooldown, runtime não aplicava.

- `core/ops/db_live_trades._norm_trade` agora aliases `entry_at→ts`,
  `engine→strategy`, `pnl_after_fees→pnl_usd`. Sem isso, backfill
  --from-vps recebia payloads do cockpit mas rejeitava silenciosamente
  (missing-key) e DB local ficava vazio.

**Não-trading**:

- `engines/millennium.py` ganhou `_scan_one_engine_live(engine_name)` —
  helper que `_paper_runner.py:290` importa. Antes do fix, todo tick dos
  3 paper standalones (citadel/jump/renaissance) logava `WARNING scan
  failed: cannot import name '_scan_one_engine_live'` e novel=0 perpétuo.

- `tools/operations/_paper_runner.py:406` — `trade_record = {` estava
  com indent extra (regression de `d168748`), IndentationError no import
  local. VPS estava em `5dfa479` (pré-bug) então VPS não pegou esse
  syntax error, só o ImportError de #2.

## Achados

1. **Shadow services no VPS rodavam código stale (commit 667614b
   cacheado em `sys.modules`)**. Manifest em `state/manifest.json`
   gravava o commit em runtime; os 5 shadows (millennium-a/b, jump,
   citadel, renaissance) e probe estavam em `commit: 667614b` enquanto
   o working tree do VPS estava em `5dfa479`. Provável race: deploy
   pull começou antes do restart sincronizado das 11:09 UTC; shadow
   restart agarrou snapshot pré-pull. Paper foi reiniciado depois
   (15:30) e pegou código novo.
   **Fix operacional**: `systemctl restart millennium_shadow@desk-shadow-{a,b}
   citadel_shadow@desk-a jump_shadow@desk-a renaissance_shadow@desk-a
   aurum_probe@desk-a`. Verificado: novo run `2026-04-25_172720s_desk-shadow-b`
   manifest agora `commit: 5dfa479`, primeiro PRIME tick `scanned=2
   engines_ok=1 novel=1 live=1` — scan funcionando.

2. **Cockpit `/v1/runs/{id}/signals` retorna 404 no VPS** (endpoint
   adicionado no commit `b53fb0e`, post-deploy 5dfa479). Não bloqueia
   nada agora porque sync_vps_db pula o cockpit e usa SSH+sqlite
   .backup direto. Quando próximo deploy do cockpit_api rodar, a rota
   passa a existir e backfill_live_trades --from-vps também sincroniza
   signals.

3. **VPS sqlite roda em WAL mode** com `aurum.db-wal` de 4MB+ ativo. SCP
   do `.db` sozinho pega snapshot estado-anterior-ao-WAL (zero linhas
   nos casos típicos). Solução em `sync_vps_db._scp_pull_vps_db`: SSH
   roda `sqlite3 .backup '/tmp/snap.db'` (atomic, WAL-safe), depois scp
   do snapshot, depois cleanup remoto. Documentado pra quem fizer mirror
   manual no futuro.

4. **JUMP runtime acima do per-engine threshold MILLENNIUM**: o sinal
   XRP que entrou hoje tinha score 0.696, abaixo do
   `JUMP_MIN_SCORE_BASE=0.79` definido em `engines/millennium.py:160`
   pra ensemble. MILLENNIUM-paper aplicou só o `MIN_SCORE`-genérico
   (0.50) do scan_mercurio interno e `MAX_OPEN_POSITIONS` da camada
   paper. Os JUMP_MIN_SCORE_* são só pros gates do orchestrator
   ensemble (linhas 1860+ no `_collect_live_signals`), que paper não
   usa. **Não é bug por si só** — foi decisão de design ao desligar
   `PORTFOLIO_EXECUTION_ENABLED=False` (linha 120). Mas vale revisitar
   se quisermos paper espelhar 100% do ensemble live.

## Estado do Sistema

- Smoke test: 172/172 ✓
- Suite full: 2339 passed / 8 skipped / 1 xfailed (60s xdist -n auto)
- 38 tests novos hoje (incluso commits prévios da sessão):
  - `test_scan_one_engine_live.py` — 6 (fix #2)
  - `test_paper_runner_sym_cooldown.py` — 3 (fix #4)
  - `test_db_live_trades_cockpit_aliases.py` — 5 (fix #5a)
  - `test_sync_vps_db.py` — 4 (fix #5b)
- VPS state: HEAD `5dfa479`, todos shadows agora em commit correto, paper-a
  com pos_000002 XRP aberta + pos_000001 fechada (-$17.85)
- DB local: 2 live_trades hoje persistidos (XRP × paper-a/b), 9 live_signals
  totais (8 disk historical + 1 SAND dedup)
- Keys intactas ✓
- Backlog restante:
  - Validar o segundo XRP (pos_000002) — ainda aberta, deveria estar bloqueada
    pelo cooldown novo se acontecesse hoje
  - JUMP_MIN_SCORE_* aplicar no paper se quiser comportamento do orchestrator
  - Cron pra rodar `python -m tools.maintenance.sync_vps_db` periodicamente
- Próximo passo sugerido: verificar próximas 24h se shadows vêm pegando
  signals correlacionados com paper trades (`engines_ok > 0` em cada tick).

## Arquivos Modificados

```
engines/millennium.py                                  +25  -0   (fix #2 — be4dd99)
tools/operations/_paper_runner.py                      +6   -7   (fix #1 — be4dd99)
tests/test_scan_one_engine_live.py                     +66  new  (fix #2 — be4dd99)
tools/operations/millennium_paper.py                   +60  -1   (fix #4 — 7fd22e3)
tests/integration/test_paper_runner_sym_cooldown.py    +200 new  (fix #4 — 7fd22e3)
core/ops/db_live_trades.py                             +9   -3   (fix #5a — a2b2cea)
tests/test_db_live_trades_cockpit_aliases.py           +110 new  (fix #5a — a2b2cea)
tools/maintenance/sync_vps_db.py                       +180 new  (fix #5b — a2b2cea)
tests/test_sync_vps_db.py                              +120 new  (fix #5b — a2b2cea)
```

## Notas para o Joao

- Você commitou `be4dd99` em paralelo enquanto eu investigava — perfect, eu
  não tive que recommitar. Mensagem do commit foi melhor que a minha.
- Restart dos 6 shadow services fiz via SSH systemctl. O Telegram deve ter
  recebido 6 mensagens de "shadow STOP" + 6 "shadow START". É barulhento
  mas igual ao restart sincronizado do morning.
- O próximo XRP que vier vai ser bloqueado pelo cooldown novo. Se quiser
  desligar pra teste, `AURUM_SYM_LOSS_COOLDOWN_BARS=0` no env do
  `aurum_paper@desk-paper-a.service`.
- Próxima vez que fizer deploy: lembrar que **shadow services não
  recarregam código por filesystem change** — sempre `systemctl restart`
  explícito. Adicionar isso ao `deploy/install_*.sh` evita esse tipo de
  drift silencioso.
- Para sincronizar VPS DB on-demand: `python -m tools.maintenance.sync_vps_db`
  (precisa SSH config em keys.json `vps_ssh.host`/`key_path`). Idempotente,
  pode rodar quantas vezes quiser. Output: "merged N trades + M signals".
- O sinal XRP que entrou era score 0.696 — abaixo do `JUMP_MIN_SCORE_BASE=0.79`
  do orchestrator ensemble. Como `PORTFOLIO_EXECUTION_ENABLED=False`, paper
  só aplica o min_score 0.50 do scan_mercurio puro. Vale decidir se isso é
  o comportamento que você quer ou se quer wire o ensemble gate no paper
  também.
