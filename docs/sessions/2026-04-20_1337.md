# Session Log — 2026-04-20 13:37

## Resumo

Sessão curta de retomada do plano "limpeza-geral 2026-04-20". Fechou dois
follow-ups pendentes: Sub-B review findings (polimento pós-code-review de
`hmm_cache`/`chronos`/`__init__`/`conftest`) e Sub-C completion (rewire
dos imports nos arquivos que Codex moveu em race + docstring expandido do
`millennium_live`). Plano limpeza-geral **100% fechado**: A + B + C.

## Commits

| Hash    | Mensagem                                                                                   | Arquivos                                                                                                                                             |
|---------|--------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------|
| 627fd1e | fix(cleanup): address code review findings on sub-projeto B                                | core/__init__.py, core/chronos.py, core/hmm_cache.py, tests/conftest.py                                                                              |
| c529e05 | fix(cleanup): finish sub-projeto C — rewire archived imports + document millennium_live    | engines/millennium_live.py, tests/engines/_archive/test_meanrev.py, tools/_archive/meanrev_partial_revert_search.py, meanrev_snapback_search.py, meanrev_variant_search.py |

## Mudanças Críticas

**Nenhuma mudança em lógica de trading.** Zero toque em CORE PROTEGIDO
(`core/indicators.py`, `core/signals.py`, `core/portfolio.py`,
`config/params.py`). Engines tuning (bridgewater/deshaw/kepos/medallion)
detectado no working tree permanece **uncommitted** — é trabalho separado,
não foi introduzido por esta sessão.

## Achados

- **Race com Codex no staging area.** Entre o `git mv` dos 5 meanrev
  files e o commit final do Sub-C, Codex commitou em paralelo
  (`688f5a9 feat(paper): backtest-parity` + `18fe497` fix-up). O
  `688f5a9` pegou carona nos meus renames de meanrev que estavam no
  index, misturando refactor de paper runner com renames de meanrev
  numa mensagem enganosa. Codex documentou isso no `18fe497`.
  Histórico ficou ok (conteúdo correto), só a mensagem do 688f5a9 é
  ambígua. **Meus fixes de imports/REPO_ROOT vieram depois no `c529e05`
  pra completar o que Codex moveu sem atualizar.**
- **2 failures em `tests/contracts/`** (bridgewater + deshaw) —
  `test_runtime_preset_robust_forces_disciplined_funding_ls_mode` e
  `test_scan_pair_stops_new_entries_after_pair_decay`. Causadas pelo
  engine tuning uncommitted, não pela limpeza. Passariam com revert
  do working tree.
- **Git index glitch intermitente (OneDrive?).** Depois de algum comando
  (provavelmente `pytest`), o `git status` mostrou "no changes staged"
  mesmo com renames staged anteriormente — mas `git ls-files` e os
  commits subsequentes confirmaram que o index estava OK. Parece
  display issue do git em dir sincronizado OneDrive.
- **Pra evitar race futura com Codex:** quando estiver em operações
  sensíveis no index (rename em massa, staging seletivo), avisar Codex
  pra parar ou trabalhar em branches separadas.

## Estado do Sistema

- Smoke test: **178/178** (0 failures)
- Suite full: **1462 passed, 7 skipped, 2 failed**
  - Os 2 failures são do engine tuning uncommitted, não da limpeza
- Plano limpeza-geral 2026-04-20: **TODOS sub-projetos ✅**
- Branch: `feat/phi-engine` — 2 commits novos nessa sessão (`627fd1e`, `c529e05`)
- Backlog restante:
  1. Engine tuning uncommitted (bridgewater PnL change + deshaw HMM
     filter + kepos HMM/RSI + medallion min_components/ema_target) —
     precisa seguir protocolo anti-overfit (mecanismo + split + grid
     fechado + DSR)
  2. Paper runner VPS com 180% notional exposure (V1 scope deviation)
  3. Restaurar `config/keys.json` — Binance demo/testnet/live +
     macro_brain (FRED + NewsAPI)
- Próximo passo sugerido: decidir destino do engine tuning uncommitted
  — branch próprio com plan anti-overfit, ou revert se experimental.

## Arquivos Modificados

Total (só esta sessão): **9 arquivos, 60 insertions, 32 deletions**.

- `core/__init__.py` (+7/−2) — `__dir__()` retorna só nomes públicos
- `core/chronos.py` (+8/−6) — skip `.copy()` em `cache_set` (cache_get copia)
- `core/hmm_cache.py` (+9/−8) — `np.ascontiguousarray` guard + IEEE-754 bytes do scalar + narrowed exception + unlink corrupt
- `tests/conftest.py` (+5/−4) — numpy/pandas imports lazy dentro de `_build_ohlcv`
- `engines/millennium_live.py` (+14/−2) — docstring expandido (consumers + rationale + NOT CORE PROTEGIDO)
- `tests/engines/_archive/test_meanrev.py` (+1/−1) — import → `engines._archive.meanrev`
- `tools/_archive/meanrev_partial_revert_search.py` (+2/−2) — import + `parents[2]`
- `tools/_archive/meanrev_snapback_search.py` (+2/−2) — idem
- `tools/_archive/meanrev_variant_search.py` (+2/−2) — idem

## Notas para o Joao

**Plano limpeza-geral 2026-04-20 fechou.** Os três sub-projetos
entregues ao longo do dia, consolidados no daily log:
- Sub-A (disco): ~2GB liberados + scripts VACUUM + archive_old_runs
- Sub-B (perf): HMM cache + lazy core init + session OHLCV fixture
- Sub-C (dead code): meanrev cluster em `_archive/`, `millennium_live`
  documentado

**⚠️ ATENÇÃO: engines/bridgewater.py tem mudança na fórmula de custos
uncommitted** — adiciona `slip_entry` ao `entry_cost` (antes era só
`COMMISSION`). Isso:
1. Invalida a calibração OOS do BRIDGEWATER
2. Quebra 2 testes de contrato
3. É efetivamente mudança na economia de trade, mesmo não estando no
   CORE PROTEGIDO estrito

Se foi intencional (pra corrigir assimetria de custos entrada vs saída),
precisa: anti-overfit plan + re-calibração walk-forward do zero. Se foi
acidente, reverter antes de outras mudanças.

**Outros tuning uncommitted (deshaw/kepos/medallion)** parecem filtros
anti-overfit (HMM regime + RSI exhaustion + min_active_components). Se
for pra levar adiante, precisa de mecanismo escrito + grid pre-registered
+ DSR.

**Race com Codex foi a lição do dia.** Evitar trabalhar em paralelo em
commits sensíveis ao index — coordenar via texto ou branches separadas
antes.

**Pra próxima sessão**: decidir destino do tuning, ou parar e zerar o
working tree se quiser baseline limpa antes de abrir nova frente.
