# Session Log — 2026-04-19 15:53

## Resumo

Sessão de **auditoria + organização do trabalho solto do Codex**. Mapeou
2 Claudes (millennium-paper, lane-2b-hmm) + Codex com 62 arquivos
uncommitted no main worktree em 3 tracks (security hardening, import
migration `core.*`, launcher modularização). Diagnóstico estrutural
achou 6 riscos — aplicou fixes cirúrgicos: encrypted-aware keys.json
no launcher (read + write + tunnel), docstring desalinhada em
`engines/live.py`, 4 testes Codex quebrados em `test_launcher_bootstrap_contracts.py`.
Comitou tudo em 4 chunks temáticos. Code-review pos-commit achou
3 issues genuínos (allow_plaintext_env fail-open, except genérico no
tunnel manager, assertion enganosa em test_infra). Tudo corrigido.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| a85792d | feat(security): hardening end-to-end — key_store encrypted-first, JWT min 32, SSH strict host, security_readiness | 14 files (+702/-307) |
| 7cb0c19 | refactor(imports): migra core.* -> core.<subpkg>.* em 51 arquivos | 51 files (+285/-138) |
| dfe9e2c | refactor(launcher): extrai cockpit_tab + command_center + dashboard_controls + encrypted-aware keys.json | 5 files (+1899/-1348) |
| 3115415 | docs(audits): 2026-04-19 workspace faxina | 1 file (+121) |
| 206a291 | feat(audits): full registry audit runner (Codex, orphaned) | 1 file (+500) — commit do Joao |
| f3cd9e9 | fix(launcher): fail-closed em keys.json quando enc ativo + assertion de escape explícito | 2 files (+24/-7) |

## Mudanças Críticas

**ATENÇÃO — mudanças em runtime de credenciais (não em lógica de trading):**

- `core/risk/key_store.py` reescrito: encrypted-first semantics,
  `AURUM_ALLOW_PLAINTEXT_KEYS=1` é opt-in explícito, `load_runtime_keys`
  exposto como entrypoint oficial.
- `launcher._load_json/_save_json("keys.json")` agora encrypted-aware:
  lê via `load_runtime_keys` com fail-closed (ignora
  `AURUM_ALLOW_PLAINTEXT_KEYS`); escrita bloqueada quando enc ativo.
- `api/auth.py`: JWT exige >=32 chars; websocket `?token=` query
  param removido (só Authorization bearer).
- `launcher_support/bootstrap.py`: SSH `StrictHostKeyChecking=yes`,
  `PasswordAuthentication=no`, `IdentitiesOnly=yes`, comando remoto
  envolvido em `bash -lc` com shlex.quote.
- `api/server.py`: bind 127.0.0.1 default, CORS whitelist.

Zero linhas em CORE PROTEGIDO (indicators/signals/portfolio/params).

## Achados

- **Split-brain launcher/keys.json**: launcher lia plaintext direto em 9
  call sites enquanto `engines/live` + `engines_live_view` já usavam
  `load_runtime_keys`. Corrigido centralmente em `_load_json`/`_save_json`.
- **Fail-open em dev com AURUM_ALLOW_PLAINTEXT_KEYS=1**: review achou
  que `load_runtime_keys()` default respeita o env, caindo pra plaintext
  stale mesmo com enc ativo. Fix: passar env var inexistente forca
  fail-closed.
- **4 testes Codex-introduzidos quebrados**: `_validate_key_path` passou
  a exigir arquivo real; fixtures usavam `C:/keys/id_ed25519` hardcoded.
  `build_vps_ssh_command` mudou shape pra `[..., host, "bash", "-lc", cmd]`.
- **Assertion enganosa**: `"diag;rm -rf /" in launch_cmd` passava porque
  o shlex.quote envolve em aspas — mas não era óbvio pro leitor que
  estava validando escape. Reforçado pra `"'diag;rm -rf /'"`.
- **Full registry audit tool órfão**: `tools/audits/full_registry_audit.py`
  (500 linhas) apareceu untracked durante a sessão (mtime 15:35), não
  estava em nenhuma branch. Joao commitou como `206a291` com nota
  "Codex orphaned".

## Estado do Sistema

- Smoke test: 178/178
- Contracts: 864/864
- Suite completa: 1321 passed, 7 skipped (1 flaky pré-existente em
  `test_engines_live_view_cockpit::test_find_latest_shadow_run_local_fallback_when_client_none`
  — pollution de `ShadowPoller` singleton de teste anterior, não é meu)
- Backlog restante:
  - Lane 2b HMM speedup (worktree `lane-2b-hmm`, Task 8 in-flight)
  - MILLENNIUM paper runner (worktree `millennium-paper`, Task 2/15)
  - Worktrees parados: `chronos-entropy`, `quantum-engine`, `safety-tests`
    (verificar se ainda têm valor)
- Próximo passo sugerido: deixar Claudes A/B continuarem; quando
  millennium-paper terminar, merge traz o plan doc que ficou untracked
  aqui.

## Arquivos Modificados (consolidado da sessão)

Commits a85792d + 7cb0c19 + dfe9e2c + 3115415 + f3cd9e9:
- **Security**: `core/risk/key_store.py`, `api/{auth,server,risk_check}.py`,
  `launcher_support/bootstrap.py`, `tools/maintenance/{encrypt_keys,rotate_keys,security_readiness}.py`,
  `deploy/{aurum_cockpit_api.service,install_cockpit_api_vps.sh}`,
  `docs/deploy/security_hardening_runbook.md`,
  `tests/contracts/test_{key_store_runtime,security_readiness,launcher_bootstrap}_contracts.py`
- **Imports**: 51 arquivos migrando `core.X` → `core.<subpkg>.X`
  (engines, tools/batteries, analysis, launcher_support, macro_brain, bot, api, CLI, smoke)
- **Launcher slice**: `launcher.py` (13038→12273 linhas), novos
  `launcher_support/{cockpit_tab,command_center,dashboard_controls}.py` (1316 linhas extraídas)
- **Fix review**: `launcher.py` (fail-closed helpers), `tests/contracts/test_infra_contracts.py` (escape assert)
- **Docs**: `docs/audits/2026-04-19_workspace_faxina.md`
- **Session log**: `docs/sessions/2026-04-19_1553.md`, `docs/days/2026-04-19.md`

## Notas para o Joao

1. **VPS redeploy atenção**: JWT agora exige >=32 chars. Se
   `AURUM_JWT_SECRET` no VPS estiver curto, startup regenera e
   **invalida todas as sessões ativas**. Antes de redeployar o cockpit
   API, conferir o secret.

2. **Plan untracked deixado de propósito**:
   `docs/superpowers/plans/2026-04-19-millennium-paper-runner.md` já
   está commitado em `feat/millennium-paper` (`7047796`). Virá via
   merge quando a Claude A terminar — não comitar aqui pra não
   duplicar.

3. **Worktrees ativos** — não mexi em nenhum:
   - `.worktrees/millennium-paper` (Claude A, Task 2/15)
   - `.worktrees/lane-2b-hmm` (Claude B, Task 8 in-flight, H8 microbench)
   - `.worktrees/chronos-entropy`, `quantum-engine`, `safety-tests` (parados)

4. **Risco menor**: os 4 worktrees parados têm os imports legados
   (`core.fs`, `core.portfolio`, etc). Quando for mergear de volta, o
   resolvedor de conflitos vai precisar respeitar a migração.

5. **CORE PROTEGIDO não foi tocado em toda a sessão** — indicators,
   signals, portfolio, params intactos.
