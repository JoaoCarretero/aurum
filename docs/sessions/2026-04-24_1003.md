# Session Log — 2026-04-24 10:03

## Resumo
Criado hub de orientação no root do projeto (AGENTS.md, MEMORY.md, CONTEXT.md,
SKILLS.md) pra todas as sessões futuras (Claude, Codex, outros agentes) terem
entry point de ~5 min de leitura antes de mexer em código. Pointer bloc
adicionado no topo do CLAUDE.md apontando pros 4 files. 4 persona stubs criados
em `docs/agents/` pra evitar colisão com o markdown editor do RESEARCH DESK.
Script `check_hub_drift.py` criado pra detectar drift futuro entre hub docs e
repo real — zero drift após correções.

## Commits
| Hash | Mensagem | Arquivos |
|------|----------|----------|
| 13a3e79 | docs(hub): AGENTS/MEMORY/CONTEXT/SKILLS — orientation hub no root | AGENTS.md, MEMORY.md, CONTEXT.md, SKILLS.md, CLAUDE.md |
| 6f69746 | docs(hub): review fixes — engines deletados + persona stubs + AGENTS nota | AGENTS.md, MEMORY.md, CONTEXT.md, docs/agents/*.md |
| (pending) | docs(hub): add drift check + path fixes + session/daily log | tools/maintenance/check_hub_drift.py, MEMORY.md, CONTEXT.md, docs/sessions/, docs/days/ |

## Mudanças Críticas
Nenhuma mudança em lógica de trading. Zero edits em `core/indicators.py`,
`core/signals.py`, `core/portfolio.py`, `config/params.py`. Trabalho 100%
documentação + um novo tool de manutenção.

## Achados
Durante o audit dos 4 hub files criados, foram encontrados 3 drifts factuais
que foram corrigidos na mesma sessão:

1. **DE SHAW/KEPOS/MEDALLION/ORNSTEIN**: status em MEMORY.md estava "ARQUIVADO
   2026-04-22" mas foram **DELETADOS** na Fase 1 em 2026-04-23 (arquivos fonte
   removidos completamente da árvore). Corrigido pra "🗑️ DELETADO 2026-04-23
   (Fase 1)".

2. **engines/_archive/**: CONTEXT.md dizia que continha aqueles 4 engines
   deletados — na verdade só tem `meanrev.py`. Corrigido.

3. **tools/reconcile_runs.py**: referenciado como `python -m tools.reconcile_runs`,
   mas o script real vive em `tools/reports/reconcile_runs.py`. Corrigido em
   MEMORY.md e CONTEXT.md pra `python -m tools.reports.reconcile_runs`.

4. **Colisão UX latente**: `launcher_support/research_desk/markdown_editor.py`
   faz fallback pra `AGENTS.md` root se `docs/agents/<key>.md` não existir. Com
   AGENTS.md sendo agora hub de orientação (não persona file), Joao poderia
   editar o hub por engano ao clicar "Edit Persona" no detail modal do
   RESEARCH DESK. Mitigação: pré-criados 4 persona stubs (scryer/arbiter/
   artifex/curator) em `docs/agents/` + nota no preâmbulo do AGENTS.md.

## Estado do Sistema
- **Smoke test**: 172/172 passed
- **Drift check**: zero drift detectado após fixes
- **Backlog restante**: continua como no daily 2026-04-24 (validação clicável
  arb hub v2, BRIDGEWATER re-test OOS, pre-existing jump.py:89)
- **Próximo passo sugerido**: validar clicavelmente o Arb Hub v2 (pendência do
  daily 2026-04-24), ou iniciar BRIDGEWATER re-test agora que tem infra de
  check_hub_drift pra confirmar que o status na MEMORY.md está correto.

## Arquivos Modificados
### Criados
- `AGENTS.md` (+107 linhas) — quem opera o repo, subagents, RESEARCH DESK
  operatives, padrão de paralelismo 3-5 agents
- `MEMORY.md` (+171 linhas, depois +5/-5 fix) — fatos permanentes, CORE, keys,
  incidentes, status engines, anti-overfit protocolo, artifact locations
- `CONTEXT.md` (+350 linhas, depois fixes) — árvore de diretórios anotada,
  pipeline canônico, parâmetros, stack, comandos
- `SKILLS.md` (+233 linhas) — workflow discipline, gatilhos superpowers, fluxos
  AURUM (nova engine, bug hunt, refactor, engine validation, deploy, fim de
  sessão)
- `docs/agents/scryer.md`, `arbiter.md`, `artifex.md`, `curator.md` (+17 linhas
  cada) — persona stubs pra bloqueio do fallback do markdown_editor
- `tools/maintenance/check_hub_drift.py` (+180 linhas) — warn-only drift
  detector entre hub docs e repo real

### Modificados
- `CLAUDE.md` (+15 linhas) — bloco "📍 LEIA PRIMEIRO" no topo apontando pros 4
  hub files

## Notas para o Joao
- O hub foi feito pra **reduzir bootstrap tax** de sessões futuras. Hoje cada
  sessão nova gasta 10-15 min fazendo `ls`, `Read CLAUDE.md`, `Glob docs/`. Com
  os 4 files, leitura MEMORY + CONTEXT vira ~2 min e tem mapa completo.
- Mas o hub **fica stale se ninguém atualizar**. Regra de manutenção: toda vez
  que mexer em `config/engines.py` (adicionar/remover engine, mudar stage),
  rodar `python -m tools.maintenance.check_hub_drift` antes do commit. Se achou
  drift, atualizar MEMORY.md ou CONTEXT.md no mesmo commit.
- **Routine semanal configurada**: `trig_01DbcYa1K6GULVwoxDudhnZB`
  ("AURUM — Hub Drift Check (semanal)"). Cron `0 12 * * 1` = segundas 09:00
  Brazil. Comportamento: roda o drift check no repo remoto; se zero drift, só
  loga; se houver drift, abre PR `chore/hub-drift-<YYYY-MM-DD>` pra branch
  ativa com audit doc em `docs/audits/<YYYY-MM-DD>_hub_drift.md`.
  Gerenciar em https://claude.ai/code/routines/trig_01DbcYa1K6GULVwoxDudhnZB
- Persona stubs em `docs/agents/` são mínimos (persona + responsabilidades +
  when-to-pause). Podes editar via launcher RESEARCH DESK (tecla `n` → click
  card → Edit Persona) sem medo de sobrescrever o hub AGENTS.md no root.
- Branch `feat/research-desk` continua ativa. Merge strategy do RESEARCH DESK
  continua em aberto — 3 opções listadas no daily 2026-04-23: (a) PR →
  chore/repo-cleanup, (b) fast-forward, (c) PR → main. Não toquei nisso.

---

> Informação > matéria. Os 4 hub files não fazem AURUM rodar melhor. Fazem
> cada sessão que o toca entender mais rápido — e perder menos contexto no
> handoff entre humanos, Claude, Codex.
