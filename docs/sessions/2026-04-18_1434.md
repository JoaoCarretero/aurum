# Session Log — 2026-04-18 14:34

## Resumo

Sessão longa de audit + execução disciplinada. Começou com review do trabalho paralelo do Codex (que estava uncommitado no repo — core/sentiment, engines/bridgewater/deshaw/meanrev + novos tools + docs). Organizou em 5 commits lógicos. Depois fechou a Lane 1 (reorganização estrutural do `core/` que o Joao começou de manhã — Phase 3 finale + merge em `feat/phi-engine` + fix post-merge). Integrou os 10 safety tests de live/janestreet que estavam parkados em branch separado. Auditou a Lane 1 pós-merge e achou 3 bugs silenciosos (funding_scanner path anchor + lookahead_scan lendo shims + BRIEFINGS_V2 mostrando shim code) + ~25 user-facing paths quebrados em docstrings/help text. Todos fixados num commit. Push de 37 commits pro origin. Iniciou Lane 2 (velocidade): rodou profile pyinstrument em 3 cenários, achou que **HMM fitting consome 43% do wall-time de CITADEL 180d**, mas depois descobriu que a **H1 (ProcessPool) foi mal interpretada** — o orquestrador da battery já usa `subprocess.call` por engine, então ThreadPool atual está correto. Errata publicada, Lane 2 parou honestamente sem fix. Fechou com 7 safety contract tests pra `engines/jump.py` via subagent-driven.

## Commits

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| a831eae | fix(sentiment): harden cache loader + merge_asof against pandas 3.14 | core/sentiment.py, test |
| a05a47d | feat(deshaw): runtime snapshot reflects CLI overrides + pairs battery | engines/deshaw.py, test, tools/deshaw_pairs_battery.py, docs/engines/deshaw/ |
| 05a66ae | feat(meanrev): enforce mean-rev thesis — reversal_bar default + 3 grids | engines/meanrev.py, tests, tools (3), docs/engines/meanrev/ (3) |
| f66cde8 | feat(bridgewater): robust preset default — funding+LS disciplined | engines/bridgewater.py, tests, tools/bridgewater_compare_battery.py |
| c2f8e9b | docs(audits): 2026-04-17 Claude + Codex day summary | docs/audits/2026-04-17_claude_codex_summary.md |
| 67cecbe | refactor(core): migrar core.analysis/ (2 modulos) com shims sys.modules | core/analysis/{analysis_export,evolution}.py + shims |
| 90265d1 | docs(core): README mapa subpacotes + rationale signals FLAT | core/README.md |
| ca305be | merge: Lane 1 organizacao estrutural — 6 subpacotes em core/ + tests/tools reorg | merge --no-ff + conflict resolution |
| f388188 | fix(tests): atualizar import tools.millennium_shadow pos-Lane 1 merge | tests/test_shadow_manifest_write.py |
| 54669f4 | test(safety): integrate live + janestreet safety contract tests | tests/contracts/test_{live,janestreet}_contracts.py |
| e148907 | fix(lane1): audit findings — path anchors + stale refs pos-reorganizacao | 28 files, 51/51 linhas |
| 4bb0b7b | docs(audits): Lane 2 Fase A — battery perf profile (HMM fit dominates) | docs/audits/2026-04-18_battery_perf_profile.md |
| b93a84e | docs(audits): Lane 2 Fase A — ERRATA, H1 mal-interpretada | docs/audits/2026-04-18_battery_perf_profile.md |
| 30d4ed4 | test(jump): safety contract tests — order flow + microstructure | tests/contracts/test_jump_contracts.py |

**14 commits nesta sessão** (+ commits paralelos do Joao na lane sidebar/popup).

## Mudanças Críticas

**Nenhuma em lógica de trading.** Zero linhas em `core/indicators.py`, `core/signals.py`, `core/portfolio.py`, `config/params.py`.

Verificado: `git diff 624802d..HEAD -- core/indicators.py core/signals.py config/params.py` = vazio. `core/portfolio.py` foi renomeado para `core/risk/portfolio.py` via Lane 1, mas conteúdo idêntico (confirmado via diff do shim + real).

**Atenção:** 2 engines tiveram default mudado por propósito do trabalho do Codex (já auditado e aprovado):
- `engines/meanrev.py` — default `entry_mode="reversal_bar"` (era touch implícito). Mean-rev puro agora, V12 breakout-disfarçado desativado.
- `engines/bridgewater.py` — default `--preset robust` (funding+LS disciplined, OI disabled). Materializa achado 2026-04-17 de que OI forward-only era bug.

Ambas mudanças têm legacy preset pra reprodução de históricos. Documentado nos commit bodies.

## Achados

### Lane 1 pós-merge — 3 bugs silenciosos

1. **`core/ui/funding_scanner.py:64`** — path anchor `parent.parent` ficou stale após mudança de depth (agora em `core/ui/`). `_ROOT` resolvia pra `core/` em vez de repo root. `_ALERT_LOG_PATH` e `config/keys.json` load quebrados. Silent — sem exception.

2. **`tools/audits/lookahead_scan.py:19-20`** — TARGETS apontava pra shims de 6 linhas (`core/htf.py`, `core/portfolio.py`). Scanner de lookahead lia shim em vez do código real → zero patterns detectados nesses 2 arquivos.

3. **`launcher.py:649-653`** — BRIEFINGS_V2 CITADEL `source_files` mostrava shim no CodeViewer em vez do código real.

Mais ~25 docstrings/help text com `python tools/X.py` apontando pra paths antigos (pós-reorganização em subdirs).

### Lane 2 Fase A — H1 mal-interpretada

Primeira versão do report concluiu "H1 CONFIRMAR → ProcessPool em batteries". **Errado.** Depois de ler `tools/batteries/longrun_battery.py:67` direito:

- O orquestrador dispara cada engine via `subprocess.call` (processo separado)
- ThreadPoolExecutor só orquestra — threads ficam esperando subprocess terminar
- Threads **não contestam GIL** aqui — trabalho CPU-bound está dentro de cada subprocess
- O 82% CPU do profile era **CITADEL standalone**, não o orquestrador

ProcessPool seria overhead pra nada. Lane 2 parou honestamente sem fix. Errata publicada em `b93a84e`.

### HMM fitting dominates (profile)

`GaussianHMMNp.fit` em `core/chronos.py:174` = **43% do wall-time** de CITADEL 180d (`_forward` + `_backward` via `scipy.special.logsumexp`). Não estava nas 6 hipóteses pré-registradas. **Registrado sem atacar** per protocolo anti-overfit (hipótese não antecipada = ciclo próprio com pre-registro).

Ideias pra ciclo futuro:
- **H7**: `@lru_cache` em `fit()` por hash dos dados (se chamadas redundantes existirem)
- **H8**: substituir `scipy.special.logsumexp` por implementação vetorizada local

Ambas ficam parkadas até novo pre-registro formal.

## Estado do Sistema

- **Suite:** 1282 passed, 7 skipped (+27 do morning — ~20 das sessões paralelas do Joao na sidebar + 7 meus do jump)
- **CORE:** zero linhas tocadas
- **Lane 1:** ✅ merged + audited + fixed (Phases 0-3 completas; Phase 4 launcher split parkada)
- **Lane 2:** Fase A concluída, honestidade: zero fix aplicável com o profile atual. Próxima lane precisa pre-registro novo
- **Branches:** `feat/phi-engine` @ 30d4ed4, pushed até b93a84e (1 commit local não-pushado: 30d4ed4)

**Engine test coverage atualizado:**
- ✅ citadel, bridgewater, deshaw, phi, kepos, graham, meanrev, ornstein, millennium, millennium_live, live, janestreet, **jump**
- ❌ Restam 4: aqr, medallion, renaissance, twosigma

### Backlog restante

1. 4 engines sem teste (aqr, medallion, renaissance, twosigma) — ~30min cada via subagent
2. H7/H8 HMM optimization — precisa pre-registro formal + profile dedicado (contar chamadas de `enrich_with_regime`, medir se cache tem retorno)
3. Lane 1 Phase 4 — launcher split 13k linhas, parkada
4. Lane 3 — workflow (plans prontos, não iniciada)
5. Push do último commit (30d4ed4 jump tests ainda local)

## Arquivos Modificados

Principais (por categoria):

**Codex work organizado (5 commits):**
- core/sentiment.py (+14)
- engines/deshaw.py (+33), tests/test_deshaw_contracts.py, docs/engines/deshaw/, tools/deshaw_pairs_battery.py
- engines/meanrev.py (+194), tests, tools/meanrev_variant_search.py + 2 novos, docs/engines/meanrev/ (3 grids)
- engines/bridgewater.py (+298), tests, tools/bridgewater_compare_battery.py
- docs/audits/2026-04-17_claude_codex_summary.md

**Lane 1 finale + merge (4 commits):**
- core/analysis/{analysis_export,evolution}.py (movidos) + shims no core/ root
- core/README.md (mapa subpacotes + rationale signals FLAT)
- 18 commits de `lane-1/organizacao-estrutural` integrados via merge --no-ff
- tests/test_shadow_manifest_write.py (1 linha fix)

**Safety tests integrados (1 commit):**
- tests/contracts/test_live_contracts.py (6 tests)
- tests/contracts/test_janestreet_contracts.py (4 tests)

**Lane 1 audit fixes (1 commit, 28 files):**
- core/ui/funding_scanner.py (path anchor)
- tools/audits/lookahead_scan.py (TARGETS)
- launcher.py (BRIEFINGS_V2)
- launcher_support/engines_live_view.py (help text)
- engines/{live,bridgewater,millennium}.py (error messages)
- tests/contracts/test_janestreet_contracts.py (path anchor — regressão da integração)
- 25 tools/*/*.py (docstrings self-referenciais)

**Lane 2 Fase A (2 commits):**
- docs/audits/2026-04-18_battery_perf_profile.md (criado + errata)
- data/perf_profile/2026-04-18/ (gitignored)

**Jump tests (1 commit):**
- tests/contracts/test_jump_contracts.py (+265, 7 tests)

## Notas para o Joao

1. **Lane 2 ficou negativa com integridade.** Nenhum fix implementado — a hipótese H1 que parecia validada na primeira leitura do profile caiu quando li o `longrun_battery.py` direito. Orquestrador já faz paralelização correta via subprocess. Ganho real está **dentro** de cada engine (HMM fit), mas isso precisa pre-registro próprio. Não forcei.

2. **O audit do Lane 1 vale releitura.** Os 3 bugs silenciosos (funding_scanner, lookahead_scan, BRIEFINGS) passaram despercebidos pela suite porque:
   - funding_scanner `_ROOT` é usado só em runtime (alert log load + keys.json read) — silent
   - lookahead_scan lê source file como texto pra regex — sem exception, só resultados errados
   - BRIEFINGS CodeViewer mostra conteúdo textual — UX regression, não crash
   Padrão comum: **reads que não eram exercitados por nenhum teste**. Vale pensar em uma contract test que garanta que paths em listas tipo TARGETS/source_files existam e não sejam shims.

3. **`engines/meanrev` e `engines/bridgewater` tiveram default behavior mudado.** Qualquer run reproduz-histórico precisa explicitar preset:
   - `python engines/meanrev.py ... --entry-mode touch` (se quiser V10-V22 antigo)
   - `python engines/bridgewater.py ... --preset legacy` (se quiser comportamento pré-2026-04-17 audit)

4. **Não pushei o último commit (30d4ed4 jump tests).** Decisão sua.

5. **4 engines ainda sem teste.** Pattern estabelecido via subagent + 5-7 contracts. Posso rodar os 4 em paralelo próxima sessão (~30min wall-clock total) ou sequencial.

6. **Worktrees pra limpar quando quiser:**
   - `.worktrees/cockpit-api` (merged via ec805f4)
   - `.worktrees/launcher-tunnel` (merged via ddfdb0b)
   - `.worktrees/lane-1-organizacao` (merged via ca305be agora)
   - `.worktrees/safety-tests` (conteúdo integrado via 54669f4, branch local pode deletar)

7. **Nome do meu branch ainda é `feat/phi-engine`.** Ele acumulou muita coisa (Lane 1 merged, cockpit merged, safety tests, audit fixes). PR pra `main` quando quiser. 38+ commits ahead agora.
