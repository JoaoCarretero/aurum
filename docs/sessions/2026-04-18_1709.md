# Session Log — 2026-04-18 17:09

## Resumo

Cinco polish/feature fixes no cockpit SHADOW e layout redistribution. Uptime
real calculado de timestamps (antes era arg CLI estatico), OPEN CHART no popup
de trade abrindo TradingView, READY TO LAUNCH comprimido, Telegram por sinal
novo, e cada engine do shadow ganhou seu proprio run folder em
`data/<engine>_shadow/<RUN_ID>/` espelhando layout de backtest. Depois layout
redistribuido (38/62 → 28/72 master/detail) com HEADER vivo (pulse dot),
HEALTH como 4 metric cards grandes e signals table com accent fresh no mais
recente. Dois commits, audit 204/204 passando.

## Commits

| Hash    | Mensagem                                                                | Arquivos |
|---------|-------------------------------------------------------------------------|----------|
| d943c00 | feat(shadow): uptime real + trade chart + Telegram por sinal + per-engine run dirs | launcher_support/engines_live_view.py, engines_sidebar.py, signal_detail_popup.py, tools/maintenance/millennium_shadow.py |
| 3e6c879 | style(cockpit): layout mais bem distribuido + engine data mais vivo     | launcher_support/engines_live_view.py, engines_sidebar.py |

## Mudancas Criticas

Nenhuma mudanca em logica de trading. Todos os edits sao na camada UI
(cockpit launcher) ou em observabilidade (shadow runner — Telegram alerts e
layout de output em disco). Zero linhas em `core/indicators.py`,
`core/signals.py`, `core/portfolio.py`, `config/params.py`.

## Achados

- **Bug uptime shadow:** sidebar exibia `run_hours` (arg CLI estatico, ~24h
  fixo) como se fosse uptime elapsed. Real uptime eh
  `(stopped_at | last_tick_at | now) - started_at`. Fix em
  `engines_sidebar.py::_uptime_from_heartbeat`.
- **Shadow agregava trades num so JSONL** (`data/millennium_shadow/<RUN>/
  reports/shadow_trades.jsonl`), dificultando navegacao per-engine. Agora
  cada engine ganha seu proprio run dir com layout simetrico a backtest.

## Estado do Sistema

- **Audit UI:** 204 passed em sidebar+popup+cockpit+live_view+shadow+millennium
- Headless Tk render de `render_detail` com heartbeat mock: 77 widgets sem
  exception
- **Backlog restante:**
  - Phase 4 Lane 1 (launcher split 13k → modulos, 2-3h GUI manual)
  - Flicker no re-render 5s do shadow (polish, in-place configure)
  - VPS settings UI dentro do launcher
  - Rotacionar tokens expostos em chat
  - 5 engines sem teste (jump, renaissance, twosigma, aqr, medallion)
- **Proximo passo sugerido:** monitorar shadow 24h+ com as novas per-engine
  dirs preenchidas e o Telegram disparando pra validar signal/noise.

## Arquivos Modificados

- `launcher_support/engines_live_view.py` — split 38/62→28/72, compress READY
  row (font 9→8, padx tightened) — +5/-5 lineas
- `launcher_support/engines_sidebar.py` — `_uptime_from_heartbeat`,
  `_metric_card` helper, HEADER pulse dot + bigger font, HEALTH metric cards,
  LAST SIGNALS fonte 6→7 + accent bar amber na row mais recente — +85/-15
- `launcher_support/signal_detail_popup.py` — botao OPEN CHART que abre
  TradingView `BINANCE:<SYM>.P 1h`, `_tradingview_url` helper — +24/-3
- `tools/maintenance/millennium_shadow.py` — `_tg_signal` per novel trade
  (primeiro tick prima silenciosamente), per-engine run dirs em
  `data/<engine>_shadow/<RUN_ID>/reports/trades.jsonl` + state/manifest.json
  + summary.json — +113/-4

## Notas para o Joao

- **Ainda ha mudancas do Codex no working tree** (`launcher.py` +169 linhas,
  `docs/audits/...`, `docs/days/2026-04-18.md`, `tests/integration/
  test_launcher_main_menu.py`, `docs/sessions/2026-04-18_1549.md`) — nao
  toquei. Decida se commita, revisa ou descarta.
- **Telegram per-sinal ja esta ativo** em qualquer shadow run novo (tick 2+).
  Primeiro tick silencia pra nao floodar o chat com historico que seeda o
  dedup set. Silent fallback se `config/keys.json` nao tem
  `telegram.bot_token` + `chat_id`.
- **Per-engine run dirs:** proximo shadow run vai criar
  `data/citadel_shadow/`, `data/jump_shadow/`, `data/renaissance_shadow/`
  automaticamente quando cada engine disparar seu primeiro sinal. Agregado
  antigo (`data/millennium_shadow/.../reports/shadow_trades.jsonl`) continua
  sendo escrito — backcompat preservada.
- **OPEN CHART** so aparece pra symbols USDT; outros symbols escondem o botao
  (evita URL quebrada).
- Layout novo (28/72) pode parecer estreito na sidebar — a ideia foi deixar
  os 4 metric cards respirarem no detail. Se ficar apertado demais em
  monitor pequeno, volto pra 32/68.
