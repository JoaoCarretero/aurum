# Session Log — 2026-04-25 15:00

## Resumo

Audit do trabalho do Codex de manhã + 3 fases consecutivas de cleanup geral do repo (Tier A + B + C parcial). Encontrei e corrigi 2 regressões críticas que o Codex deixou (paper runner indent + ssh_tunnel reaper deletado), integrei trabalho deferido de 2 branches (cockpit-trade-chart + foundation-hardening parcial + cockpit-all-engines parcial), arquivei 57 plans/specs já enviados, droppei 3 branches obsoletas. Outro Claude rodou em paralelo (lane paper cooldown + DB sync VPS + website branding + shadow scripts) — coordenado sem conflito.

## Commits (12 desta sessão)

| Hash | Mensagem | Arquivos |
|------|----------|----------|
| `be4dd99` | fix(paper-runner): per-engine runners — indent + missing scan helper | _paper_runner.py, engines/millennium.py +helper, tests/test_scan_one_engine_live.py NEW |
| `4d432dd` | fix(ssh-tunnel): restore netstat_pid_dead orphan reaper | launcher_support/ssh_tunnel.py |
| `7f16d34` | test(paper-runner): extend _isolate_runner_db helper to all monkey-patched tests | tests/integration/test_paper_runner_{ks,tick}.py |
| `c74e77b` | chore(cleanup): drop one-shot diagnostics + archive _revalidation outputs | tools/diagnostics/* deleted, docs/audits/_archive/ |
| `d8ae63a` | Merge feat/cockpit-trade-chart — mplfinance chart popup + trade history panel | 10 files merged |
| `4734f2c` | feat(audit): daily script comparando backtest replay vs live trades | tools/debug/audit_live_vs_backtest_daily.py NEW |
| `f521ba7` | fix(audit): review final findings — window cutoff + direction norm + systemd | deploy/aurum_audit_daily.{service,timer}, install_audit_daily.sh |
| `27127a2` | fix(core): resolve submodule-vs-function shadow collision in lazy __getattr__ | core/__init__.py +23 |
| `9e93490` | feat(ops): zombie scanner — telegram alert quando runner fica hung | tools/maintenance/zombie_scanner.py NEW (232 lines) + 8 tests |
| `5487cf1` | deploy(zombie-scanner): systemd service + timer | deploy/zombie_scanner.{service,timer} NEW |
| `b46fda3` | feat(backtest-config): adiciona 360d/720d presets + entry editavel | launcher.py +3, launcher_support/screens/config_backtest.py +33 |
| `4704242` | chore(docs): archive 57 shipped plans + specs to _completed/ | 28 plans + 29 specs renamed |

Outro Claude paralelo committou nesta janela: `7fd22e3` (SYM_LOSS_COOLDOWN runtime), `a2b2cea` (DB sync VPS), `c4d8942` (session log), `791168a` (3 shadow scripts citadel/jump/renaissance). Sem conflito com meus commits.

## Mudanças Críticas

**ATENÇÃO: 2 regressões do Codex em `d168748` foram corrigidas:**

1. **`tools/operations/_paper_runner.py:406` IndentationError** — Codex deixou indent quebrado em `_flatten_all`. Resultado: `python -m py_compile` retornava exit 1, **TODOS os 3 per-engine paper runners (citadel/jump/renaissance) crashavam no boot com ImportError**. Felizmente o VPS estava em `5dfa479` (HEAD anterior), não pegou esse commit.

2. **`launcher_support/ssh_tunnel.py` reaper deletado** — Codex deletou 35 linhas do orphan ssh.exe reaper (`netstat_pid_dead` fallback) que o Claude tinha adicionado em `19dcb01` esta manhã. Codex labeled "close audit regressions" mas reintroduziu uma. Restaurado verbatim.

**+ 1 bug crítico complementar:**

3. **`engines.millennium._scan_one_engine_live` faltava** — `_paper_runner.py` chamava `from engines.millennium import _scan_one_engine_live` em `_scan_new_signals` (lazy import), mas a função NUNCA foi adicionada a `engines/millennium.py`. Per-engine paper runners crashariam na primeira tick com `ImportError`. Adicionei a função (22 lines) + 6 contract tests.

**+ 1 fix CORE-adjacent (mas NÃO no protected list):**

4. **`core/__init__.py` shadow-collision fix** (port manual de `9f2d582`). Resolvia bug `engines/jump.py:89` onde `from core import indicators` retornava o módulo em vez da função sob certas ordens de import — `_resolve_shadow_collisions()` força resolução eager.

Zero toque em `core/{indicators,signals,portfolio}.py` ou `config/params.py`. CLAUDE.md compliance ok.

## Achados

- **Codex deixou test isolation incompleto** em `test_paper_runner_tick.py`: adicionou `_isolate_runner_db` helper mas só plugou em 2 das 7 funções de teste. Estendi pras 5 restantes — antes os testes vazavam writes pra `data/aurum.db` real.
- **`feat/per-engine-runners` totalmente subsumido** por `feat/cockpit-all-engines` — único commit realmente único era cópia (`a9a4c8c`) de `b2c3d60` que já estava em research-desk.
- **`feat/engines-frontend-rebuild` foi walked-back** via commit `4535e23 chore: delete unused frontend-rebuild package` (2026-04-23) que removeu 4836 linhas. Branch obsoleta — refactor R3-R5 foi rejeitado por UX feedback após port pra research-desk.
- **`stash@{0}` cockpit engine controls v2** era REDUNDANTE — `VPS_CANONICAL_SERVICES` tuple já estava em `engines_live_view.py:3972` via outra rota.
- **Outro Claude implementou** o `SYM_LOSS_COOLDOWN` que eu deixei como TDD failing-test (`test_paper_runner_sym_cooldown.py`) — `7fd22e3 feat(paper)`. Coordenação invisível mas eficaz.
- **Outro Claude trouxe shadow runners** (citadel/jump/renaissance + `_shadow_runner.py` template) em `791168a` — eram itens que eu havia identificado em `cockpit-all-engines` e deferido por conflict risk; eles fizeram cherry-pick cirúrgico de só 4 NEW files.

## Estado do Sistema

- **Smoke**: 172/172 ✅
- **Tests collected**: 2431 (+~159 desta sessão: zombie 8 + chart 65 + scan_one_engine 6 + audit + outros)
- **CORE intacto** ✅ — `core/{indicators,signals,portfolio}.py`, `config/params.py`
- **`config/keys.json` íntegro** ✅
- **Branches local**: **6** (era 15 no início do dia)
- **Branches origin**: **6** (era 30)
- **Stashes**: **1** (era 4) — só BW hypothesis 2026-04-23 sobra
- **Worktrees**: **3** (era 6)
- **Plans active**: **8** (era 36) — 28 → `_completed/`
- **Specs active**: **9** (era 38) — 29 → `_completed/`

### Trabalho integrado em research-desk nesta sessão

| Origem | O que veio |
|--------|------------|
| `feat/cockpit-trade-chart` | mplfinance chart popup + trade history panel + 65 tests (MERGE completo) |
| `feat/cockpit-all-engines` | audit_live_vs_backtest_daily script + systemd timer + core/__init__.py shadow fix |
| `feat/foundation-hardening` | zombie_scanner.py + tests + systemd files |
| `fix/shadow-tick-cadence` | (via outro Claude em paralelo) 4 shadow runner files |
| `feat/per-engine-runners` (c4ec68b) | 360d/720d backtest presets + custom days entry |

### Trabalho deferido (não integrado, com razão clara)

| Item | Razão |
|------|-------|
| `cockpit-all-engines` paper file mods (eb7c375, eb4be51, fe90ea6, cc44927, f819cfa, etc.) | Conflito direto com paper lane que outro Claude tocou em `7fd22e3` |
| `cockpit-all-engines` harmonics warm-up (`fe90ea6`) | `core/harmonics.py` é CORE-adjacent (afeta RENAISSANCE backtest); decisão exige audit dedicado |
| `engines-frontend-rebuild` (já dropada) | Refactor walked-back via `4535e23` |
| `website-refinement` 5 commits | Outro Claude editando os mesmos `server/website/*` files ativamente |

## Arquivos Modificados (este branch vs main)

**217 files changed, +20598 / -1390 lines** (cumulativo de research-desk vs main)

Principais (somente desta sessão):
- `tools/operations/_paper_runner.py` — indent fix + scan_helper import
- `engines/millennium.py` — `_scan_one_engine_live` helper added (+22)
- `core/__init__.py` — `_resolve_shadow_collisions` (+23)
- `launcher_support/ssh_tunnel.py` — reaper restored (+35)
- `tests/test_scan_one_engine_live.py` — NEW (6 contract tests)
- `tests/tools/test_zombie_scanner.py` — cherry-pick from foundation-hardening
- `tools/maintenance/zombie_scanner.py` — cherry-pick (232 lines)
- `tools/debug/audit_live_vs_backtest_daily.py` — cherry-pick (322 lines)
- `deploy/aurum_audit_daily.{service,timer}`, `deploy/install_audit_daily.sh` — NEW
- `deploy/zombie_scanner.{service,timer}` — NEW
- `launcher_support/{trade_chart_popup,trade_history_panel}.py` — via merge cockpit-trade-chart
- `launcher_support/screens/config_backtest.py` — 720d preset (+33)
- `docs/audits/_archive/_revalidation_*.txt` (4 moved)
- `docs/superpowers/{plans,specs}/_completed/` — 57 files moved

## Notas para o Joao

1. **`stash@{0}` BW hypothesis (153 lines docs)** sobra — única decisão pendente. Ler conteúdo (`git stash show -p stash@{0}`) e decidir pop/drop quando der.

2. **Branches restantes (6)** todas com razão clara:
   - `feat/cockpit-all-engines` + `fix/shadow-tick-cadence` — paper file mods + harmonics warmup pendentes (high conflict). Revisitar quando outro Claude estabilizar lane paper.
   - `feat/website-refinement` — outro Claude está vivo na lane.
   - `experiment/capula` — engine WIP.

3. **Working tree pendente** (não-meu, do outro Claude — não toquei):
   - 9 files `server/website/*` (Reveal, StatusTicker, EquityCurve, tokens, Hero, Performance, styles, BRAND.md NEW, IridescentCard.jsx NEW)
   - 3 files DB sync work (sync_vps_db, _shadow_runner _DB_HOOK, test_sync_vps_db, test_shadow_runner_db_hook)

4. **Coordenação multi-Claude funcionou bem** — outro Claude implementou meu TDD `test_paper_runner_sym_cooldown.py` enquanto eu auditava Codex; eles cherry-pickaram shadow runners enquanto eu archivava plans. Nenhum conflito de branches/files.

5. **Audit de hoje confirmou regra de ouro 2026-04-15**: Codex tentou de novo um padrão suspeito (deletar fix do Claude alegando "close audit regressions") + deixou bug crítico de indent. CLAUDE.md proteção funcionou — verifiquei tudo antes de aceitar, achei e revertí.

> "O ruído é o inimigo." — `d168748` parecia "fix audit regressions" mas continha 2 regressões reais. Detectado pelo audit + smoke + py_compile, não pelo CI.
