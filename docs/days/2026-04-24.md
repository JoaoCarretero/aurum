# Daily Log — 2026-04-24

## Sessões do dia
- 15:44 — **Cockpit sidebar redesign + trade history + DB audit** (Opus 4.7 1M, `feat/research-desk`): 11 commits. **Sidebar**: widening 24→30 c/ minsize 260px, single-line 4-col grid (accent+badge 2ch+name+metric 7ch), bucket headers usam o mesmo grid das rows pra alinhamento per-pixel, uptime per engine enriquecido do cache VPS cockpit. **Trade history paper**: fetch `/v1/runs/{id}/trades` via VPS API (antes passava `trades=[]` hardcoded); cross-run aggregator (`_fetch_paper_engine_trades_sync`) agrega closed trades de runs stopped c/ dedupe por `(ts, symbol, strategy)` + filter `novel_total > 0` + cap 15 em janela de 50 runs. **DB audit**: `backfill_live_runs.py --from-vps` puxou 156 runs VPS, DB 8 → 157 rows, cobertura expandida pra todas engines (antes só millennium); `cleanup_orphan_run_dirs.py` archivou 588 smoke-test leftovers em `data/_archive/` (reversível, 27MB); `live_engines_audit.py` read-only pra health check VPS↔DB↔disk. Ticker perdido recuperado: **OPUSDT** LONG +$0.58 (millennium paper 13:48 UTC). Zero CORE. 2034 tests. — [2026-04-24_1544](../sessions/2026-04-24_1544.md)
- 16:00 — **Shadow/paper ↔ backtest aderência 100%** (Opus 4.7 1M, `fix/shadow-tick-cadence`): 8 commits consertando tick alignment (candle-aligned), tolerance 45min, bayes warm-up em `scan_hermes` live_mode, fix do jump.py:89 pre-existing bug (shadow collision no lazy `__getattr__`), entropy_norm observability, feature snapshot, daily audit cronjob no VPS (23:00 UTC), 3 rounds de code review consecutivos aplicados. Aderência estrutural 3/3 engines (RENAISSANCE + CITADEL + JUMP agora TODOS PASS). VPS patched contra `unattended-upgrades`. 1090 tests pass. — [2026-04-24_1600](../sessions/2026-04-24_1600.md)
- 11:24 — **Cockpit fills + staleness filter + DB VACUUM** (Opus 4.7 1M, `feat/research-desk`): extensão da sessão de 10:34 — 7 commits. (1) detail pane fills vertically em shadow (`actions.pack(side="bottom")` + paper spacer); (2) empty signal-feed label centrado (`pack(expand=True)`); (3) app footer pinned bottom (`side="bottom"`); (4) `_hide_app_footer`/`_show_app_footer` API — engines_live usa rodapé próprio; (5) restored `body.grid_rowconfigure(0, weight=1)` em `_apply_master_layout`; (6) `is_run_stale`/`resolve_status` em `core/ops/run_catalog` — filtra zumbis em /data engines + cockpit counters; (7) audit aurum.db: dedupe `cleanup_stale_rows`, VACUUM **426 MB → 448 KB**, `auto_vacuum=INCREMENTAL`. 450 targeted tests + 172 smoke verde. — [2026-04-24_1124](../sessions/2026-04-24_1124.md)
- 10:34 — Polish live cockpit sidebar (Opus 4.7 1M): cleanup do refactor do codex (420 linhas de `render_sidebar` órfã + 3 row renderers duplicados), unificação em `_render_nav_row`, collapse do master pane inteiro (`C`), header master alinhado com `_hl2_header`. — [2026-04-24_1034](../sessions/2026-04-24_1034.md)
- 10:03 — **Hub de orientação + alignment multi-pass** no root (AGENTS.md + MEMORY.md + CONTEXT.md + SKILLS.md + pointer no CLAUDE.md + README.md com pointer pros 5 hub files) pra reduzir bootstrap tax de sessões futuras. 5 persona stubs em `docs/agents/` (scryer/arbiter/artifex/curator + oracle externo) bloqueando fallback do markdown_editor do research_desk. `tools/maintenance/check_hub_drift.py` (warn-only, 7 checks) detecta drift entre hub docs e repo real. 7 drifts corrigidos durante 3 passes de audit: engines DELETADOS vs arquivados, _archive content, reconcile_runs path, colisão UX markdown_editor/AGENTS.md, README.md com 4 engines deletados referenciados, CONTEXT.md hawkes note, alignment 5 operativos (ORACLE novo + TIPO 1/2 pipeline externo integrado). Routine remota semanal `trig_01DbcYa1K6GULVwoxDudhnZB` (segundas 09:00 Brazil) roda drift check automaticamente. Zero mudança em CORE. 5 commits. — [2026-04-24_1003](../sessions/2026-04-24_1003.md)
- 09:00 — Arbitrage Hub v2 (density layout) — **11/12 tasks do plano shipped** via subagent-driven-development (Task 12 foi audit, passou); merge do chore/repo-cleanup; 14 commits + 1 merge em feat/arb-hub-v2. — [2026-04-24_1400](../sessions/2026-04-24_1400.md)

## Entregas principais (consolidado)
- **🎯 ADERÊNCIA 100% SHADOW/PAPER ↔ BACKTEST** (branch `fix/shadow-tick-cadence`, 8 commits): sessão massiva fechando o loop estrutural entre live trading e backtest. (1) **Tick alignment** — novo `seconds_until_next_tick_boundary` alinha loops de tick com candle grid UTC + 60s post-close; evita cadência aleatória pós-restart. (2) **Tolerance stale 2.0→3.0** (30min→45min) + wrapper defaults deletados; fim de shadow/paper em fases diferentes do candle. (3) **Bayes warm-up em `scan_hermes` live_mode** — processa histórico completo pra popular `_BayesWR` antes da emission tail; fim de score divergindo ±0.29 live vs backtest. (4) **`entropy_norm` persistido** no trade record + feature snapshot (pattern, hurst, h_regime, score, rr) em `signals.jsonl` de todos os 4 decisions (opened/stale/max_open/direction_conflict). (5) **fix pre-existing `jump.py:89`** via `_resolve_shadow_collisions` no `core/__init__.py` — `from core import indicators` agora retorna função (antes retornava módulo por PEP 562 race com Python import). (6) **Daily audit cronjob** `aurum_audit_daily.timer` no VPS (23:00 UTC) compara backtest replay 15d vs shadow_trades+signals+trades.jsonl, envia resumo via Telegram, gera JSON artifact. (7) **VPS `needrestart.conf` patched** pra não auto-restartar services aurum em upgrades (aurum/citadel/jump/renaissance/millennium/janestreet/bridgewater/phi/twosigma/aqr/winton). (8) 3 rounds de code review consecutivos aplicados, 5 issues critical/important fixados in-branch. **Tests**: 1090 pass + 72 skip. **Adherence structural 3/3 engines** (RENAISSANCE + CITADEL + JUMP). Audit baseline: RENAISSANCE 41.7% match (7 missed pré-fix, esperado subir pra 100% em 48-72h). Backlog: CITADEL/JUMP path-dependent state warm-up (task #25).
- **Hub de orientação AURUM** (4 novos files no root + infra de drift detection): AGENTS.md (quem opera), MEMORY.md (fatos permanentes + CORE + keys.json + incidentes + anti-overfit), CONTEXT.md (mapa de diretórios + pipeline + comandos), SKILLS.md (workflows). Pointer "LEIA PRIMEIRO" no topo do CLAUDE.md. 4 persona stubs em `docs/agents/{scryer,arbiter,artifex,curator}.md` pra bloquear fallback do `markdown_editor.py` do research_desk (colisão UX latente: sem stubs, Joao clicando "Edit Persona" abriria o hub AGENTS.md root). `tools/maintenance/check_hub_drift.py` warn-only: 6 checks (engine count, CORE protection, referenced commands/docs exist, hub sections, persona stubs). Zero drift após fixes. Audit achou e corrigiu 3 drifts: (a) DE SHAW/KEPOS/MEDALLION/ORNSTEIN marcados "arquivado 2026-04-22" → corrigido "DELETADO 2026-04-23 Fase 1" (arquivos fonte removidos); (b) `engines/_archive/` dito conter os 4 deletados → só tem `meanrev.py`; (c) `python -m tools.reconcile_runs` → `python -m tools.reports.reconcile_runs` (script real). Smoke 172/172, zero mudança em CORE.
- **Live cockpit sidebar polida** (branch `feat/research-desk`): removi 420 linhas de código órfão em `engines_sidebar.py` (`render_sidebar` inalcançável desde `d9e5db6`) + 3 row renderers duplicados em `engines_live_view.py` deixados pelo codex. Unifiquei LIVE/READY/RESEARCH em um `_render_nav_row` com alinhamento por `width=N` em chars (pixel-perfect cross-bucket). Adicionei collapse do master pane inteiro (chevron `⟨` + atalho `C`) com rail compacto que mostra contadores por bucket. Master handle redesenhado pra casar com `_hl2_header` (mesma altura, pady, separador 1px). Canvas bg → `PANEL` pra cessar o "rasgão preto" abaixo das rows. Bucket `LIVE` renomeado pra "RUNNING" (elimina duplicação com o handle). 172/172 smoke verde, 64/64 targeted.
- **Cockpit fills + staleness + DB VACUUM** (extensão da polish sidebar): 7 commits atacando três frentes. **Tela enquadrada**: (a) detail pane em shadow agora fills vertically — `actions.pack(side="bottom")` + `sig_box.expand=True` faz o REFRESH sentar no rodapé do detail_frame; (b) empty signal-feed label centralizado via `pack(expand=True)` sem fill; (c) paper mode ganhou trailing spacer pra absorver leftover vertical; (d) app footer (`v2.0 + f_lbl`) pinned ao bottom da janela via `side="bottom"` — antes caía no topo do screens_container por causa de reorder pack_forget/pack; (e) engines_live oculta app footer em on_enter (tem rodapé próprio rico com TUNNEL UP), restaura em on_exit; (f) `body.grid_rowconfigure(0, weight=1)` restaurado em `_apply_master_layout` — sem isso master/detail panes paravam na altura do conteúdo. **Staleness pipeline**: `core/ops/run_catalog` ganhou `is_run_stale(run)` + `resolve_status(status, last_tick_at)`. Threshold 30 min (2 ticks perdidos = morto). Aplicado em `_summary_from_local` + `_collect_single_vps_run` → status vira "stale". `_vps_running_slugs`/`_vps_running_instance_count` filtram zumbis antes de contar. `/data engines` ganhou seção STALE (amber) entre LIVE (green) e FINISHED (dim). 10 contract tests novos. **DB audit**: `aurum.db` 426 MB → 448 KB (99.9% freelist reclamado via VACUUM); `auto_vacuum=INCREMENTAL`; dedupe da `cleanup_stale_rows` (definida 2x, segunda sobrescrevia primeira). Backup em `data/aurum.db.backup-20260424_112054`. 172/172 smoke + 450 targeted tests verdes. Zero toque em CORE.
- **Arbitrage Hub v2 end-to-end operacional em código** na branch `feat/arb-hub-v2`:
  - 2 novos módulos puros `core/arb/tab_matrix.py` (CEX_VENUES + is_cex + matches_type + opps_sort_key + compact_labels) e `core/arb/lifetime.py` (LifetimeTracker + stable_key blake2b + fmt_duration), ~280 linhas + 43 testes
  - `ScoreResult` ganhou `profit_usd_per_1k_24h` + `depth_pct_at_1k` (additive, 2 helpers novos)
  - `_ARB_TAB_DEFS` 3→8 tabs (CEX-CEX / DEX-DEX / CEX-DEX / PERP-PERP / SPOT-SPOT / BASIS / POSITIONS / HISTORY) com category separator entre type e meta
  - `_ARB_LEGACY_TAB_MAP` flipped de v1 direction → v2 direction (opps→cex-cex, engine→positions, spot→spot-spot)
  - `_ARB_OPPS_COLS` 6→8 cols (dropped SCORE, adicionados PROFIT$/LIFE/DEPTH$1k)
  - `paint_opps` filtra por `matches_type(pair, _arb_active_type_tab)` + renderiza 8 cols
  - LifetimeTracker wired no paint_opps (observe_pairs + GC a cada 100 ticks + age → LIFE column)
  - `hub_telem_update` atualiza counters `(N)` por tab em cada refresh tick
  - Auto-compact: 4 níveis (0=full+counters, 1=no counters, 2=slash, 3=abbrev) via `compact_labels`
  - **Task 10 shipped**: 3 filter chips novos (PROFIT$ ≥ / LIFE ≥ / VENUES allowlist) com popovers interativos (numeric entry + checkbox). `filter_and_score` enforce os 3 novos filters, sort migrado de `_key` closure pra `opps_sort_key`
- **Plan-vs-reality gaps surfaced**: Task 5 `viab`/`breakeven_h` não existiam em main (só em chore); Task 6 `_ARB_OPPS_COLS` era criação não update; Task 7+ `launcher_support/screens/arbitrage_hub.py` só em chore. Resolvido via merge `chore/repo-cleanup` → `feat/arb-hub-v2` (opção C)
- **Subagent-driven-development executado**: 7 tasks via implementer+spec-reviewer+code-quality-reviewer flow. 2 tasks (7+8 e 9+11) combinadas por acoplamento tight. Haiku pra tarefas mecânicas 1-5; sonnet pra 6, 7+8, 9+11 (launcher.py edits)

## Commits do dia: 14 arb feat + 1 arb merge + 1 research-desk merge + 1 robustness patch + 1 sidebar polish + 7 cockpit-fill + 5 hub docs + 8 shadow-tick-fix + 11 cockpit-redesign/trade-history/audit = 49

### Cockpit fills + staleness + DB audit (branch feat/research-desk, pass 11:24)
- c71342b fix(cockpit): detail pane fills vertical — actions at bottom + paper spacer
- db4720d fix(cockpit): center empty signal-feed label — kill dead zone in middle
- a7f714c fix(launcher): pin app footer to window bottom via side='bottom'
- f684b84 fix(launcher): hide app footer for engines_live — ricos footer interno vence
- 562399a fix(engines-live): restore body.grid_rowconfigure weight=1 — panes fill vertically
- 8b58cc9 fix(run-catalog): stale running filter — kill zombie runs in /data + cockpit
- cd8ab56 chore(db): dedupe cleanup_stale_rows + VACUUM 426MB→448KB


### Shadow/paper ↔ backtest adherence (branch fix/shadow-tick-cadence)
- eb4be51 fix(shadow+paper): alinha ticks ao grid de candles + tolerance 3x
- cc44927 feat(harmonics): persistir entropy_norm + feature snapshot pos-scan
- f819cfa fix(paper+harmonics): review findings — 2 CRITICAL + 1 observability gap
- b99b8b8 chore: review findings round 2 — edge guards + integration test + CLI
- fe90ea6 feat(harmonics): bayes warm-up em live_mode — aderencia 100% com backtest
- 60f68d0 feat(audit): daily script comparando backtest replay vs live trades
- 9f2d582 fix(core): resolve submodule-vs-function shadow collision em lazy __getattr__
- 685f603 fix(audit): review final findings — window cutoff + direction norm + systemd units versioned
### Hub de orientação (branch feat/research-desk)
- 13a3e79 docs(hub): AGENTS/MEMORY/CONTEXT/SKILLS — orientation hub no root
- 6f69746 docs(hub): review fixes — engines deletados + persona stubs + AGENTS nota
- f4c51a3 docs(hub): drift check + path fixes + session/daily log
- b120d21 docs(hub): logs atualizados com routine semanal aplicada
- 5d446b1 docs(hub): README pointer + drift fixes + 5-operative alignment + drift check expansion

### Live cockpit sidebar polish (branch feat/research-desk)
- 5494071 refactor(engines-live): polish sidebar alignment + full-pane collapse


### Foundation modules (Tasks 1-4)
- 5b9396d feat(arb): add tab_matrix venue classifier
- b6b5e59 feat(arb): tab predicate dispatcher by venue/kind
- c99880e feat(arb): sort comparator + compact label helper
- 37270ce test(arb): use string color fixture matching _ARB_TAB_DEFS shape
- 78203ce feat(arb): lifetime tracker with blake2b stable key

### ScoreResult + launcher data (Tasks 5-6)
- cfb89f9 feat(arb-scoring): profit/depth fields for density view
- e809432 feat(arb): add v2 supporting data types (tab categories, legacy map, opps cols, filter defaults, lifetime tracker)

### Merge + migration + re-apply
- 014ec0e merge(chore/repo-cleanup): bring Phase-1+2+3 refactors + BRIDGEWATER under feat/arb-hub-v2
- c3d3a35 refactor(arb): migrate v2 helpers to core/arb subpackage + tests/core
- cbe2949 feat(arb-scoring): re-apply v2 density fields post-merge (profit/depth)

### Hub render + painter (Tasks 7+8, 9+11)
- ce98223 feat(arb-hub): 8-tab density layout with generic filtered renderer (Tasks 7+8)
- 06ca05e fix(launcher): remove duplicate Phase-1 _ARB_OPPS_COLS shadowing v2 8-col
- 610d61d feat(arb-hub): 8-column paint_opps with type filter + counter wiring (Tasks 9+11)
- 25243bc feat(arb-hub): profit/life/venues filter chips + persistence (Task 10)

### Merge para parent + robustness (feat/research-desk)
- 2ce5c17 merge(feat/arb-hub-v2): Arbitrage Hub v2 density — 8 tabs + filter chips + LifetimeTracker
- 28bbf1c feat(arb-scoring): robustness patches — explicit-None APR + NaN/inf guards

## Estado final
- **Suite**: 2034 passed / 72 skipped / test_arb_hub_v2 env-flake excluded (passes isolated)
- **Cockpit-focused suite**: 101 targeted tests green (paper_extras 5-tuple, aggregator dedupe+novel-filter, backfill from-vps + all engines)
- **Mudanças em CORE de trading?** **NÃO** — zero toque em core/indicators.py, core/signals.py, core/portfolio.py, config/params.py (dia inteiro)
- **VPS↔DB↔cockpit alinhados**: tunnel UP, DB 157 rows (= VPS 156 + 1 legacy), 0 VPS-running-sem-DB mismatches, 0 órfãos em disco (588 archived)
- **Backlog top**:
  - Validação clicável do hub v2 (launcher parent já roda v2 pós-merge em feat/research-desk)
  - Merge pra trunk limpa (opcional): feat/research-desk → chore/repo-cleanup → main
  - Pre-existing jump.py:89 bug (~5min fix, embora já fixado em fix/shadow-tick-cadence)
  - Posição órfã LINKUSDT SHORT da run `2026-04-21_012204` (millennium paper stopped mas VPS hb ainda running)
  - Automação do sync VPS→DB (hoje manual via `backfill_live_runs.py --from-vps`)

## Pendências pra amanhã
- **Clicar pelo hub no Tk** — `python launcher.py` no parent, validar 8 tabs + 3 popovers novos (PROFIT$ / LIFE / VENUES). Hub já é v2 por default. **Também validar visualmente cockpit sidebar (alinhamento single-line) + TRADE HISTORY (OPUSDT/RENDERUSDT devem aparecer)**.
- **BRIDGEWATER re-test OOS**: per params.py comment, o bug LIVE_SENTIMENT_UNBOUNDED fixed em 9b41c76 (2026-04-17) nunca teve re-test formal. Tem prewarm de cache agendado? ver stash@{2} de 2026-04-23_1641
- **Pre-existing jump.py:89**: `indicators` module/function collision. ~5min fix (já fixado em fix/shadow-tick-cadence commit `9f2d582`, mas não merged ainda em feat/research-desk)
- **Decisão posição órfã LINKUSDT**: paper run stopped, mas VPS carrega 1 posição aberta há 3d. Flatten via SSH/systemctl no VPS quando sobrar tempo.
- **Rotina de sync DB local**: considerar `/schedule` pra rodar `backfill_live_runs.py --from-vps --apply` a cada 5-10min em background, mantendo DB sempre espelhado. Hoje é manual.

## Nota do dia
**Missão "hub operacional hoje" — cumprida em código, full feature, landed no parent**. 11 das 12 tasks do plano v2 density entregues via subagent-driven (Task 12 foi audit, passou), 2 bundles combinados por coupling, 1 merge de chore que trouxe Phase-1+2+3 junto, merge final em feat/research-desk pushed pra origin, robustness patches (explicit-None APR + NaN/inf guards) aplicados on top. Hub agora é 8-tab com filter por tipo, 8 cols (PROFIT$/LIFE/DEPTH$1k novos), LifetimeTracker ao vivo, counter `(N)` por tab, 3 filter chips novos com popovers, e `math.isfinite()` guards protegendo contra edge cases numéricos. Stdlib only, zero deps novas, CORE files intactos.

O que mais me impressionou: cada plan-vs-reality gap levantou um pause-report-decide loop em vez de silent adaptation. Opção C (merge chore) foi tua call mas é cara — viu os 3 Phase cleanups chegarem juntos. Agora o hub v2 de fato fala a mesma língua do resto do codebase (core/arb subpackage, tests/core/, Phase-3 extração já extraída).

> "Quem está lendo e quem está sendo lido? AURUM lê. O varejo é lido." — Com 8 tabs operacionais, a leitura fica mais nítida.
