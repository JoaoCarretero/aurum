# Daily Log — 2026-04-23

## Sessões do dia
- 19:03 — rebuild do frontend ENGINES (R3): 16 tasks TDD (state/keyboard/widgets/panes/dialogs/view), subagent-driven com two-stage review, 114 passed, zero regressão, branch pronto pra R4 — [2026-04-23_1903](../sessions/2026-04-23_1903.md)
- 16:40 — rebuild do frontend ENGINES (R1+R2): brainstorm + spec + plan + 7 tasks TDD (scaffold + data/ layer: cockpit/procs/aggregate/log_tail), subagent-driven com two-stage review — [2026-04-23_1640](../sessions/2026-04-23_1640.md)
- 11:19 — caça dupla a bugs: ghost-exit do paper + shadow trades invisíveis — [2026-04-23_1119](../sessions/2026-04-23_1119.md)
- 10:38 — pós-migração OneDrive→local: venv 3.11, fix atalho, audit completo, fix 2x Ctrl+C + colisão live_runs PK, deploy VPS + migração systemd, httpx fix + baseline 1740 pass — [2026-04-23_1038](../sessions/2026-04-23_1038.md)

## Entregas principais (consolidado)
- **Migração finalizada**: venv Py 3.11 recriado com `[all,dev]` instalado via wheels; atalho do desktop apontando pra `C:\Users\Joao\projects\aurum.finance`; Defender exclusion adicionada
- **3 bugs corrigidos + 1 deploy**:
  1. Ghost-exit do paper (sessão 11:19) — fix em `_fetch_new_bars` + arquivamento de 10 runs contaminados
  2. 2x Ctrl+C necessário pra fechar runners (sessão 10:38) — handler escalonado (1º = flag, 2º = `sys.exit(130)`)
  3. Colisão de PK em `live_runs` entre per-engine paper/shadow (sessão 10:38) — `build_run_id` ganha param `engine`
- **Deploy VPS**: merge limpo origin/feat/per-engine-runners (zero conflitos), 11 procs restarted graceful SIGTERM, migrados nohup → systemd com `enable + start`
- **Defesa**: shadow trades agora aparecem em RUNS HISTORY; `except Exception: pass` removido; handler SIGINT escalonado
- **Rebuild engines frontend (R1+R2)**: spec de 495 linhas + plan de 3329 linhas + 7 tasks TDD executadas. Package `launcher_support/engines_live/` criado com 4 módulos pure data (cockpit/procs/aggregate/log_tail). 44 testes novos, 105 passed no escopo, delegates preservam compat com launcher.py. Branch `feat/engines-frontend-rebuild` pronto pra continuar em R3 (panes/widgets).
- **Rebuild engines frontend (R3 completa)**: 16 tasks TDD executadas (state, keyboard, 3 widgets, 8 panes, 2 dialogs, view orchestrator). 32 arquivos novos, ~3700 linhas adicionadas, zero arquivos modificados. 114 passed no escopo engines_live + integration. 6 bugs do plan auto-detectados pelos implementers (format_uptime kw-only, _options shadow tk.Misc, test/code mismatch engine_card, winfo_ismapped em withdrawn root, update_shelf arg missing, lambda forward-ref). Branch pronto pra R4 (wire-up launcher.py — precisa validação visual manual).

## Commits do dia: 36 (6 da manhã + 13 da sessão R1+R2 + 17 da sessão R3)
- a16fe57 — fix(paper+shadow-view): ghost-exit (sessão 11:19)
- 68a2550 (VPS cherry-pick de a16fe57)
- b65ee47 — fix(runners): 2o SIGINT forca sys.exit imediato (chore/repo-cleanup)
- 8155891 — fix(runners): 2o SIGINT forca sys.exit imediato (feat/per-engine-runners)
- f1a4ce4 — fix(per-engine): engine no RUN_ID evita colisao em live_runs PK
- 0a25031 — chore(deps): add httpx>=0.27,<1 to [dev] extras
- cfe29d1 + 3da995f — commits VPS (wip pre-merge + merge commit)

## Estado final
- Suite (main branch): 1740 passed / 8 skipped / 0 failed
- Suite (feat/engines-frontend-rebuild escopo R1+R2+R3 + integration): **114 passed / 0 failed / 0 skipped** (cenário xdist; skips esporádicos em runs isolados por Tk fixture quirk — conhecido, não-blocker)
- Mudanças em CORE de trading? **SIM (sessão 11:19)** — Bug ghost-exit afetava label_trade intrabar do paper. **NÃO (sessão 10:38 + sessão 16:40 + sessão 19:03)** — mudanças de UX/observabilidade/arquitetura, zero toque em sinais/custos/sizing.
- VPS: 12 systemd services active (11 per-engine + cockpit_api), todos enabled. Uptime 20d, 4% disco, 81% RAM livre.
- Backlog top:
  - Monitorar 2-4h os novos systemd services pra confirmar saúde (ticks, novel, auto-restart)
  - Investigar `test_cockpit_paper_endpoints.py` se cockpit paper virar critical path
  - Adicionar contract test `exit_at > entry_at` (defesa-em-profundidade — sessão 11:19)
  - Retomar consolidação do protocolo 4-engines (sessão 2026-04-22)
  - Auditar `_fetch_new_bars` em scripts per-engine (pode ter padrão duplicado do bug 11:19)

## Pendências pra amanhã
- Acompanhar próximos 24h dos paper/shadow runners systemd-managed
- Retomar protocolo-4-engines pendente
- Verificar se `novel=0` persistente nos shadows era artefato da colisão de PK ou padrão real de mercado
- **Rebuild engines frontend R4 (3 tasks, ~3h)**: re-export render + replace call sites em launcher.py + E2E visual checklist (precisa validação manual Joao rodando o launcher)
- **Rebuild engines frontend R5 (3 tasks, ~2h)**: shim engines_live_view.py + engines_sidebar.py + final suite + docs + PR

## Nota do dia
Dia de limpeza operacional profunda + reset arquitetural + rebuild
frontend ENGINES quase inteiro. Manhã foi caça a bugs + reconstrução
pós-migração. Tarde (16:40 BRT) o disco mudou de rotação: spec + plan
de 3800 linhas pra rebuild do ENGINES view, e 7 tasks TDD executadas
por subagents com two-stage review (R1+R2). Noite (19:03 BRT) voltei
e executei R3 inteira — as 16 tasks restantes — em um fluxo contínuo
de subagent-driven development: state, keyboard, 3 widgets, 8 panes,
2 dialogs, view orchestrator. Os subagents detectaram e corrigiram
6 bugs do plan durante implementação (format_uptime keyword-only,
_options shadowing tk.Misc, test/code contradição, winfo_ismapped em
withdrawn root, update_shelf signature, lambda forward-ref). 114/114
passed no escopo, zero regressão. Três fases → dois dias; amanhã
R4+R5 fecham a epic com validação visual. O CD tá quase inteiro lido.
