# Daily Log — 2026-04-20

## Sessões do dia

- 13:37 — Retomada do plano limpeza-geral: Sub-B review findings + Sub-C completion (fechamento do plano) — [session log](../sessions/2026-04-20_1337.md)
- (anteriores do dia) — Planejamento e execução de Sub-A (disco) + Sub-B (perf) + refactor de paper runner backtest-parity + cockpit polish + cleanup de VPS

## Entregas principais (consolidado)

- **Plano limpeza-geral 2026-04-20 100% fechado** (Sub-A + Sub-B + Sub-C)
- **~2GB liberados em OneDrive** (Sub-A): scripts `db_vacuum.py` + `archive_old_runs.py` + remoção de `server/website/dist/` do index
- **Perf no walk-forward e suite** (Sub-B): `core/hmm_cache.py` (memoização de `GaussianHMMNp.fit`), `core/__init__.py` lazy via PEP 562, session-scoped OHLCV fixtures em `tests/conftest.py`
- **Meanrev cluster arquivado** (Sub-C): 5 files → `_archive/` (engine + test + 3 tool scripts); `engines/millennium_live.py` com docstring expandido explicando o papel de shim de bootstrap
- **Paper runner backtest-parity** (Codex): `PaperExecutor` + `PositionManager` agora espelham `core.signals.label_trade` exato (BE + trailing + liquidation + canonical direction). 15 testes novos, 2 rewrites.

## Commits do dia: 17

Destaques (novos desta sessão): `627fd1e` (sub-B review) + `c529e05` (sub-C finish). Outros do dia cobriram sub-A/sub-B commits anteriores, plan/spec docs, paper refactor (688f5a9/18fe497 do Codex), cockpit polish, cleanup de runs.

## Estado final

- Suite: **1462 passed, 7 skipped, 2 failed**
- Smoke: **178/178**
- Os 2 failures (`test_bridgewater_contracts`, `test_deshaw_revalidation_contracts`) são do engine tuning **uncommitted** no working tree, não da limpeza
- Mudanças em CORE de trading? **NÃO** (zero toque em `indicators`/`signals`/`portfolio`/`params`)
- Backlog top:
  1. Engine tuning uncommitted (bridgewater PnL change + deshaw/kepos/medallion filters) — precisa anti-overfit protocol antes de entrar no main
  2. Paper runner VPS com 180% notional exposure (V1 scope deviation — KS fast_halt protege)
  3. Restaurar `config/keys.json` — Binance demo/testnet/live + macro_brain (FRED + NewsAPI)

## Pendências pra amanhã

- Decidir destino do engine tuning uncommitted (branch próprio com grid pre-registered, ou revert se experimental)
- Monitorar primeiros exits do paper VPS + validar Telegram TARGET/STOP + cockpit atualizando equity
- Restaurar secrets faltantes via OneDrive version history ou password manager

## Nota do dia

Limpeza-geral fechou sem tocar em CORE PROTEGIDO — o plano inteiro foi
ship-it-and-forget. Mas a sessão revelou trabalho paralelo de tuning em
4 engines no working tree que precisa passar pelo protocolo anti-overfit
antes de virar commit. Race com Codex no staging area complicou o log do
Sub-C (renames entraram num commit de paper) mas sem quebrar conteúdo.
Lição: coordenar com Codex antes de operações sensíveis ao index.
