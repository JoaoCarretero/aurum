# Daily Log — 2026-04-20

## Sessões do dia

- 14:27 — **Launcher screen manager (piloto splash entregue)** —
  brainstorm→spec→plan→exec inline de 13 tasks TDD. Infra
  `Screen`+`ScreenManager` com cache de widgets criada do zero,
  instrumentação `@timed_legacy_switch` em 9 legacy call sites pra
  baseline, `SplashScreen` migrada como piloto (build-once + swap),
  migration guide em `docs/architecture/screen_manager.md` pras
  próximas. 14 commits, 19 files novos, 34 tests novos, zero regressão.
  Resolve root cause do lag "FPS-baixo" no screen switching —
  [session log](../sessions/2026-04-20_1427.md)
- 17:22 — **Paper parity + cockpit stability + UX** — resumiu changes
  uncommitted (paper backtest-parity 15 testes + teste rewrites),
  cockpit API auto-zombie-detect (derive effective_status com threshold
  `max(tick_sec*3, 600s)`), fix launcher cleanup em `_clr()` (teardown
  de `_engines_live_handle` previne freeze pos-LIVE), collapsible
  READY/RESEARCH/EXPERIMENTAL buckets em paper, cancel timers em
  mode-switch shadow↔paper↔demo (freeze fix). Fechou com design doc
  pra multi-instance runs (6-phase plan, decisões fechadas via Q&A,
  código fica pra próxima sessão). VPS validado: paper+shadow healthy
  pra 24h monitoring (paper 20 ticks 4h57m, shadow 87 ticks 21h45m) —
  [session log](../sessions/2026-04-20_1722.md)
- 13:37 — Retomada do plano limpeza-geral: Sub-B review findings + Sub-C completion (fechamento do plano) — [session log](../sessions/2026-04-20_1337.md)
- (anteriores do dia) — Planejamento e execução de Sub-A (disco) + Sub-B (perf) + refactor de paper runner backtest-parity + cockpit polish + cleanup de VPS
- **15:26–15:42 — ScreenManager rollout (4 telas a mais)** — pós-piloto, migradas em sequência: core menu screens, macro brain, runs history, engines live. Mais `b4f37a0` fix MAIN_GROUPS→MAIN_MENU desbloqueando imports. 5 commits, 4 screens + 1 unblock.
- **16:23 — Millennium shadow/paper gating** — `ccc2e46` alinha live-signal gating entre shadow e paper; novo módulo `tools/operations/millennium_signal_gate.py` centraliza o gate. Tira duplicação e divergência silenciosa.
- **17:41 — Bridgewater robust defaults + audit/battery suite** — commit grande `22a1796` (57 files, +4449/-910): `core/sentiment.py` com defaults promovidos; audits BRIDGEWATER/DE SHAW/KEPOS criados; `tools/anti_overfit_grid.py` (+646) e 3 baterias bridgewater (regime filter, rolling compare, sentiment window). Aparenta ter vindo de outro agente (Codex provável).
- **19:18 — ScreenManager metrics persistence (Lane 1 Phase 4 retomada)** — descoberto que logger `aurum.launcher.screens` estava sem FileHandler e `runtime_health` zerava a cada relaunch; as 5 telas migradas hoje não tinham baseline empírico. Implementado `_persistence.py` (RotatingFileHandler + JSON dump no shutdown), wired em `App.__init__`/`_quit`. 1 commit (`1a50dfa`), 4 tests novos — [session log](../sessions/2026-04-20_1918.md)
- **21:30 — Root cause do paper sem trade + multi-instance Fase 1-3 + fixes UX** — [session log](../sessions/2026-04-20_2130.md).
  (1) Descoberto que `scan_symbol`/`scan_mercurio`/`scan_hermes` param em `len(df)-MAX_HOLD-2` pra label WIN/LOSS em backtest → ignoram últimos 50h de bars → paper nunca vê signal no bar atual. (2) Adicionado `live_mode=True` + `live_tail_bars=4` params (backtest bit-idêntico com default False). (3) Novo `_collect_live_signals()` em millennium.py; paper/shadow wire. (4) Multi-instance Fase 1-3: `--label` em runners, cockpit API expõe label, launcher instance picker. (5) UX: paper stop responsivo (sleep chunked 2s), sidebar collapse lateral, botão `+ NEW INSTANCE`, RUNNING count integra VPS, tunnel auto-boot com badge CFG ERR. 7 commits. Deployed VPS: paper PRIMED signals_seen=21→1, zero Telegrams fake pós-fix.

## Entregas principais (consolidado)

- **Plano limpeza-geral 2026-04-20 100% fechado** (Sub-A + Sub-B + Sub-C)
- **~2GB liberados em OneDrive** (Sub-A): scripts `db_vacuum.py` + `archive_old_runs.py` + remoção de `server/website/dist/` do index
- **Perf no walk-forward e suite** (Sub-B): `core/hmm_cache.py` (memoização de `GaussianHMMNp.fit`), `core/__init__.py` lazy via PEP 562, session-scoped OHLCV fixtures em `tests/conftest.py`
- **Meanrev cluster arquivado** (Sub-C): 5 files → `_archive/` (engine + test + 3 tool scripts); `engines/millennium_live.py` com docstring expandido explicando o papel de shim de bootstrap
- **Paper runner backtest-parity shipado** (Codex + sessão 17:22): `PaperExecutor` + `PositionManager` espelham `core.signals.label_trade` exato (BE + trailing + liquidation + canonical direction). 15 testes novos, 2 test rewrites.
- **Cockpit auto-zombie-detect**: `tools/cockpit_api.py` agora deriva status via `_effective_status(hb)` — heartbeat `running` com `last_tick_at` > `max(tick_sec*3, 600s)` reporta `stopped`. Resolve o tipo de zombie que 2026-04-20_1139 virou quando foi SIGKILL-ado sem fechar heartbeat. Deployed no VPS + restart.
- **Launcher freeze fixes (2 de 3 causas conhecidas)**:
  - `_clr()` agora teardown `_engines_live_handle` (evita pollers de cockpit brigando com GIL enquanto BACKTEST/MAIN constroem widgets)
  - `set_mode()` cancela `paper_refresh_aid` / `shadow_refresh_aid` / `shadow_after_id` antes de trocar state — shadow↔demo↔paper não trava mais
- **UX cockpit**: buckets READY LIVE / RESEARCH / EXPERIMENTAL colapsáveis (chevron click). Paper mode colapsa READY por padrão (user pain "tá ocupando muita tela").
- **Multi-instance design doc** (pra próxima sessão): `docs/superpowers/specs/2026-04-20-multi-instance-runs-design.md` com 6 fases, decisões fechadas (local-first, label nominal, coluna DB).
- **Launcher screen manager (sessão 14:27)**: `launcher_support/screens/` package novo com `Screen` ABC + `ScreenManager` (cache-based, sub-ms switching) + `SplashScreen` piloto + instrumentação `@timed_legacy_switch` em 9 legacy methods + migration guide em `docs/architecture/screen_manager.md`. Resolve lag "FPS-baixo" reportado em navegação.
- **ScreenManager — 4 telas migradas pós-piloto (15:26–15:42)**: core menu screens, macro brain, runs history, engines live. Backlog #4 do dia avançou sozinho além do previsto.
- **Millennium signal gate centralizado**: `tools/operations/millennium_signal_gate.py` — alinha gating shadow↔paper, elimina divergência silenciosa.
- **Bridgewater recalibration pack**: audit em `docs/audits/2026-04-20_bridgewater_recalibration.md`, plano em `docs/engines/bridgewater/2026-04-20_recalibration_plan.md`, 3 batteries (regime filter, rolling compare, sentiment window audit), `tools/anti_overfit_grid.py` genérico. DE SHAW e KEPOS ganharam audits+planos também.

## Commits do dia: 60

**Meus da sessão 17:22** (em ordem cronológica):
- `18fe497` feat(paper): backtest-parity — BE + trailing + liquidation + canonical direction
- `688f5a9` feat(paper): ⚠ mensagem errada — conteúdo = meanrev renames (race com Codex no index)
- `0f1ad47` fix(cockpit): derive effective status to unmask zombie runs
- `df391b0` fix(launcher): tear down ENGINES LIVE handle on screen switch
- `9d7c6e9` feat(cockpit): collapsible READY LIVE / RESEARCH / EXPERIMENTAL buckets
- `1184520` fix(cockpit): cancel prior mode refresh timers on mode switch
- `4f59d7e` docs(specs): multi-instance runs design — 6 fases pra local + DB + VPS

**Sessão 14:27 (ScreenManager refactor, 14 commits):** `6847e7b` → `bb8acbd` — spec + plan + scaffold + `Screen` ABC + `ScreenManager` (cache-miss, cache-hit, error, timing) + `@timed_legacy_switch` decorator em 9 methods + tk-real integration test + **`SplashScreen` migrated** (db8085b) + **migration guide** (bb8acbd). Piloto completo e validado.

**Pós-daily-log (14:33–17:41, 7 commits não-planejados):**
- `b4f37a0` fix(launcher): MAIN_GROUPS → MAIN_MENU (unblock imports)
- `2d57526` feat(launcher): migrate core menu screens to ScreenManager
- `c696377` feat(launcher): migrate macro brain screen to ScreenManager
- `4adbe28` feat(launcher): migrate runs history screen to ScreenManager
- `56218cd` feat(launcher): migrate engines live screen to ScreenManager
- `ccc2e46` fix(millennium): align shadow/paper live-signal gating (+ `millennium_signal_gate.py`)
- `22a1796` fix(bridgewater): robust defaults + audit research paths (57 files, +4449/-910)

## Estado final

- **Suite:** 1498 passed, 7 skipped, 2 failed durante o dia; após commit `22a1796` (bridgewater recalibration), contracts suite = **835 passed, 0 failed** (revalidado 21:58 UTC). Os 2 failures do snapshot anterior foram fechados pelo próprio commit de recalibração.
- **Smoke:** 178/178
- **VPS validado às 21:58 UTC:** paper run `2026-04-20_1225` tick #38 (ok=38, novel=35, open=0, equity=$10000, ks=NORMAL, primed=yes); shadow run `2026-04-19_1937` tick #106 (novel=664 cumulative). Shadow disparou Telegram BEARISH várias vezes entre 19:52–23:37 (JUMP 5×, CITADEL+RENAISSANCE 1×). Todos 3 serviços (cockpit_api, paper, shadow) active/running.
- ⚠ **Achado — paper sem posição em 11h live:** ~~investigar~~ ✅ **RESOLVIDO na sessão 21:30.** Root cause: engine loop para em `len(df)-MAX_HOLD-2` (precisa bars forward pra label WIN/LOSS em backtest) → nunca gera signal no bar atual. Fix: `live_mode=True` param em citadel/jump/harmonics + `_collect_live_signals()` em millennium.py. Deployed VPS. Prime count 21→1.
- Mudanças em CORE de trading? **NÃO** (zero toque em `indicators`/`signals`/`portfolio`/`params`)
- **Backlog top:**
  1. ~~Multi-instance Fase 1~~ ✅ **RESOLVIDO na sessão 21:30** — Fase 1, 2, 3 todas shipadas (`--label` runners + cockpit API expõe + launcher picker + botão `+` NEW INSTANCE).
  2. **Ghost dirs em `data/millennium_shadow/`** — hoje 51 (subiu de 29), launcher segura file handles. Investigar fonte + cleanup script.
  3. ~~2 contract tests failing~~ ✅ verificado 21:58 UTC: 835/835 contract tests passing após commit 22a1796 close.
  4. **ScreenManager** — 5 telas migradas hoje (splash + core menu + macro brain + runs history + engines live). **Métricas persistentes agora funcionam** (commit `1a50dfa`): próxima sessão, Joao abre launcher 5-10min, fecha limpo, e `data/.launcher_logs/screen_metrics_*.json` + `screens.log` ficam prontos pra análise `ms_avg × frequência`.
  5. **Commit 688f5a9** — mensagem errada permanece (não vale reescrever history)
  6. **Live_mode gap de gates dinâmicos** (streak_cooldown / dd_scale com account evoluído / portfolio_allows correlação) — live hoje é mais agressivo que backtest nesses 4 aspectos. Runtime paper_executor + KS gate cobrem parcialmente. Fechar gap requer trackear state no paper_executor. Fase separada.
  7. **Tunnel auto-heal mid-run** — se keys.json é consertada enquanto launcher roda, config in-memory continua stale. Precisa reload no watchdog.
  8. **Shadow instance picker no launcher** — só paper tem hoje. Shadow seguiria o mesmo padrão de `_active_paper_runs` + picker widget.

## Pendências pra amanhã

- **Verificar VPS após 24h** (checkpoint 03:22 CEST do dia 21): se paper abriu posição real (equity ≠ $10,000), pipeline end-to-end validado. Se zero trades, investigar frequência de setup vs filtros. Shadow tem que ter disparado Telegrams limpos (sem fakes históricos).
- **Investigar ghost dirs** quando launcher estiver fechado (pra liberar file handles).
- **Coletar métricas do ScreenManager:** abrir launcher 5-10min navegando telas, fechar limpo, rodar script de análise (a construir) sobre `data/.launcher_logs/screen_metrics_*.json` + `screens.log` pra ranquear próxima migração por `ms_avg × freq`.
- **Integração com Codex live_runs refactor**: commits `64ff2c6` (migration 001) + `959aea9` (db_live_runs API) + docs já prontos. Próxima sessão: aplicar migration em `aurum.db` e migrar paper/shadow runners pra upsertar no DB em vez de só JSON.

## Nota do dia

Dia de dois halves. **Manhã/tarde-cedo:** limpeza-geral fechou 100%
sem tocar em CORE PROTEGIDO — ship-it-and-forget. **Tarde/noite:** sessão
longa de "voltar de onde parei" virou cadeia de fixes (paper parity +
cockpit zombie-detect + launcher freeze + UX colapsáveis + mode-switch
freeze) mais design doc pra multi-instance (fases fechadas, código
só na próxima sessão).

Padrão do dia: zero toque em CORE PROTEGIDO (indicators/signals/portfolio/params)
em AMBAS as sessões. Tudo isolado em paper runner, cockpit API, launcher UX.
Rastro cirúrgico, backtest calibrados continuam válidos.

**Lição do incidente 688f5a9:** race com Codex no staging area pode
levar meu commit a ganhar o conteúdo de outro agente. Próxima vez que
for committar algo sensível, verificar `git diff --cached` imediatamente
antes do `git commit` pra detectar mudanças surpresa.

**Paper e shadow rodando 24h a partir de agora** — amanhã validamos
se algum signal live passou, se Telegram dispara exits, se KS halts
funcionam em real-time.
