# Daily Log — 2026-04-21

## Sessões do dia
- 10:03 — Diagnóstico total paper/shadow, 4 bugs corrigidos (priming, ts futuro, crash-loop service, hedge acidental) — [session log](../sessions/2026-04-21_1003.md)

## Entregas principais (consolidado)
- Paper agora abre trade no 1º tick (antes esperava 15min = zero trade em runs curtos)
- Portfolio gate V2 impede hedge acidental cross-engine (JUMP + RENAISSANCE opostos)
- `ts` futuro no Telegram do shadow clampado (cosmético)
- Service paper resiliente a `/etc/aurum/paper.env` ausente (fim do crash-loop)
- Evidência end-to-end: 1 tick real contra Binance abrindo posição (`data/millennium_paper/2026-04-21_125102/`)

## Commits do dia: 2
- b6b64a7 fix(paper): portfolio gate V2 — bloquear direções opostas cross-engine
- 6cf3a7a fix(paper+shadow): priming, ts futuro e crash-loop do service

## Estado final
- Smoke: 178/178 passed
- Suite crítica: 238 passed, 7 skipped
- Mudanças em CORE de trading? **SIM** — portfolio gate V2 + priming semantics em paper
  (core protegido `indicators.py` / `signals.py` / `portfolio.py` / `params.py` **intocados**)
- Backlog top: (1) confirmar no VPS que o ciclo completo roda sem SIGTERM via journalctl,
  (2) bug do mkdir em import-time do millennium_paper, (3) simetria shadow × paper
  (opcional — shadow é sinal puro por design)

## Pendências pra amanhã
- Puxar fixes no VPS e monitorar journalctl por 1h pra validar Fix 2B em produção
- Se SIGTERM persistir, investigar `MemoryMax=1G` vs pico de uso do paper
- Considerar mover mkdir pra dentro de `run_paper()` em vez de import-time

## Nota do dia
Diagnóstico sistemático salva. Rodar systematic-debugging em 4 fases (root cause →
pattern → hypothesis → fix) pegou 4 bugs em uma hora onde um único "commit e reza"
teria corrigido talvez 1. O bug do hedge acidental cross-engine só apareceu porque
o João achou estranho receber JUMP SHORT + RENAISSANCE LONG no mesmo LINKUSDT —
alguém olhando um dashboard sem atenção teria deixado passar.
